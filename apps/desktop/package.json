{
  "name": "@rolester/desktop",
  "version": "0.2.2",
  "private": true,
  "type": "module",
  "description": "Thin Electron shell over the Rolester local server (M5 paid-POC deliverable).",
  "author": "CodesWhat",
  "main": "main.mjs",
  "scripts": {
    "dev": "node scripts/brand-dev-app.mjs && electron .",
    "stage": "node scripts/stage.mjs",
    "dist": "npm --prefix ../.. run app:build && npm run stage && electron-builder --mac dmg"
  },
  "devDependencies": {
    "electron": "43.0.0",
    "electron-builder": "^26.15.3"
  }
}
