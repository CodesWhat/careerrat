---
phase: 04-runtime-routing
plan: 05
type: execute
wave: 5
depends_on:
  - "04-04"
files_modified:
  - apps/web/src/onboarding/steps/FinishStep.jsx
  - apps/web/src/onboarding/steps/FinishStep.test.jsx
  - .planning/architecture/runtime-routing-policy.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements:
  - RUNT-01
  - RUNT-02
  - RUNT-03
must_haves:
  truths:
    - "Discovery quick-start and next handoffs still start or reuse visible chat sessions only when the user chooses them."
    - "Docs identify local company proposals as the default app path, discovery chat as explicit agent-led workflow, and POST /api/skill/run as retained allowlisted full runtime."
    - "Regression commands cover runtime config, CompaniesStep local routing, FinishStep handoffs, and Phase 3 proposal routes together."
  artifacts:
    - path: "apps/web/src/onboarding/steps/FinishStep.jsx"
      provides: "Runtime-capability-aware explicit discovery handoff controls"
      exports: ["runQuickStartHandoff", "runNextDiscoveryHandoff", "FinishStep"]
    - path: "apps/web/src/onboarding/steps/FinishStep.test.jsx"
      provides: "Regression tests for quick-start/next explicit chat handoffs"
    - path: ".planning/architecture/runtime-routing-policy.md"
      provides: "Project-local policy documenting Phase 4 route split"
    - path: "docs/ARCHITECTURE.md"
      provides: "Public architecture language for deterministic, chat, and retained skill runtime layers"
  key_links:
    - from: "apps/web/src/onboarding/steps/FinishStep.jsx"
      to: "apps/web/src/lib/api.js"
      via: "startDiscoveryQuickStart and startDiscoveryNext"
      pattern: "startDiscovery"
    - from: "apps/web/src/onboarding/steps/FinishStep.jsx"
      to: "apps/web/src/onboarding/ChatPanel.jsx"
      via: "explicit chat render only after handoff response"
      pattern: "ChatPanel"
    - from: "docs/ARCHITECTURE.md"
      to: ".planning/architecture/runtime-routing-policy.md"
      via: "same route class language"
      pattern: "full skill runtime"
---

<objective>
Preserve explicit agent-led discovery handoffs and close the route-policy docs around the new local default.

Purpose: RUNT-03 requires chat handoffs to remain available as a user-selected workflow, and RUNT-01 requires the retained full skill runtime to stay documented and allowlisted rather than removed.
Output: FinishStep regressions plus concise docs tying local proposals, explicit chat handoff, and retained full skill runtime together.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-runtime-routing/04-RESEARCH.md
@.planning/phases/04-runtime-routing/04-VALIDATION.md
@.planning/phases/04-runtime-routing/04-PATTERNS.md
@.planning/phases/04-runtime-routing/04-01-SUMMARY.md
@.planning/phases/04-runtime-routing/04-02-SUMMARY.md
@.planning/phases/04-runtime-routing/04-03-SUMMARY.md
@.planning/phases/04-runtime-routing/04-04-SUMMARY.md
@apps/web/src/onboarding/steps/FinishStep.jsx
@apps/web/src/onboarding/steps/FinishStep.test.jsx
@src/cli/discovery-route.mjs
@tests/discovery-route.test.mjs
@tests/chat-runtime.test.mjs
@.planning/architecture/runtime-routing-policy.md
@docs/ARCHITECTURE.md
</context>

## Artifacts this phase produces

- `src/cli/skill-run-route.mjs`: expanded `GET /api/runtime/config` payload fields `skills`, `chatSkills`, `ai.available`, `ai.route`, `discovery.companyProposals`, `discovery.manualCompanySeeds`, and `discovery.chatHandoffs`.
- `apps/web/src/lib/api.js`: wrappers `getRuntimeConfig`, `createCompanyProposals`, `getCompanyProposals`, and `decideCompanyProposal`.
- `apps/web/src/onboarding/OnboardingPage.jsx`: runtime capability loading and propagation to onboarding steps.
- `apps/web/src/onboarding/steps/CompaniesStep.jsx`: local company proposal default UX, proposal create/read helpers, proposal decision helpers, and secondary `ChatPanel` handoff.
- `apps/web/src/onboarding/steps/CompaniesStep.test.jsx`: local proposal default, no hidden chat escalation, manual/no-AI degradation, and decision-state coverage.
- `apps/web/src/onboarding/steps/FinishStep.jsx`: explicit discovery handoff gating from runtime capabilities while preserving quick-start and next-chat flows.
- `apps/web/src/onboarding/steps/FinishStep.test.jsx`: quick-start/next handoff regressions after runtime capability propagation.
- `.planning/architecture/runtime-routing-policy.md` and `docs/ARCHITECTURE.md`: routing docs for local default, explicit chat, and retained full skill runtime.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve FinishStep explicit discovery handoffs</name>
  <files>apps/web/src/onboarding/steps/FinishStep.jsx, apps/web/src/onboarding/steps/FinishStep.test.jsx</files>
  <read_first>
    @apps/web/src/onboarding/steps/FinishStep.jsx
    @apps/web/src/onboarding/steps/FinishStep.test.jsx
    @apps/web/src/onboarding/OnboardingPage.jsx
    @src/cli/discovery-route.mjs
    @tests/discovery-route.test.mjs
  </read_first>
  <behavior>
    - Test: runtime capability with `discoveryChatHandoffs:false` hides quick-start/next chat CTAs but keeps Write config and manual finish links visible.
    - Test: runtime capability with `discoveryChatHandoffs:true` preserves runQuickStartHandoff and runNextDiscoveryHandoff route calls and returned ChatPanel rendering.
    - Test: guidance outside research-boards, discover-companies, or search-jobs is still ignored.
  </behavior>
  <action>
    Update FinishStep to accept `runtimeCapabilities` from OnboardingPage. Compute discovery chat availability from `runtimeCapabilities.discoveryChatHandoffs` when present, falling back to the existing `aiEnabled` prop for direct tests/legacy callers. Keep `runQuickStartHandoff`, `runNextDiscoveryHandoff`, `extractDiscoveryGuidance`, `/api/discovery/quick-start`, `/api/discovery/next`, and ChatPanel behavior intact. Add or update FinishStep tests for capability-gated handoff visibility and existing quick-start/next helper behavior.
  </action>
  <verify>
    <automated>npm --workspace apps/web run test -- src/onboarding/steps/FinishStep.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    FinishStep still calls the quick-start and next discovery routes only after explicit button actions, and chat CTAs are hidden when runtime config says chat handoffs are unavailable.
  </acceptance_criteria>
  <done>FinishStep handoff regressions pass with runtime capability gating.</done>
</task>

<task type="auto">
  <name>Task 2: Update runtime routing docs</name>
  <files>.planning/architecture/runtime-routing-policy.md, docs/ARCHITECTURE.md</files>
  <read_first>
    @.planning/architecture/runtime-routing-policy.md
    @docs/ARCHITECTURE.md
    @.planning/phases/04-runtime-routing/04-RESEARCH.md
    @.planning/phases/03-company-discovery-api/03-VERIFICATION.md
  </read_first>
  <action>
    Update the routing policy to name the Phase 4 default: app company discovery controls call local proposal create/read/decision routes before chat. Preserve the existing policy that `/api/discovery/quick-start`, `/api/discovery/next`, `/api/chat/*`, ChatPanel, and POST /api/skill/run remain available for explicit user-led or retained full-runtime workflows. Update docs/ARCHITECTURE.md with concise layer language for deterministic/local API, bounded AI, conversational chat handoff, and retained full skill runtime. Keep docs domain-neutral and avoid private compensation examples.
  </action>
  <verify>
    <automated>rg -n "company-proposals|company-proposal-decisions|/api/discovery/quick-start|/api/discovery/next|/api/chat|/api/skill/run" .planning/architecture/runtime-routing-policy.md docs/ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    Docs distinguish the local proposal default, explicit chat handoff, and retained full skill runtime without suggesting hidden fallback from local proposal errors to chat.
  </acceptance_criteria>
  <done>Routing docs contain the Phase 4 split and retained runtime paths.</done>
</task>

<task type="auto">
  <name>Task 3: Run cross-surface runtime routing regression gate</name>
  <files>apps/web/src/onboarding/steps/FinishStep.test.jsx, .planning/architecture/runtime-routing-policy.md, docs/ARCHITECTURE.md</files>
  <read_first>
    @.planning/phases/04-runtime-routing/04-VALIDATION.md
    @tests/discovery-route.test.mjs
    @tests/company-proposals-route.test.mjs
    @tests/company-proposal-decisions.test.mjs
    @tests/skill-run-route.test.mjs
    @tests/chat-runtime.test.mjs
    @tests/skill-runtime.test.mjs
  </read_first>
  <action>
    Run the focused backend and frontend gates from 04-VALIDATION.md. Also run targeted static scans over touched app/runtime files for retained-route boundaries: local proposal code must reference Phase 3 proposal routes and must not introduce a new POST /api/skill/run caller; skill-run-route must still expose POST /api/skill/run. Record any failing unrelated pre-existing tests precisely in the SUMMARY rather than broadening scope.
  </action>
  <verify>
    <automated>node --test tests/discovery-route.test.mjs tests/company-proposals-route.test.mjs tests/company-proposal-decisions.test.mjs tests/skill-run-route.test.mjs tests/chat-runtime.test.mjs tests/skill-runtime.test.mjs && npm --workspace apps/web run test -- src/onboarding/steps/CompaniesStep.test.jsx src/onboarding/steps/FinishStep.test.jsx src/onboarding/OnboardingPage.test.jsx && rg -n "/api/skill/run" src/cli/skill-run-route.mjs && rg -n "company-proposals|company-proposal-decisions" apps/web/src/lib/api.js apps/web/src/onboarding/steps/CompaniesStep.jsx</automated>
  </verify>
  <acceptance_criteria>
    Focused backend and frontend gates pass; static scans prove retained full runtime still exists and CompaniesStep local proposal paths use Phase 3 routes.
  </acceptance_criteria>
  <done>SUMMARY records the full focused regression result and any scoped exceptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to discovery handoff routes | Explicit user action can start or reuse a chat session. |
| docs to implementation | Route policy guides future implementers and must not imply hidden escalation. |
| retained skill runtime | Full tool runtime remains powerful and must stay allowlisted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16 | Elevation of Privilege | Discovery handoff controls | high | mitigate | FinishStep tests prove chat starts only from explicit quick-start/next actions and uses existing allowlisted chat runtime. |
| T-04-17 | Repudiation | Runtime routing docs | medium | mitigate | Docs name local default, explicit chat, and retained full runtime separately. |
| T-04-18 | Information Disclosure | Docs/examples | low | mitigate | Keep examples domain-neutral and avoid private compensation values or keys. |
| T-04-19 | Tampering | Proposal route docs | low | mitigate | Docs point confirmed writes to Phase 3 decision route/source-config owners, not React-side state edits. |
</threat_model>

<verification>
Run `node --test tests/discovery-route.test.mjs tests/company-proposals-route.test.mjs tests/company-proposal-decisions.test.mjs tests/skill-run-route.test.mjs tests/chat-runtime.test.mjs tests/skill-runtime.test.mjs && npm --workspace apps/web run test -- src/onboarding/steps/CompaniesStep.test.jsx src/onboarding/steps/FinishStep.test.jsx src/onboarding/OnboardingPage.test.jsx`.
</verification>

<success_criteria>
- FinishStep handoffs remain explicit and capability-gated.
- Docs describe all three retained paths: local proposals, chat handoffs, and POST /api/skill/run.
- Focused Phase 04 runtime-routing regressions pass.
- No hidden fallback from local proposal errors to chat/full skill runtime is introduced.
</success_criteria>

<output>
Create `.planning/phases/04-runtime-routing/04-05-SUMMARY.md` when done.
</output>
