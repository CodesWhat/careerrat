---
phase: 06-canonical-db-app-shell
plan: 09
type: execute
wave: 3
depends_on:
  - 06-08
files_modified:
  - tests/db-app-shell-regression.test.mjs
  - apps/web/src/onboarding/steps/WelcomeStep.test.jsx
  - apps/web/src/onboarding/steps/WelcomeStep.jsx
  - src/cli/tracker-dev.mjs
autonomous: true
gap_closure: true
requirements:
  - APP-01
  - APP-04
user_setup: []
must_haves:
  truths:
    - "APP-01: `/app/onboarding` no longer advertises the legacy `/onboard` static page as normal UX per D-01 and D-02."
    - "APP-01: tracker-dev route/help copy labels `/onboard`, `/search`, `/packet`, `/evaluate`, and `/answer` as explicit compatibility/debug/export surfaces, not retained utility pages, per D-01 and D-02."
    - "APP-04: Static regression coverage scans normal React product pages for legacy static-page affordances, not only NavList, per D-09 and D-10."
  artifacts:
    - tests/db-app-shell-regression.test.mjs
    - apps/web/src/onboarding/steps/WelcomeStep.test.jsx
    - apps/web/src/onboarding/steps/WelcomeStep.jsx
    - src/cli/tracker-dev.mjs
  key_links:
    - "WelcomeStep.jsx -> WelcomeStep.test.jsx verifies normal React onboarding has no legacy `/onboard` affordance."
    - "React product-page static guard -> tests/db-app-shell-regression.test.mjs catches legacy static-page links outside NavList."
    - "tracker-dev compatibility route copy -> tests/db-app-shell-regression.test.mjs verifies explicit compatibility/debug/export framing."
---

<objective>
Close the APP-01 verification gap where `/app/onboarding` still promotes the legacy `/onboard` static page and tracker-dev still frames static pages as retained utility UX.

Purpose: Phase 6 cannot be verified while normal React onboarding points users back to compatibility pages or while route/help copy treats static compatibility pages as product utilities.
Output: RED/GREEN/REFACTOR gap closure for onboarding legacy affordances, tracker-dev compatibility-page wording, and a broader React product-page static guard.
</objective>

## Phase Goal

**As a** CareerRat app user, **I want to** complete onboarding inside the canonical React `/app` surface, **so that** static compatibility pages do not appear as normal product choices.

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-canonical-db-app-shell/06-CONTEXT.md
@.planning/phases/06-canonical-db-app-shell/06-RESEARCH.md
@.planning/phases/06-canonical-db-app-shell/06-VALIDATION.md
@.planning/phases/06-canonical-db-app-shell/06-VERIFICATION.md
@.planning/phases/06-canonical-db-app-shell/06-04-SUMMARY.md
@.planning/phases/06-canonical-db-app-shell/06-08-SUMMARY.md
@AGENTS.md
@apps/web/src/onboarding/steps/WelcomeStep.jsx
@apps/web/src/app-shell/NavList.test.jsx
@tests/db-app-shell-regression.test.mjs
@src/cli/tracker-dev.mjs
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Make Electron/React `/app` DB-source-of-truth and remove compatibility surfaces from product paths | 06-09, 06-10 | COVERED | Gap plans close the onboarding surfaces missed by the original 06-08 rollup.
REQ | APP-01 | `/app` is canonical product surface; compatibility surfaces are not normal UX | 06-09 | COVERED | Removes `/app/onboarding` legacy `/onboard` affordance and reframes static pages as compatibility/debug/export.
REQ | APP-02 | Dashboard, packet, tracker/activity, scanner context, and source setup views read DB-derived snapshots | 06-10 | COVERED | Sibling gap plan moves onboarding source readiness to DB source config.
CONTEXT | D-01 | Remove legacy product affordances from app path | 06-09 | COVERED | WelcomeStep and tracker-dev route copy stop advertising legacy static pages as normal flows.
CONTEXT | D-02 | Legacy/generated surfaces only debug/export utilities | 06-09 | COVERED | tracker-dev static pages are explicit compatibility/debug/export surfaces.
CONTEXT | D-04 | Product routes DB-derived and fail closed without DB | 06-10 | COVERED | Sibling plan derives source readiness from SQLite source config.
CONTEXT | D-06 | App-visible source setup converges on DB-derived snapshots or DB verbs | 06-10 | COVERED | Sibling plan updates `/api/onboard/state` and FinishStep readiness.
CONTEXT | D-07 | Move source setup seams DB-first now | 06-10 | COVERED | Sibling plan removes DB-mode readiness dependency on `config/search-sources.yml`.
CONTEXT | D-09 | Aggressive static regression guards | 06-09 | COVERED | This plan expands static guard coverage beyond NavList into normal React onboarding pages.
CONTEXT | D-10 | Narrow debug/export allowlists | 06-09 | COVERED | This plan requires static compatibility pages to be named compatibility/debug/export surfaces.
VERIFICATION | APP-01-gap | `/app/onboarding` advertises legacy `/onboard`; tracker-dev help/route copy lists static pages as retained utilities | 06-09 | COVERED | Directly addresses both artifacts named in `06-VERIFICATION.md`.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Expand legacy-affordance regression coverage</name>
  <files>tests/db-app-shell-regression.test.mjs, apps/web/src/onboarding/steps/WelcomeStep.test.jsx</files>
  <read_first>
    - tests/db-app-shell-regression.test.mjs
    - apps/web/src/onboarding/steps/WelcomeStep.jsx
    - apps/web/src/app-shell/NavList.test.jsx
    - src/cli/tracker-dev.mjs
  </read_first>
  <behavior>
    - Rendering `WelcomeStep` does not expose a normal link to the legacy static onboarding page.
    - The static guard scans normal React product pages for legacy static-page anchors and labels, not only `NavList.jsx`.
    - tracker-dev does not frame `/onboard`, `/search`, `/packet`, `/evaluate`, or `/answer` as retained utility pages.
  </behavior>
  <action>Create `apps/web/src/onboarding/steps/WelcomeStep.test.jsx` with Vitest and `renderToStaticMarkup`; render `WelcomeStep` with a minimal state and no-op `goNext`, then assert the canonical start button remains while the legacy static onboarding link and classic-page wording are absent. Extend `tests/db-app-shell-regression.test.mjs` with an explicit normal React product-page list that includes onboarding step components such as `WelcomeStep.jsx` and `FinishStep.jsx`; add comment-stripped assertions that React product pages do not contain direct legacy static-page anchors or normal-UX labels for `/onboard`, `/search`, `/packet`, `/evaluate`, `/answer`, or `/tracker`. Add a tracker-dev copy/classification assertion that fails while the help/404 text groups these static pages under retained utility pages instead of explicit compatibility/debug/export surfaces. Keep the existing generated-file guard intact per D-09 and D-10.</action>
  <acceptance_criteria>
    - `apps/web/src/onboarding/steps/WelcomeStep.test.jsx` exists and fails against current `WelcomeStep.jsx` because it renders an `href` to `/onboard`.
    - `tests/db-app-shell-regression.test.mjs` scans normal React product pages for legacy static-page affordances, not only NavList.
    - The static guard fails if `/onboard`, `/search`, `/packet`, `/evaluate`, `/answer`, or `/tracker` is advertised from normal React product pages as a legacy/static/classic page affordance.
    - The tracker-dev guard fails while help/route copy lists `/onboard`, `/search`, `/packet`, `/evaluate`, and `/answer` as retained utility pages rather than explicit compatibility/debug/export surfaces.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'node --test tests/db-app-shell-regression.test.mjs; db_status=$?; npm --workspace apps/web run test -- src/onboarding/steps/WelcomeStep.test.jsx; web_status=$?; test $db_status -ne 0 && test $web_status -ne 0'</automated>
  </verify>
  <done>APP-01 has RED coverage for the missing items: remove/demote the `/onboard` link from normal React onboarding and guard normal React product pages against legacy static-page affordances.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN/REFACTOR: Remove onboarding fallback link and reframe static pages</name>
  <files>apps/web/src/onboarding/steps/WelcomeStep.jsx, src/cli/tracker-dev.mjs</files>
  <read_first>
    - apps/web/src/onboarding/steps/WelcomeStep.jsx
    - apps/web/src/onboarding/steps/WelcomeStep.test.jsx
    - src/cli/tracker-dev.mjs
    - tests/db-app-shell-regression.test.mjs
  </read_first>
  <behavior>
    - `WelcomeStep` keeps the canonical `Get started` action and no longer links to the legacy static onboarding page.
    - tracker-dev help and not-found copy present `/app` as the product route and any retained static pages as compatibility/debug/export surfaces.
    - Existing debug/export generated dashboard routes remain available and audited through their existing allowlist.
  </behavior>
  <action>After confirming the new tests are RED, remove the normal `/onboard` anchor from `WelcomeStep.jsx`; keep the canonical start button, error handling, and SQLite/setup copy. In `src/cli/tracker-dev.mjs`, replace the "Retained utility pages and APIs" framing with explicit compatibility/debug/export grouping for the static byte pages named in the verification gap. Add or update route metadata such as a `STATIC_COMPATIBILITY_ROUTES` list or equivalent auditable structure for `/evaluate`, `/answer`, `/onboard`, `/search`, and `/packet`; use that structure in help and 404 copy so these paths are not described as first-run/product UX. Preserve `/chat` and local API route descriptions as explicit user-selected chat/API handoffs, not generated-file debug/export routes. Run the new tests and refactor only wording/classification helpers needed to keep the route table auditable per D-01, D-02, D-09, and D-10.</action>
  <acceptance_criteria>
    - `WelcomeStep.jsx` no longer renders `href="/onboard"` or "Prefer the classic step-by-step page?" in the React `/app/onboarding` flow.
    - tracker-dev help/route copy no longer lists `/onboard`, `/search`, `/packet`, `/evaluate`, and `/answer` as retained utility pages; retained mentions label them explicit compatibility/debug/export surfaces.
    - Existing `/app`, `/api/data/*`, `/api/search/*`, `/api/packet*`, `/api/onboard/*`, `/api/chat/*`, and debug/export generated dashboard routes remain mounted.
    - `node --test tests/db-app-shell-regression.test.mjs` and `npm --workspace apps/web run test -- src/onboarding/steps/WelcomeStep.test.jsx` pass.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/db-app-shell-regression.test.mjs && npm --workspace apps/web run test -- src/onboarding/steps/WelcomeStep.test.jsx</automated>
  </verify>
  <done>APP-01 is closed for the onboarding legacy-affordance gap, and APP-04 now guards normal React product pages beyond NavList.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> React `/app/onboarding` | User-facing onboarding copy determines whether the static compatibility page appears to be normal product UX. |
| Local browser -> tracker-dev route discovery | Help and 404 route copy describe which local routes are product, compatibility, debug, or export surfaces. |
| Static guard -> product React pages | Tests scan source text to prevent future legacy static-page affordances from returning to normal React pages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-15 | Spoofing | `WelcomeStep.jsx` | medium | mitigate | Remove the legacy static onboarding link and add a component regression test for the canonical React flow. |
| T-06-16 | Spoofing | `tracker-dev.mjs` help/404 copy | medium | mitigate | Classify retained static pages as compatibility/debug/export surfaces instead of retained utility product pages. |
| T-06-17 | Tampering | `tests/db-app-shell-regression.test.mjs` product-page guard | medium | mitigate | Expand static guard coverage to normal React product pages beyond NavList. |
| T-06-SC | Tampering | npm installs | high | mitigate | No package-manager install tasks are planned; executors must not add or upgrade packages in this gap closure. |
</threat_model>

<verification>
<automated>node --test tests/db-app-shell-regression.test.mjs && npm --workspace apps/web run test -- src/onboarding/steps/WelcomeStep.test.jsx</automated>
<automated>node --test tests/dashboard-route.test.mjs tests/data-route.test.mjs tests/packet-route.test.mjs tests/search-route.test.mjs tests/boards-route.test.mjs tests/desktop-routing.test.mjs tests/db-app-shell-regression.test.mjs && npm --workspace apps/web run test -- src/app-shell/NavList.test.jsx src/onboarding/steps/WelcomeStep.test.jsx</automated>
</verification>

<success_criteria>
- The `/app/onboarding` welcome step no longer advertises the legacy `/onboard` static page as normal UX.
- tracker-dev route/help copy treats `/onboard`, `/search`, `/packet`, `/evaluate`, and `/answer` as explicit compatibility/debug/export surfaces when retained.
- A regression guard scans normal React product pages for legacy static-page affordances, not only NavList.
- No packages are added or upgraded.
</success_criteria>

<output>
Create `.planning/phases/06-canonical-db-app-shell/06-09-SUMMARY.md` when done.
</output>
