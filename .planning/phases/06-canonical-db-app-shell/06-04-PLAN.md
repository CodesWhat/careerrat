---
phase: 06-canonical-db-app-shell
plan: 04
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - apps/web/src/app-shell/NavList.jsx
  - apps/web/src/App.jsx
  - src/cli/tracker-dev.mjs
autonomous: true
requirements:
  - APP-01
  - APP-03
  - APP-04
user_setup: []
must_haves:
  truths:
    - "APP-01: /app normal navigation contains only SPA product routes and no Classic or legacy static-page product links per D-01, D-02, and D-03."
    - "APP-03: tracker-dev starts and serves /app without requiring generated tracker export files as product prerequisites per D-04 and D-05."
    - "APP-04: Remaining compatibility routes are named debug/export routes and pass the static allowlist guard per D-09 and D-10."
  artifacts:
    - apps/web/src/app-shell/NavList.jsx
    - apps/web/src/App.jsx
    - src/cli/tracker-dev.mjs
  key_links:
    - "NavList.jsx -> NavList.test.jsx"
    - "tracker-dev.mjs DEBUG_EXPORT_ROUTES -> tests/db-app-shell-regression.test.mjs"
---

<objective>
Retire legacy product navigation and classify compatibility routes as debug/export-only.

Purpose: The visible app shell and dev-server route metadata must make /app the canonical product surface while preserving needed compatibility utilities behind auditable names.
Output: Green APP-01/APP-04 nav and static-guard behavior without product dependency on generated tracker exports.
</objective>

## Phase Goal

**As a** CareerRat app user, **I want to** land in the React app without legacy product affordances, **so that** compatibility exports cannot masquerade as the normal UX.

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-canonical-db-app-shell/06-CONTEXT.md
@.planning/phases/06-canonical-db-app-shell/06-RESEARCH.md
@.planning/phases/06-canonical-db-app-shell/06-VALIDATION.md
@AGENTS.md
@docs/ARCHITECTURE.md
@apps/web/src/app-shell/NavList.jsx
@apps/web/src/App.jsx
@src/cli/tracker-dev.mjs
@tests/db-app-shell-regression.test.mjs
@apps/web/src/app-shell/NavList.test.jsx
</context>

## Artifacts This Phase Produces

- New tests: `tests/db-app-shell-regression.test.mjs`, `apps/web/src/app-shell/NavList.test.jsx`, `tests/scanner-seen-set-db.test.mjs`, `tests/db-scan-context.test.mjs`.
- Updated tests: `tests/packet-route.test.mjs`, `tests/boards-route.test.mjs`, `tests/search-route.test.mjs`.
- New DB scanner helper file: `src/core/db/scan-context.mjs`.
- New internal symbols: `DEBUG_EXPORT_ROUTES`, `isDebugExportRoute`, `readPacketApplicationsFromDb`, `readDbSearchSources`, `writeDbSearchSources`, `readDbSearchResults`, `hasConfiguredDbSourcesOnly`, `buildDbSeenSets`, `readDbScannerRows`, `buildSeenSetsForRun`.
- Changed route contracts: `GET /api/packet/list`, `GET /api/packet`, `POST /api/boards/add`, `POST /api/search/scan`, `GET /api/search/results`, `GET /api/search/sources`.
- Removed normal product affordance: the `Classic` external nav item from `/app`.
- New CLI flags: none.
- New DB fields or migrations: none.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove Classic from normal React app navigation</name>
  <files>apps/web/src/app-shell/NavList.jsx, apps/web/src/App.jsx</files>
  <read_first>
    - apps/web/src/app-shell/NavList.jsx
    - apps/web/src/App.jsx
    - apps/web/src/app-shell/NavList.test.jsx
    - apps/web/src/components/icons.jsx
  </read_first>
  <behavior>
    - `NavList` renders Home, Settings, Onboarding, Inbox, Jobs, Calendar, Network, and Library as normal product nav items.
    - `NavList` does not render `Classic` and does not render a normal link to the legacy tracker route.
    - `App.jsx` route comments describe /app routes as canonical product routes without framing legacy tracker as normal navigation.
  </behavior>
  <action>After confirming `NavList.test.jsx` is RED, remove the `ExternalNavItem` render, remove its helper if unused, remove the unused `ListIcon` import, and update adjacent route comments in `NavList.jsx` and `App.jsx` so they describe `/app` as the canonical product shell per D-01, D-02, and D-03. Preserve the Inbox badge behavior and every existing SPA route item.</action>
  <acceptance_criteria>
    - `apps/web/src/app-shell/NavList.jsx` no longer imports `ListIcon`.
    - `NavList` renders no `Classic` label and no external nav item to the legacy tracker route.
    - `apps/web/src/App.jsx` no longer describes the legacy tracker as reachable through normal nav.
    - `npm --workspace apps/web run test -- src/app-shell/NavList.test.jsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace apps/web run test -- src/app-shell/NavList.test.jsx</automated>
  </verify>
  <done>/app primary navigation is DB-product-only and the APP-01 nav regression test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify tracker-dev compatibility routes as debug/export-only</name>
  <files>src/cli/tracker-dev.mjs</files>
  <read_first>
    - src/cli/tracker-dev.mjs
    - tests/db-app-shell-regression.test.mjs
    - docs/ARCHITECTURE.md
  </read_first>
  <behavior>
    - `tracker-dev.mjs` exposes compatibility/static generated-file routes through named debug/export route constants.
    - `/app` can be served without `workspace/tracker.json` being present.
    - Help and 404 copy present `/app` as the product route and compatibility generated-file routes as debug/export utilities.
  </behavior>
  <action>Introduce `DEBUG_EXPORT_ROUTES` as an audited constant listing legacy generated-dashboard routes, raw workspace feed routes, and raw storage-adapter API feed routes that remain debug/export utilities per D-02 and D-10. Add `isDebugExportRoute(url)` and route those compatibility paths through the named list instead of unstructured product-route prose. Change `main()` so missing generated tracker export files do not `process.exit(1)` before serving `/app`; the initial legacy render should become best-effort/debug-only while `/app` and DB APIs still boot. Update printHelp and final 404 text so `/app` is the normal app entry and legacy generated/static paths are labeled debug/export-only per D-01 through D-05.</action>
  <acceptance_criteria>
    - `src/cli/tracker-dev.mjs` defines `DEBUG_EXPORT_ROUTES` and uses it for compatibility route classification.
    - Missing `workspace/tracker.json` no longer prevents the server from listening and serving `/app`.
    - Raw generated tracker/activity feed routes are not described as normal product routes in help or 404 text.
    - A local syntax/static route check confirms `tracker-dev.mjs` defines and uses debug/export route classification before the final rollup runs the global guard.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'node --check src/cli/tracker-dev.mjs && node -e "const fs=require(\"node:fs\"); const s=fs.readFileSync(\"src/cli/tracker-dev.mjs\",\"utf8\"); for (const token of [\"DEBUG_EXPORT_ROUTES\",\"isDebugExportRoute\"]) { if (!s.includes(token)) { throw new Error(token + \" missing from tracker-dev debug/export classification\"); } }"'</automated>
  </verify>
  <done>Compatibility routes are auditable debug/export utilities and the app shell no longer depends on generated tracker export presence at boot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> React nav | Product affordance decisions are made from visible navigation. |
| Local browser -> tracker-dev | Route discovery and generated-file access happen through the local HTTP server. |
| Debug/export route -> generated files | Compatibility file reads are allowed only behind named debug/export routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Spoofing | `NavList.jsx` | medium | mitigate | Remove Classic from normal nav and prove via `NavList.test.jsx`. |
| T-06-03 | Information Disclosure | `tracker-dev.mjs` raw feed routes | medium | mitigate | Gate generated-file routes behind `DEBUG_EXPORT_ROUTES` and static allowlist tests. |
| T-06-09 | Denial of Service | `tracker-dev.mjs` startup | medium | mitigate | Missing generated tracker export no longer prevents `/app` from serving. |
| T-06-SC | Tampering | npm installs | high | mitigate | No package-manager install tasks are planned; executors must not add or upgrade packages in Phase 6. |
</threat_model>

<verification>
- `npm --workspace apps/web run test -- src/app-shell/NavList.test.jsx`
- `bash -lc 'node --check src/cli/tracker-dev.mjs && node -e "const fs=require(\"node:fs\"); const s=fs.readFileSync(\"src/cli/tracker-dev.mjs\",\"utf8\"); for (const token of [\"DEBUG_EXPORT_ROUTES\",\"isDebugExportRoute\"]) { if (!s.includes(token)) { throw new Error(token + \" missing from tracker-dev debug/export classification\"); } }"'`
</verification>

<success_criteria>
- APP-01 is green for React nav.
- APP-03/APP-04 debug/export route classification is locally verifiable in `tracker-dev.mjs`; Plan 06-08 runs the global static guard after all Wave 1 migrations.
- `/app` product boot is not blocked by generated tracker export absence.
</success_criteria>

<output>
Create `.planning/phases/06-canonical-db-app-shell/06-04-SUMMARY.md` when done.
</output>
