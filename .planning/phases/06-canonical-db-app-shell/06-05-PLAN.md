---
phase: 06-canonical-db-app-shell
plan: 05
type: execute
wave: 1
depends_on:
  - 06-02
files_modified:
  - src/cli/packet-route.mjs
autonomous: true
requirements:
  - APP-02
  - APP-03
user_setup: []
must_haves:
  truths:
    - "APP-02: Packet list/detail routes build from DB-derived application rows per D-04 and D-06."
    - "APP-03: Packet routes do not call storage-adapter tracker export reads per D-05."
  artifacts:
    - src/cli/packet-route.mjs
  key_links:
    - "requireDb/assembleTrackerObject -> packet route application list."
    - "Existing artifact resolver -> packet route DB application artifacts."
---

<objective>
Migrate packet product APIs from tracker export reads to DB-derived rows.

Purpose: Packet review is a product route; it must fail closed without DB and must not treat generated tracker JSON as source of truth.
Output: Green packet route tests with no tracker-export read dependency.
</objective>

## Phase Goal

**As a** CareerRat app user, **I want to** review packet artifacts from DB-derived application rows, **so that** packet UX stays correct even when compatibility exports are absent.

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-canonical-db-app-shell/06-CONTEXT.md
@.planning/phases/06-canonical-db-app-shell/06-RESEARCH.md
@.planning/phases/06-canonical-db-app-shell/06-VALIDATION.md
@AGENTS.md
@src/cli/packet-route.mjs
@src/cli/dashboard-route.mjs
@src/core/db/connection.mjs
@src/core/db/export-to-tracker.mjs
@src/core/paths/workspace.mjs
@tests/packet-route.test.mjs
</context>

## Artifacts This Phase Produces

- New tests: `tests/db-app-shell-regression.test.mjs`, `apps/web/src/app-shell/NavList.test.jsx`, `tests/scanner-seen-set-db.test.mjs`, `tests/db-scan-context.test.mjs`.
- Updated tests: `tests/packet-route.test.mjs`, `tests/boards-route.test.mjs`, `tests/search-route.test.mjs`.
- New DB scanner helper file: `src/core/db/scan-context.mjs`.
- New internal symbols: `DEBUG_EXPORT_ROUTES`, `isDebugExportRoute`, `readPacketApplicationsFromDb`, `readDbSearchSources`, `writeDbSearchSources`, `readDbSearchResults`, `hasConfiguredDbSourcesOnly`, `buildDbSeenSets`, `readDbScannerRows`, `buildSeenSetsForRun`.
- Changed route contracts: `GET /api/packet/list`, `GET /api/packet`, `POST /api/boards/add`, `POST /api/search/scan`, `GET /api/search/results`, `GET /api/search/sources`.
- Removed normal product affordance: the `Classic` external nav item from `/app`.
- New CLI flags: none.
- New DB fields or migrations: none.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace packet tracker adapter reads with DB-derived application rows</name>
  <files>src/cli/packet-route.mjs</files>
  <read_first>
    - src/cli/packet-route.mjs
    - src/cli/dashboard-route.mjs
    - src/core/db/connection.mjs
    - src/core/db/export-to-tracker.mjs
    - tests/packet-route.test.mjs
  </read_first>
  <behavior>
    - Missing DB returns HTTP 409 with a setup error for both packet routes.
    - DB-seeded applications produce the same packet list and detail shapes current callers use.
    - Generated tracker exports are not read by `packet-route.mjs`.
  </behavior>
  <action>After confirming the packet tests are RED, remove the `defaultAdapter` import and `readTrackerOrRespondError` path from `src/cli/packet-route.mjs`. Import `requireDb` and `assembleTrackerObject` or read application rows directly from DB; prefer `assembleTrackerObject(db)` if custom stages are needed for `classifyStage`. Add internal `readPacketApplicationsFromDb({ repoRoot, env })` that returns `{ applications, stages }` or throws `NO_DATABASE`. Convert `NO_DATABASE` to HTTP 409 `{ ok:false, error }`. Keep `isGatedIn`, artifact path safety, inline artifact handling, markdown conversion, and `needsYouFindings` behavior unchanged per D-04, D-05, and D-06.</action>
  <acceptance_criteria>
    - `src/cli/packet-route.mjs` has no import from `../core/storage/storage-adapter.mjs`.
    - `src/cli/packet-route.mjs` does not call `readTracker`.
    - `GET /api/packet/list` and `GET /api/packet?id=...` return 409 when DB is missing.
    - All packet route happy-path artifact tests pass from DB-seeded rows without `workspace/tracker.json`.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/packet-route.test.mjs</automated>
  </verify>
  <done>Packet product APIs are DB-derived, fail closed without DB, and preserve packet artifact behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> packet API | Packet list/detail requests supply app IDs and receive artifact markdown/html. |
| Packet API -> DB | Application rows and stage context are read from canonical DB state. |
| Packet API -> workspace artifacts | Artifact paths are resolved under workspace with traversal protections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering | `src/cli/packet-route.mjs` data source | high | mitigate | Replace tracker export reads with DB-derived rows and 409 missing-DB handling. |
| T-06-05 | Information Disclosure | Packet artifact path resolver | medium | mitigate | Preserve path traversal collapse-to-null behavior and run packet tests. |
| T-06-SC | Tampering | npm installs | high | mitigate | No package-manager install tasks are planned; executors must not add or upgrade packages in Phase 6. |
</threat_model>

<verification>
- `node --test tests/packet-route.test.mjs`
</verification>

<success_criteria>
- APP-02 packet route coverage passes from DB fixtures.
- Packet-route source no longer depends on generated tracker exports; Plan 06-08 runs the global static guard after all Wave 1 migrations.
</success_criteria>

<output>
Create `.planning/phases/06-canonical-db-app-shell/06-05-SUMMARY.md` when done.
</output>
