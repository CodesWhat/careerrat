---
phase: 06-canonical-db-app-shell
plan: 07
type: execute
wave: 1
depends_on:
  - 06-03
files_modified:
  - tests/db-scan-context.test.mjs
  - src/core/db/scan-context.mjs
  - scripts/scan-sourced.mjs
  - src/cli/search-route.mjs
autonomous: true
requirements:
  - APP-02
  - APP-03
  - APP-04
user_setup: []
must_haves:
  truths:
    - "APP-02: Search scanner product routes and scanner seen sets read DB-derived source, application, sourced, and activity context per D-06 and D-07."
    - "APP-03: Search product routes do not read generated scan-result or tracker export files as source of truth per D-04 and D-05."
    - "APP-04: Static guards fail if scanner product code reintroduces generated-file dependencies per D-09."
  artifacts:
    - src/core/db/scan-context.mjs
    - scripts/scan-sourced.mjs
    - src/cli/search-route.mjs
  key_links:
    - "src/core/db/scan-context.mjs -> scripts/scan-sourced.mjs"
    - "src/cli/search-route.mjs -> tests/search-route.test.mjs"
    - "scripts/scan-sourced.mjs -> tests/scanner-seen-set-db.test.mjs"
---

<objective>
Migrate scanner context, results, and seen-set dedupe to DB-derived product state.

Purpose: The app's scanner routes must be ready for DB-backed auto-sourcing without reading generated tracker exports or scan-result files as product state.
Output: DB scanner helper plus green search-route and scanner seen-set tests.
</objective>

## Phase Goal

**As a** CareerRat app user, **I want to** run and inspect sourcing through DB-backed scanner context, **so that** duplicates and results reflect canonical application state.

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-canonical-db-app-shell/06-CONTEXT.md
@.planning/phases/06-canonical-db-app-shell/06-RESEARCH.md
@.planning/phases/06-canonical-db-app-shell/06-VALIDATION.md
@AGENTS.md
@src/cli/search-route.mjs
@scripts/scan-sourced.mjs
@src/core/tracker/tracker-data.mjs
@src/core/db/connection.mjs
@src/core/db/verbs/source-config.mjs
@src/core/db/verbs/sourced.mjs
@src/core/scoring/sourced-scanner.mjs
@tests/search-route.test.mjs
@tests/scanner-seen-set-db.test.mjs
@tests/db-app-shell-regression.test.mjs
</context>

## Artifacts This Phase Produces

- New tests: `tests/db-app-shell-regression.test.mjs`, `apps/web/src/app-shell/NavList.test.jsx`, `tests/scanner-seen-set-db.test.mjs`, `tests/db-scan-context.test.mjs`.
- Updated tests: `tests/packet-route.test.mjs`, `tests/boards-route.test.mjs`, `tests/search-route.test.mjs`.
- New DB scanner helper file: `src/core/db/scan-context.mjs`.
- New internal symbols: `DEBUG_EXPORT_ROUTES`, `isDebugExportRoute`, `readPacketApplicationsFromDb`, `readDbSearchSources`, `writeDbSearchSources`, `readDbSearchResults`, `hasConfiguredDbSourcesOnly`, `buildDbSeenSets`, `readDbScannerRows`, `buildSeenSetsForRun`.
- Changed route contracts: `GET /api/packet/list`, `GET /api/packet`, `POST /api/boards/add`, `POST /api/search/scan`, `GET /api/search/results`, `GET /api/search/sources`.
- Removed normal product affordance: the `Classic` external nav item from `/app`.
- New CLI flags: none.
- New DB fields or migrations: none.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DB scanner context helper</name>
  <files>src/core/db/scan-context.mjs, tests/db-scan-context.test.mjs</files>
  <read_first>
    - src/core/tracker/tracker-data.mjs
    - src/core/db/connection.mjs
    - src/core/db/verbs/sourced.mjs
    - src/core/scoring/sourced-scanner.mjs
    - tests/scanner-seen-set-db.test.mjs
  </read_first>
  <behavior>
    - `buildDbSeenSets({ repoRoot, env })` returns `seenUrls`, `seenReqIds`, `seenCompanyRoles`, and `tracker` from DB rows.
    - `readDbScannerRows({ repoRoot, env })` returns sourced rows in stable row order for product result routes.
    - Missing DB propagates the existing `NO_DATABASE` error.
  </behavior>
  <action>Create `src/core/db/scan-context.mjs` and a focused helper test `tests/db-scan-context.test.mjs`. Implement `readDbRows(db, table)` for `applications` and `sourced` JSON rows, `valueSet(rows, selectors)` helpers matching the semantics of `buildSeenSets`, `buildDbSeenSets({ repoRoot, env })`, and `readDbScannerRows({ repoRoot, env })`. Reuse or mirror the same normalized URL/request/company-role fields expected by `filterAndDedupeOffers`. Do not import tracker storage adapters or generated-file loaders. The helper test should seed DB application and sourced rows, assert `buildDbSeenSets` returns URL/request/company-role sets and tracker-shaped app context, assert `readDbScannerRows` returns stable DB sourced rows, and assert missing DB propagates `NO_DATABASE` per D-04, D-05, D-06, and D-07.</action>
  <acceptance_criteria>
    - New helper file exports `buildDbSeenSets` and `readDbScannerRows`.
    - `tests/db-scan-context.test.mjs` covers helper behavior without importing `scripts/scan-sourced.mjs` or `src/cli/search-route.mjs`.
    - Helper reads only DB tables and does not import tracker-data or storage adapter modules.
    - Helper returns a `tracker` object with an `apps` array so existing `computeFamilyOutcomes` call sites can keep their current input shape.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/db-scan-context.test.mjs</automated>
  </verify>
  <done>DB scanner context helper exists and supplies DB-derived duplicate context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use DB seen sets in runSourcedScan when DB exists</name>
  <files>scripts/scan-sourced.mjs</files>
  <read_first>
    - scripts/scan-sourced.mjs
    - src/core/db/scan-context.mjs
    - src/core/tracker/tracker-data.mjs
    - tests/scanner-seen-set-db.test.mjs
  </read_first>
  <behavior>
    - DB-mode scanner dedupe uses DB application/sourced rows.
    - Legacy CLI mode without DB keeps the existing tracker export seen-set behavior for compatibility CLI use.
    - Search-source and sourced-scan config remain DB-first when DB exists.
  </behavior>
  <action>Import `buildDbSeenSets` from `src/core/db/scan-context.mjs`. Add internal `buildSeenSetsForRun(pathCtx)` that returns `buildDbSeenSets(pathCtx)` when `dbExists(pathCtx)` is true and otherwise calls the existing `buildSeenSets(repoRoot)` for compatibility CLI mode. Replace the direct seen-set call in `runSourcedScan()` with `buildSeenSetsForRun(pathCtx)`. Keep existing DB-first source config loading, JD capture, sourced row persistence, watermarks, and debug scan-result writing behavior unchanged per D-04 through D-07.</action>
  <acceptance_criteria>
    - `scripts/scan-sourced.mjs` no longer calls the legacy seen-set helper directly inside `runSourcedScan`.
    - DB-mode scanner seen-set tests pass and prove tracker-export-only duplicates are ignored.
    - Existing scanner seen-set tests pass with stub fetches and do not require search-route changes from Task 3.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/scanner-seen-set-db.test.mjs</automated>
  </verify>
  <done>Scanner dedupe uses DB rows when DB exists and preserves compatibility CLI behavior when DB is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make search product routes DB-only for sources and results</name>
  <files>src/cli/search-route.mjs</files>
  <read_first>
    - src/cli/search-route.mjs
    - src/core/db/scan-context.mjs
    - src/core/db/connection.mjs
    - src/core/db/verbs/source-config.mjs
    - tests/search-route.test.mjs
  </read_first>
  <behavior>
    - Missing DB returns 409 for scan, sources, and results product routes.
    - DB source config controls whether scans can run.
    - Search results are read from DB sourced rows and ignore scan-result files.
  </behavior>
  <action>Import `requireDb` or centralize DB presence through a `withDb` helper and import `readDbScannerRows` from `src/core/db/scan-context.mjs`. Replace `hasRunnableSearchConfig` with `hasConfiguredDbSourcesOnly(pathCtx)` that requires DB and checks only `sourceConfigGet` results. Change `POST /api/search/scan` to return 409 on missing DB, 400 on DB with no configured sources, and otherwise run `runSourcedScan`. Change `GET /api/search/results` to return DB sourced rows from `readDbScannerRows` with a stable JSON shape such as `{ ok:true, source:"db", count, offers }`; it must not read scan-result JSON files. Change `GET /api/search/sources` to return 409 when DB is missing and count only DB source config rows. Keep malformed JSON and in-flight scan behavior unchanged per D-04, D-05, D-06, D-07, and D-08.</action>
  <acceptance_criteria>
    - `src/cli/search-route.mjs` no longer imports file-system helpers for scan-result reads or legacy source config reads.
    - Missing DB tests for scan/results/sources expect 409.
    - DB sources counts and scan run tests pass with DB fixtures and stub fetch.
    - DB results tests prove generated scan-result JSON files are ignored by product result route.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/search-route.test.mjs</automated>
  </verify>
  <done>Search product routes read DB source and result context only, and scanner dedupe uses canonical DB rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> `/api/search/*` | Product scan requests and result reads cross from browser into local scanner orchestration. |
| Scanner -> external ATS/RSS sources | Stubbed in tests; real runs fetch public job postings. |
| Scanner -> DB rows | Duplicate detection and product results must reflect canonical DB state. |
| Scanner -> debug artifacts | Scan-result JSON files can be written as debug/report artifacts but not read as product state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | `src/cli/search-route.mjs` result source | high | mitigate | Replace scan-result file reads with DB sourced row reads and 409 missing-DB handling. |
| T-06-08 | Repudiation | `scripts/scan-sourced.mjs` seen sets | medium | mitigate | `buildDbSeenSets` drives DB-mode duplicate context and tests prove tracker-export-only rows are ignored. |
| T-06-11 | Denial of Service | `/api/search/scan` concurrent scans | low | mitigate | Preserve the existing in-flight 409 guard. |
| T-06-SC | Tampering | npm installs | high | mitigate | No package-manager install tasks are planned; executors must not add or upgrade packages in Phase 6. |
</threat_model>

<verification>
- `node --test tests/db-scan-context.test.mjs`
- `node --test tests/scanner-seen-set-db.test.mjs`
- `node --test tests/search-route.test.mjs`
</verification>

<success_criteria>
- APP-02 scanner/source context is DB-derived.
- APP-03 generated tracker and scan-result files are not product state.
- Plan 06-08 runs the global static guard after all Wave 1 migrations to cover search/scanner product seams.
</success_criteria>

<output>
Create `.planning/phases/06-canonical-db-app-shell/06-07-SUMMARY.md` when done.
</output>
