---
phase: 06-canonical-db-app-shell
plan: 10
type: execute
wave: 3
depends_on:
  - 06-08
files_modified:
  - tests/onboard-route.test.mjs
  - apps/web/src/onboarding/steps/FinishStep.test.jsx
  - src/cli/onboard-route.mjs
  - apps/web/src/onboarding/steps/FinishStep.jsx
autonomous: true
gap_closure: true
requirements:
  - APP-02
  - APP-03
user_setup: []
must_haves:
  truths:
    - "APP-02: DB-mode onboarding source readiness is derived from SQLite `sourceConfigGet({ name: \"search-sources\" })`, not `config/search-sources.yml`, per D-04, D-06, and D-07."
    - "APP-02: FinishStep treats compatibility file export as explicit export/debug support, not product source setup readiness, per D-02 and D-06."
    - "APP-03: `config/search-sources.yml` remains a compatibility/export artifact and is not a normal `/app` readiness dependency per D-04 and D-07."
  artifacts:
    - tests/onboard-route.test.mjs
    - apps/web/src/onboarding/steps/FinishStep.test.jsx
    - src/cli/onboard-route.mjs
    - apps/web/src/onboarding/steps/FinishStep.jsx
  key_links:
    - "`/api/onboard/state` -> `sourceConfigGet({ name: \"search-sources\" })` proves DB-mode source readiness without compatibility YAML."
    - "FinishStep readiness/copy -> FinishStep.test.jsx proves compatibility export is not normal product source setup state."
    - "Route tests -> no compatibility YAML fallback proves APP-02/APP-03 source setup boundary."
---

<objective>
Close the APP-02 verification gap where DB-mode onboarding source readiness still depends on `config/search-sources.yml` and FinishStep frames compatibility export as the normal app source setup flow.

Purpose: Phase 6 cannot be verified while `/app/onboarding` treats a compatibility source file as product readiness in DB mode.
Output: RED/GREEN/REFACTOR gap closure for DB source-config readiness in `/api/onboard/state`, FinishStep readiness naming/copy, and backend/frontend tests proving DB-mode readiness without `config/search-sources.yml`.
</objective>

## Phase Goal

**As a** CareerRat app user, **I want to** see onboarding source readiness from canonical SQLite state, **so that** compatibility files remain explicit exports instead of product setup state.

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-canonical-db-app-shell/06-CONTEXT.md
@.planning/phases/06-canonical-db-app-shell/06-RESEARCH.md
@.planning/phases/06-canonical-db-app-shell/06-VALIDATION.md
@.planning/phases/06-canonical-db-app-shell/06-VERIFICATION.md
@.planning/phases/06-canonical-db-app-shell/06-06-SUMMARY.md
@.planning/phases/06-canonical-db-app-shell/06-08-SUMMARY.md
@AGENTS.md
@src/cli/onboard-route.mjs
@src/core/db/verbs/source-config.mjs
@tests/onboard-route.test.mjs
@apps/web/src/onboarding/steps/FinishStep.jsx
@apps/web/src/onboarding/steps/FinishStep.test.jsx
@apps/web/src/lib/api.js
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Make Electron/React `/app` DB-source-of-truth and remove compatibility surfaces from product paths | 06-09, 06-10 | COVERED | Gap plans close the onboarding surfaces missed by the original 06-08 rollup.
REQ | APP-01 | `/app` is canonical product surface; compatibility surfaces are not normal UX | 06-09 | COVERED | Sibling gap plan removes `/onboard` affordance and static utility framing.
REQ | APP-02 | Dashboard, packet, tracker/activity, scanner context, and source setup views read DB-derived snapshots | 06-10 | COVERED | This plan moves onboarding source readiness to SQLite source config.
REQ | APP-03 | `workspace/tracker.json` and compatibility files remain export/debug artifacts only | 06-10 | COVERED | This plan keeps `config/search-sources.yml` out of DB-mode app readiness.
CONTEXT | D-01 | Remove legacy product affordances from app path | 06-09 | COVERED | Sibling gap plan covers visible static onboarding affordance.
CONTEXT | D-02 | Legacy/generated surfaces only debug/export utilities | 06-09, 06-10 | COVERED | This plan rewords FinishStep compatibility export copy; sibling plan handles static route copy.
CONTEXT | D-04 | Product routes DB-derived and fail closed without DB | 06-10 | COVERED | `/api/onboard/state` reads source readiness from DB source config in DB mode.
CONTEXT | D-06 | App-visible source setup converges on DB-derived snapshots or DB verbs | 06-10 | COVERED | FinishStep uses DB readiness semantics for source setup and compatibility export separately.
CONTEXT | D-07 | Move source setup seams DB-first now | 06-10 | COVERED | DB-mode onboarding no longer reads `config/search-sources.yml` for readiness.
CONTEXT | D-09 | Aggressive static regression guards | 06-09 | COVERED | Sibling gap plan expands React static affordance guard.
CONTEXT | D-10 | Narrow debug/export allowlists | 06-09 | COVERED | Sibling gap plan audits static compatibility route classification.
VERIFICATION | APP-02-gap | DB-mode onboarding readiness depends on `config/search-sources.yml`; FinishStep copy presents compatibility export as normal app flow | 06-10 | COVERED | Directly addresses both artifacts named in `06-VERIFICATION.md`.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Add DB source-readiness route and FinishStep copy tests</name>
  <files>tests/onboard-route.test.mjs, apps/web/src/onboarding/steps/FinishStep.test.jsx</files>
  <read_first>
    - tests/onboard-route.test.mjs
    - src/cli/onboard-route.mjs
    - src/core/db/verbs/source-config.mjs
    - apps/web/src/onboarding/steps/FinishStep.jsx
    - apps/web/src/onboarding/steps/FinishStep.test.jsx
  </read_first>
  <behavior>
    - In DB mode, `/api/onboard/state` reports source setup readiness from a stored SQLite `search-sources` row even when `config/search-sources.yml` is absent.
    - In DB mode, `/api/onboard/state` ignores a compatibility `config/search-sources.yml` file when no stored SQLite `search-sources` row/data is present.
    - FinishStep copy presents compatibility-file export as explicit export support, not product source setup readiness.
  </behavior>
  <action>Extend `tests/onboard-route.test.mjs` with DB-mode route tests around `GET /api/onboard/state`. Import `sourceConfigGet` and `sourceConfigPut` from the existing DB source-config verb module. Add one test that initializes DB setup, writes a stored `search-sources` row with at least one enabled search or tracked company, verifies `config/search-sources.yml` is absent, and expects onboarding state to report source readiness present. Add a second test that writes only the compatibility YAML file in a DB workspace with no stored DB source-config row/data and expects onboarding state to report source readiness absent. Extend `apps/web/src/onboarding/steps/FinishStep.test.jsx` with render assertions that fail while FinishStep uses `configReady`/`searchSourcesPresent` as compatibility-file readiness and while copy says normal setup generates `config/search-sources.yml`; expected copy should make compatibility export explicit and separate from SQLite product source setup. Keep existing discovery handoff tests unchanged.</action>
  <acceptance_criteria>
    - Route tests prove DB-mode readiness works without `config/search-sources.yml`.
    - Route tests prove DB-mode readiness does not become true from `config/search-sources.yml` alone.
    - Component tests fail while FinishStep presents compatibility-file export and `config/search-sources.yml` as normal app flow.
    - Existing onboarding route and FinishStep tests remain in non-watch commands.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'node --test tests/onboard-route.test.mjs; route_status=$?; npm --workspace apps/web run test -- src/onboarding/steps/FinishStep.test.jsx; web_status=$?; test $route_status -ne 0 && test $web_status -ne 0'</automated>
  </verify>
  <done>APP-02 has RED coverage for the missing items: DB-mode readiness from SQLite source config without compatibility YAML, and FinishStep compatibility export copy separated from product source setup state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN/REFACTOR: Derive onboarding source readiness from SQLite and reword FinishStep</name>
  <files>src/cli/onboard-route.mjs, apps/web/src/onboarding/steps/FinishStep.jsx</files>
  <read_first>
    - src/cli/onboard-route.mjs
    - src/core/db/verbs/source-config.mjs
    - tests/onboard-route.test.mjs
    - apps/web/src/onboarding/steps/FinishStep.jsx
    - apps/web/src/onboarding/steps/FinishStep.test.jsx
  </read_first>
  <behavior>
    - DB-mode `GET /api/onboard/state` derives source setup readiness from `sourceConfigGet({ name: "search-sources" })` and its `stored` row/data.
    - Compatibility file export remains available through `POST /api/onboard/write-config`, but it is labeled as export/support output rather than product setup readiness.
    - FinishStep uses source-readiness naming for DB setup state and compatibility-export naming for file generation.
  </behavior>
  <action>After confirming the new tests are RED, import `sourceConfigGet` in `src/cli/onboard-route.mjs` and add a small helper such as `dbSearchSourcesPresent(pathCtx)` that returns true only when the DB `search-sources` config is stored and contains an actual configured source path: at least one enabled `searches[]` entry or at least one valid tracked-company source. A stored default row, empty arrays, or source-catalog metadata alone must not mark readiness true. In the DB branch of `GET /api/onboard/state`, set the existing state field or an added source-readiness field from that helper instead of `existsSync(userPath(pathCtx, "config/search-sources.yml"))`; keep the legacy non-DB branch file-based because it is outside DB mode. Reword comments and response framing around `POST /api/onboard/write-config` so DB mode is described as exporting compatibility files while DB source config remains the product source setup state. In `FinishStep.jsx`, rename local `configReady` style logic to source-setup readiness, use DB readiness to trigger source preview/add-source behavior, and change the finish/export card copy and button label so compatibility file export is explicit. Remove user-facing text that says a LinkedIn source was added to `config/search-sources.yml`; describe it as added to DB source setup and disabled by default. Preserve existing quick-start, discovery handoff, previewBoards, addBoard, and writeConfig behavior per D-02, D-04, D-06, and D-07.</action>
  <acceptance_criteria>
    - In DB mode, `GET /api/onboard/state` derives source setup readiness from SQLite `sourceConfigGet({ name: "search-sources" })` rather than `config/search-sources.yml`.
    - `/api/onboard/state` reports source readiness true with a stored DB `search-sources` row and no compatibility YAML file.
    - `/api/onboard/state` reports source readiness false when only `config/search-sources.yml` exists in a DB workspace.
    - A stored default `search-sources` row or source-catalog-only row does not mark DB-mode source readiness true.
    - `/app` onboarding finish copy makes compatibility file export explicit and does not present `config/search-sources.yml` as normal product source setup state.
    - `node --test tests/onboard-route.test.mjs` and `npm --workspace apps/web run test -- src/onboarding/steps/FinishStep.test.jsx` pass.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/onboard-route.test.mjs && npm --workspace apps/web run test -- src/onboarding/steps/FinishStep.test.jsx</automated>
  </verify>
  <done>APP-02 is closed for onboarding source readiness: DB mode uses SQLite source config, FinishStep separates DB source setup from compatibility export, and compatibility files are not normal app readiness state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> `/api/onboard/state` | The React onboarding UI trusts readiness fields returned by the local onboarding route. |
| Onboarding route -> SQLite source config | Source readiness must come from canonical DB source setup state in DB workspaces. |
| FinishStep -> compatibility export route | A user action can export compatibility files, but that export must not masquerade as product source setup state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-18 | Tampering | `src/cli/onboard-route.mjs` readiness source | high | mitigate | Derive DB-mode source readiness from `sourceConfigGet({ name: "search-sources" })` and test that compatibility YAML alone is ignored. |
| T-06-19 | Spoofing | `FinishStep.jsx` readiness/copy | medium | mitigate | Reword FinishStep so compatibility export is explicit and DB source setup is the product state. |
| T-06-20 | Repudiation | onboarding route/component tests | medium | mitigate | Add backend and frontend tests that prove DB readiness without `config/search-sources.yml` and no YAML-only readiness in DB mode. |
| T-06-SC | Tampering | npm installs | high | mitigate | No package-manager install tasks are planned; executors must not add or upgrade packages in this gap closure. |
</threat_model>

<verification>
<automated>node --test tests/onboard-route.test.mjs && npm --workspace apps/web run test -- src/onboarding/steps/FinishStep.test.jsx</automated>
<automated>node --test tests/onboard-route.test.mjs tests/dashboard-route.test.mjs tests/data-route.test.mjs tests/packet-route.test.mjs tests/search-route.test.mjs tests/boards-route.test.mjs tests/desktop-routing.test.mjs tests/db-app-shell-regression.test.mjs && npm --workspace apps/web run test -- src/app-shell/NavList.test.jsx src/onboarding/steps/FinishStep.test.jsx</automated>
</verification>

<success_criteria>
- In DB mode, onboarding source readiness is derived from SQLite `sourceConfigGet({ name: "search-sources" })` and its stored row/data.
- Stored default source-config rows and source-catalog-only rows do not mark DB-mode source setup ready.
- DB-mode readiness works without `config/search-sources.yml`.
- A compatibility `config/search-sources.yml` file alone does not mark DB-mode onboarding source setup ready.
- FinishStep copy makes compatibility file export explicit and does not present compatibility export as product source setup state.
- No packages are added or upgraded.
</success_criteria>

<output>
Create `.planning/phases/06-canonical-db-app-shell/06-10-SUMMARY.md` when done.
</output>
