---
phase: 06-canonical-db-app-shell
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/packet-route.test.mjs
autonomous: true
requirements:
  - APP-02
  - APP-03
user_setup: []
must_haves:
  truths:
    - "APP-02: Packet product APIs read DB-derived application rows and artifacts per D-04 and D-06."
    - "APP-03: Packet product APIs do not require or read generated tracker exports per D-05."
  artifacts:
    - tests/packet-route.test.mjs
  key_links:
    - "tests/packet-route.test.mjs -> src/cli/packet-route.mjs locks the DB-only packet contract."
---

<objective>
Create the RED packet-route contract for DB-derived packet list and packet detail reads.

Purpose: The packet API is currently tracker-export-backed; Phase 6 needs tests that require DB-derived rows and fail closed when the DB is absent.
Output: Updated packet route tests that fail before `packet-route.mjs` migrates off tracker exports.
</objective>

## Phase Goal

**As a** CareerRat app user, **I want to** open packet data from the canonical DB-backed product API, **so that** generated tracker exports remain compatibility artifacts only.

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-canonical-db-app-shell/06-CONTEXT.md
@.planning/phases/06-canonical-db-app-shell/06-RESEARCH.md
@.planning/phases/06-canonical-db-app-shell/06-VALIDATION.md
@AGENTS.md
@src/cli/packet-route.mjs
@src/cli/dashboard-route.mjs
@src/core/db/import-from-tracker.mjs
@src/core/db/export-to-tracker.mjs
@tests/packet-route.test.mjs
@tests/dashboard-route.test.mjs
</context>

## Artifacts This Phase Produces

- New tests: `tests/db-app-shell-regression.test.mjs`, `apps/web/src/app-shell/NavList.test.jsx`, `tests/scanner-seen-set-db.test.mjs`, `tests/db-scan-context.test.mjs`.
- Updated tests: `tests/packet-route.test.mjs`, `tests/boards-route.test.mjs`, `tests/search-route.test.mjs`.
- New DB scanner helper file: `src/core/db/scan-context.mjs`.
- New internal symbols: `DEBUG_EXPORT_ROUTES`, `isDebugExportRoute`, `readPacketApplicationsFromDb`, `readDbSearchSources`, `writeDbSearchSources`, `readDbSearchResults`, `hasConfiguredDbSourcesOnly`, `buildDbSeenSets`, `readDbScannerRows`, `buildSeenSetsForRun`.
- Changed route contracts: `GET /api/packet/list`, `GET /api/packet`, `POST /api/boards/add`, `POST /api/search/scan`, `GET /api/search/results`, `GET /api/search/sources`.
- Removed normal product affordance: the `Classic` external nav item from `/app`.
- New CLI flags: none.
- New DB fields or migrations: none.

<tasks>

<task type="auto">
  <name>Task 1: Rewrite packet tests for DB-derived application rows</name>
  <files>tests/packet-route.test.mjs</files>
  <read_first>
    - tests/packet-route.test.mjs
    - tests/dashboard-route.test.mjs
    - src/cli/packet-route.mjs
    - src/core/db/import-from-tracker.mjs
    - src/core/paths/workspace.mjs
  </read_first>
  <action>Update packet route fixtures to seed a temporary DB through `importFromTracker({ repoRoot, sourceDir })` using a fixture-source tracker file, while asserting no `workspace/tracker.json` exists in the temp workspace. Preserve artifact files under `workspace/tailored`. Change the missing-state tests for `GET /api/packet/list` and `GET /api/packet?id=...` from tracker-file 404 expectations to DB fail-closed 409 expectations per D-04 and D-05. Keep the existing artifact path-safety, inline text, terminal-status, and `needsYouCount` assertions.</action>
  <acceptance_criteria>
    - Tests no longer call helper code that writes `workspace/tracker.json` into the temp repo as the packet route's data source.
    - DB-seeded packet list still includes gated-in applications, terminal applications with artifacts, inline artifacts, and path traversal null behavior.
    - Missing DB assertions expect HTTP 409 and an error mentioning database setup.
    - The RED tests fail against current `src/cli/packet-route.mjs` because it reads the storage adapter tracker export.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'node --test tests/packet-route.test.mjs; test $? -ne 0'</automated>
  </verify>
  <done>Packet route tests enforce DB-derived packet reads and fail against the current tracker-export implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> `/api/packet/*` | Untrusted app requests ask for packet data and artifact content. |
| Packet API -> local artifact files | Stamped artifact values can be inline text or workspace-relative paths. |
| Packet API -> DB | Product packet rows must be DB-derived and fail closed without DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering | `src/cli/packet-route.mjs` application source | high | mitigate | Updated tests require DB-seeded packet rows and 409 on missing DB, preventing tracker-export substitution. |
| T-06-05 | Information Disclosure | Packet artifact path resolver | medium | mitigate | Preserve traversal-to-null tests and inline/path distinction while changing only the application-row source. |
| T-06-SC | Tampering | npm installs | high | mitigate | No package-manager install tasks are planned; executors must not add or upgrade packages in Phase 6. |
</threat_model>

<verification>
Wave 0 RED verification:
- `bash -lc 'node --test tests/packet-route.test.mjs; test $? -ne 0'`
</verification>

<success_criteria>
- APP-02 and APP-03 have packet-route failing coverage before implementation.
- The route contract is clear: DB rows are product state; generated tracker exports are not.
</success_criteria>

<output>
Create `.planning/phases/06-canonical-db-app-shell/06-02-SUMMARY.md` when done.
</output>
