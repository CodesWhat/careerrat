---
phase: 10-local-packet-engine
plan: 03
type: tdd
wave: 2
depends_on: ["10-02"]
files_modified:
  - tests/packet-answers.test.mjs
  - tests/form-questions.test.mjs
  - src/core/packet/schemas/packet-schemas.mjs
  - src/core/packet/questions.mjs
  - src/core/packet/answers.mjs
  - src/core/db/verbs/app.mjs
  - src/cli/packet-route.mjs
autonomous: true
requirements: [PKT-02, PKT-03]
must_haves:
  truths:
    - "Application questions can be captured through supported provider extraction or manual paste and saved as durable packet context."
    - "EEO, disability, veteran, demographic, and similar self-identification prompts are excluded before generated-answer automation."
    - "Generated non-EEO answers are evidence-grounded or explicitly marked as needing user input."
  artifacts:
    - "src/core/packet/questions.mjs"
    - "src/core/packet/answers.mjs"
    - "appRegisterPacketQuestionCapture"
    - "POST /api/packet/questions"
    - "POST /api/packet/answers"
  key_links:
    - "POST /api/packet/questions -> artifacts.packetQuestionsSource -> packetManifest.questions"
    - "artifacts.packetQuestionsSource -> draftPacketAnswers() -> generatePacket()"
    - "D-08..D-14 -> question capture and answer boundaries"
    - "PKT-02/PKT-03 -> grounded answers and EEO exclusion"
---

## Phase Goal

**As a** CareerRat user, **I want to** generate ATS-ready apply packets through local APIs, **so that** I can prepare grounded application materials without launching whole skill workflows for ordinary packet work.

<objective>
Implement the question capture and answer drafting slice.

Purpose: Packet generation needs normalized non-EEO questions and grounded answers before it can produce a complete application packet.
Output: Question capture service, self-identification exclusion, answer proposal service, and local packet question/answer routes.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-local-packet-engine/10-CONTEXT.md
@.planning/phases/10-local-packet-engine/10-RESEARCH.md
@.planning/phases/10-local-packet-engine/10-PATTERNS.md
@.planning/phases/10-local-packet-engine/10-VALIDATION.md
@AGENTS.md
@.agents/skills/answer-question/SKILL.md
@.agents/skills/tailor-application/SKILL.md
@src/core/apply/form-questions.mjs
@config/form-questions.schema.json
@src/core/documents/tailor.mjs
@src/core/ai/bounded-ai.mjs
@src/core/packet/context.mjs
@src/core/packet/schemas/packet-schemas.mjs
@src/cli/packet-route.mjs
@tests/form-questions.test.mjs
@tests/packet-answers.test.mjs
@tests/documents-tailor.test.mjs
@tests/bounded-ai.test.mjs
</context>

## Artifacts This Phase Produces

- Route paths: `POST /api/packet/gate`, `POST /api/packet/questions`, `POST /api/packet/answers`, `POST /api/packet/generate`, `POST /api/packet/export`.
- Core modules: `src/core/packet/context.mjs`, `src/core/packet/gate.mjs`, `src/core/packet/questions.mjs`, `src/core/packet/answers.mjs`, `src/core/packet/generate.mjs`, `src/core/packet/exports.mjs`, `src/core/packet/schemas/packet-schemas.mjs`.
- Functions/helpers: `buildPacketContext`, `evaluatePacketGate`, `capturePacketQuestions`, `classifySelfIdentificationQuestion`, `filterAnswerableQuestions`, `loadPacketQuestionCapture`, `draftPacketAnswers`, `draftCoverLetterBlocks`, `validatePacketEvidenceIds`, `generatePacket`, `exportPacketArtifacts`, `appRegisterPacketQuestionCapture`, `appRegisterPacketArtifacts`.
- Schemas: `packetGateRequestSchema`, `packetGateAiVerdictSchema`, `packetQuestionCaptureRequestSchema`, `packetQuestionCaptureArtifactSchema`, `packetAnswerProposalSchema`, `packetCoverLetterProposalSchema`, `packetGenerateRequestSchema`, `packetExportRequestSchema`, `packetManifestSchema`.
- Application artifact fields: `artifacts.packetManifest`, `artifacts.packetQuestionsSource`, `artifacts.packetQuestionsCapturedAt`, `artifacts.packetQuestionCount`, `artifacts.packetQuestionExcludedCount`, `artifacts.resumeSource`, `artifacts.coverLetterSource`, `artifacts.answersSource`, `artifacts.resumePdf`, `artifacts.coverLetterPdf`, `artifacts.answersPdf`, `artifacts.resumeDocx`, `artifacts.coverLetterDocx`.
- Tests: `tests/packet-generate-route.test.mjs`, `tests/packet-runtime-boundary.test.mjs`, `tests/packet-engine.test.mjs`, `tests/packet-answers.test.mjs`, `tests/packet-export.test.mjs`, updated `tests/packet-page.test.mjs`, updated `tests/answer-page.test.mjs`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED question and answer contracts</name>
  <files>tests/packet-answers.test.mjs, tests/form-questions.test.mjs</files>
  <read_first>
    tests/packet-answers.test.mjs
    tests/form-questions.test.mjs
    src/core/apply/form-questions.mjs
    .planning/phases/10-local-packet-engine/10-VALIDATION.md
  </read_first>
  <behavior>
    - Provider-captured Greenhouse and Ashby questions preserve `demographicSectionPresent` but pass only non-self-identification questions to answer drafting.
    - Manual-pasted disability, veteran, race, ethnicity, gender, pronoun-as-demographic, and voluntary self-ID prompts are excluded conservatively.
    - `POST /api/packet/questions` persists normalized captured questions as an internal artifact and records `packetManifest.questions` metadata for later generation.
    - Non-EEO "why this company", recent tools, role fit, and relevant experience prompts produce grounded answer proposals or `NEEDS YOU` gaps.
  </behavior>
  <action>Run and tighten the Wave 0 PKT-03 tests. Add assertions for manual-paste self-identification prompts because existing provider normalizers already cover provider demographic sections. Add assertions that question capture writes a durable packet question artifact path and manifest question metadata before answer drafting. The RED failure must name missing packet question/answer modules or missing packet question persistence rather than provider normalizer regressions.</action>
  <verify>
    <automated>bash -lc 'node --test tests/packet-answers.test.mjs tests/form-questions.test.mjs; test $? -ne 0'</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail for missing Phase 10 packet question/answer owners or missing durable question capture metadata.
    - Provider fixtures still prove existing Greenhouse/Ashby demographic sections are not answerable questions.
    - Manual paste fixtures cover disability, veteran, demographic, and compliance wording.
  </acceptance_criteria>
  <done>RED question and answer contracts fail for the intended missing implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN packet question capture and exclusion</name>
  <files>src/core/packet/schemas/packet-schemas.mjs, src/core/packet/questions.mjs</files>
  <read_first>
    src/core/apply/form-questions.mjs
    config/form-questions.schema.json
    src/core/packet/schemas/packet-schemas.mjs
    tests/form-questions.test.mjs
    tests/packet-answers.test.mjs
  </read_first>
  <behavior>
    - `capturePacketQuestions({ source:"url", url, fetchImpl })` delegates to `fetchFormQuestions()` for supported providers.
    - `capturePacketQuestions({ source:"paste", text })` delegates to `parseManualQuestions()` and then applies self-identification classification.
    - `filterAnswerableQuestions()` returns `{ answerable, excluded, demographicSectionPresent }` with excluded reasons and no generated answer text.
    - `packetQuestionCaptureArtifactSchema` validates persisted capture JSON with source, capturedAt, answerable IDs, excluded IDs, and excluded reason codes.
  </behavior>
  <action>Create `src/core/packet/questions.mjs` with `capturePacketQuestions`, `classifySelfIdentificationQuestion`, and `filterAnswerableQuestions`. Keep provider extraction deterministic per D-14 and classify manual-paste self-identification prompts conservatively per D-12 and D-13. Extend packet schemas with `packetQuestionCaptureRequestSchema`, `packetQuestionCaptureArtifactSchema`, and shared question/exclusion response shapes. Do not modify provider extraction behavior except to add tests that lock existing demographic exclusion.</action>
  <verify>
    <automated>node --test tests/packet-answers.test.mjs tests/form-questions.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Supported provider extraction remains Greenhouse/Ashby plus manual fallback.
    - Excluded questions are visible as metadata with reason codes and never passed to answer drafting.
    - Uncertain self-identification-like prompts are excluded rather than drafted.
  </acceptance_criteria>
  <done>Packet question capture and EEO/demographic exclusion pass focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN answer drafting service and routes</name>
  <files>src/core/packet/schemas/packet-schemas.mjs, src/core/packet/answers.mjs, src/core/db/verbs/app.mjs, src/cli/packet-route.mjs</files>
  <read_first>
    src/core/packet/questions.mjs
    src/core/packet/context.mjs
    src/core/documents/tailor.mjs
    src/core/ai/bounded-ai.mjs
    src/core/db/verbs/app.mjs
    src/core/db/verbs/shared.mjs
    src/cli/packet-route.mjs
    src/cli/data-route.mjs
    tests/packet-answers.test.mjs
    tests/packet-generate-route.test.mjs
  </read_first>
  <behavior>
    - `draftPacketAnswers()` calls bounded AI for finite answer proposals with evidence IDs or explicit gaps, then validates every answer through existing document helpers.
    - `draftPacketAnswers({ applicationId })` loads the durable question capture from `artifacts.packetQuestionsSource` or `packetManifest.questions` when a route body does not pass questions directly.
    - Answers for unsupported candidate facts become `NEEDS YOU: <reason>` and mark the packet reviewable, not upload-ready.
    - `POST /api/packet/questions` writes the normalized capture through `appRegisterPacketQuestionCapture()` before returning local JSON envelopes.
    - `POST /api/packet/answers` drafts only answerable persisted questions and never invokes the full answer-question skill runtime by default.
  </behavior>
  <action>Create `appRegisterPacketQuestionCapture()` in `src/core/db/verbs/app.mjs` to write `artifacts.packetQuestionsSource`, captured/excluded counts, `packetQuestionsCapturedAt`, and the `packetManifest.questions` summary in one DB verb transaction. Create `src/core/packet/answers.mjs` with `draftPacketAnswers` using labels `skill:"packet-engine"`, `action:"draft-answers"`, `operation:"packet:answers"`, `structuredMode:"native-preferred"`, and `packetAnswerProposalSchema`. Validate proposals with `buildShortAnswer()`, forbidden wording checks, and evidence/gap rules. Mount `POST /api/packet/questions` and `POST /api/packet/answers` in `src/cli/packet-route.mjs` using existing capped body and error-envelope patterns; the questions route must persist the capture before answers or generation consume it.</action>
  <verify>
    <automated>node --test tests/packet-answers.test.mjs tests/form-questions.test.mjs tests/documents-tailor.test.mjs tests/bounded-ai.test.mjs tests/packet-generate-route.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - No generated answer exists for excluded self-identification prompts.
    - Captured questions survive route boundaries through `artifacts.packetQuestionsSource` and `packetManifest.questions`, and answer drafting can consume that durable capture by application ID.
    - No raw prompt/model text, current compensation, private candidate data, or unconfirmed raw proposal content appears in response envelopes.
    - Missing AI returns a manual/reviewable answers response that preserves captured questions and gaps.
  </acceptance_criteria>
  <done>Local question and answer APIs are mounted and covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider/manual form text -> question capture | External form labels are normalized into answerable or excluded packet questions. |
| Answerable questions -> bounded AI | Non-EEO questions and local context become finite answer proposal prompts. |
| AI answer proposal -> markdown artifact | Proposed prose becomes packet content only after evidence and placeholder validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-03-01 | Information Disclosure | `filterAnswerableQuestions()` | high | mitigate | Deterministically exclude provider and manual self-identification prompts before answer drafting. |
| T-10-03-02 | Tampering | `draftPacketAnswers()` | high | mitigate | Require schema-validated answer proposals plus evidence IDs or gap markers, then run document validators. |
| T-10-03-03 | Repudiation | Excluded question metadata | medium | mitigate | Return excluded reason codes and `demographicSectionPresent` metadata so UI can show what was skipped. |
| T-10-03-04 | Information Disclosure | Bounded AI envelopes | high | mitigate | Tests require safe envelopes with no raw prompt, model text, current compensation, or private-only fields. |
| T-10-03-05 | Tampering | Captured question persistence | medium | mitigate | Persist normalized question capture through a DB verb with manifest metadata and tests proving generation consumes the stored artifact. |
| T-10-SC | Tampering | npm installs | low | accept | No package installs. |
</threat_model>

<verification>
Run `node --test tests/packet-answers.test.mjs tests/form-questions.test.mjs tests/documents-tailor.test.mjs tests/bounded-ai.test.mjs tests/packet-generate-route.test.mjs`.
</verification>

<success_criteria>
- PKT-03 provider and manual-paste question capture works locally.
- Captured questions are durable through application artifact metadata and `packetManifest.questions`.
- EEO, disability, veteran, demographic, and similar prompts are excluded before generated-answer automation.
- PKT-02 answers are grounded, validated, or marked as needing user input.
</success_criteria>

<output>
Create `.planning/phases/10-local-packet-engine/10-03-SUMMARY.md` when done.
</output>
