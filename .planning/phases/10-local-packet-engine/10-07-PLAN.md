---
phase: 10-local-packet-engine
plan: 07
type: execute
wave: 6
depends_on: ["10-06"]
files_modified:
  - docs/ARCHITECTURE.md
  - .planning/architecture/runtime-routing-policy.md
  - .planning/architecture/skill-decomposition.yml
autonomous: true
requirements: [PKT-01, PKT-02, PKT-03, PKT-04]
must_haves:
  truths:
    - "Documentation and routing policy describe the local packet engine as the product default and retained skills as explicit handoffs."
    - "Focused packet verification is green, and any repo-wide failures are identified as unrelated residuals rather than packet regressions."
  artifacts:
    - "docs/ARCHITECTURE.md packet-engine section"
    - ".planning/architecture/runtime-routing-policy.md packet owner updates"
    - ".planning/architecture/skill-decomposition.yml packet owner updates"
  key_links:
    - "PKT-01..PKT-04 -> final verification"
    - "Runtime routing policy -> no hidden retained runtime default"
---

## Phase Goal

**As a** Rolester user, **I want to** generate ATS-ready apply packets through local APIs, **so that** I can prepare grounded application materials without launching whole skill workflows for ordinary packet work.

<objective>
Close Phase 10 with docs, source-of-truth routing updates, and focused verification.

Purpose: Make the shipped packet engine discoverable to future agents and prove the final phase behavior against the validation strategy.
Output: Updated architecture/routing/decomposition docs and final verification evidence.
</objective>

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-local-packet-engine/10-CONTEXT.md
@.planning/phases/10-local-packet-engine/10-RESEARCH.md
@.planning/phases/10-local-packet-engine/10-PATTERNS.md
@.planning/phases/10-local-packet-engine/10-VALIDATION.md
@docs/ARCHITECTURE.md
@.planning/architecture/runtime-routing-policy.md
@.planning/architecture/skill-decomposition.yml
@AGENTS.md
@tests/packet-generate-route.test.mjs
@tests/packet-runtime-boundary.test.mjs
@tests/packet-engine.test.mjs
@tests/packet-answers.test.mjs
@tests/packet-export.test.mjs
</context>

## Artifacts This Phase Produces

- Route paths: `POST /api/packet/gate`, `POST /api/packet/questions`, `POST /api/packet/answers`, `POST /api/packet/generate`, `POST /api/packet/export`.
- Core modules: `src/core/packet/context.mjs`, `src/core/packet/gate.mjs`, `src/core/packet/questions.mjs`, `src/core/packet/answers.mjs`, `src/core/packet/generate.mjs`, `src/core/packet/exports.mjs`, `src/core/packet/schemas/packet-schemas.mjs`.
- Functions/helpers: `buildPacketContext`, `evaluatePacketGate`, `capturePacketQuestions`, `classifySelfIdentificationQuestion`, `filterAnswerableQuestions`, `loadPacketQuestionCapture`, `draftPacketAnswers`, `draftCoverLetterBlocks`, `validatePacketEvidenceIds`, `generatePacket`, `exportPacketArtifacts`, `appRegisterPacketQuestionCapture`, `appRegisterPacketArtifacts`.
- Schemas: `packetGateRequestSchema`, `packetGateAiVerdictSchema`, `packetQuestionCaptureRequestSchema`, `packetQuestionCaptureArtifactSchema`, `packetAnswerProposalSchema`, `packetCoverLetterProposalSchema`, `packetGenerateRequestSchema`, `packetExportRequestSchema`, `packetManifestSchema`.
- Application artifact fields: `artifacts.packetManifest`, `artifacts.packetQuestionsSource`, `artifacts.packetQuestionsCapturedAt`, `artifacts.packetQuestionCount`, `artifacts.packetQuestionExcludedCount`, `artifacts.resumeSource`, `artifacts.coverLetterSource`, `artifacts.answersSource`, `artifacts.resumePdf`, `artifacts.coverLetterPdf`, `artifacts.answersPdf`, `artifacts.resumeDocx`, `artifacts.coverLetterDocx`.
- Tests: `tests/packet-generate-route.test.mjs`, `tests/packet-runtime-boundary.test.mjs`, `tests/packet-engine.test.mjs`, `tests/packet-answers.test.mjs`, `tests/packet-export.test.mjs`, updated `tests/packet-page.test.mjs`, updated `tests/answer-page.test.mjs`.

<tasks>

<task type="auto">
  <name>Task 1: Update packet runtime documentation</name>
  <files>docs/ARCHITECTURE.md, .planning/architecture/runtime-routing-policy.md, .planning/architecture/skill-decomposition.yml</files>
  <read_first>
    docs/ARCHITECTURE.md
    .planning/architecture/runtime-routing-policy.md
    .planning/architecture/skill-decomposition.yml
    .planning/phases/10-local-packet-engine/10-CONTEXT.md
    .planning/phases/10-local-packet-engine/10-RESEARCH.md
  </read_first>
  <action>Document the Phase 10 shipped boundary: local packet APIs own gate, question capture, answer drafting, packet generation, export, and DB artifact stamping; bounded AI is allowed only for finite schema-validated gate/answer judgment; retained `evaluate-job`, `tailor-application`, and `answer-question` skills remain explicit agent/user handoffs rather than ordinary product defaults. Update the decomposition entries for evaluate/tailor/answer packet substeps to point at the new local owners while preserving deferred auto-submit and broad browser form-fill boundaries.</action>
  <verify>
    <automated>npm run lint:placeholders</automated>
  </verify>
  <acceptance_criteria>
    - Docs name the local route paths and core owners created in Phase 10.
    - Docs state PDF default and DOCX conditional behavior.
    - Docs state EEO/disability/veteran/demographic answer exclusion.
    - Docs do not teach generated tracker/activity files as packet product source of truth.
  </acceptance_criteria>
  <done>Architecture and routing docs match the local packet engine behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Run final Phase 10 verification rollup</name>
  <files>docs/ARCHITECTURE.md, .planning/architecture/runtime-routing-policy.md, .planning/architecture/skill-decomposition.yml</files>
  <read_first>
    .planning/phases/10-local-packet-engine/10-VALIDATION.md
    tests/packet-generate-route.test.mjs
    tests/packet-runtime-boundary.test.mjs
    tests/packet-engine.test.mjs
    tests/packet-answers.test.mjs
    tests/packet-export.test.mjs
    tests/packet-page.test.mjs
    tests/answer-page.test.mjs
  </read_first>
  <action>Run the focused Phase 10 packet suite from 10-VALIDATION.md plus the current packet baseline tests. Run `npm test` before close-out; if it still fails only on the known Phase 08 deep-ingest AI module/schema gaps from STATE.md, record that residual clearly in the summary and do not patch unrelated Phase 08 files.</action>
  <verify>
    <automated>node --test tests/packet-route.test.mjs tests/form-questions.test.mjs tests/documents-tailor.test.mjs tests/structured-oneshot.test.mjs tests/bounded-ai.test.mjs tests/call-ai.test.mjs tests/evaluate-gate.test.mjs tests/data-route.test.mjs tests/packet-page.test.mjs tests/answer-page.test.mjs tests/packet-generate-route.test.mjs tests/packet-runtime-boundary.test.mjs tests/packet-engine.test.mjs tests/packet-answers.test.mjs tests/packet-export.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Focused Phase 10 packet suite is green.
    - `npm run lint:placeholders` is green after docs updates.
    - `npm test` is run and any failure is classified as Phase 10 regression or documented unrelated residual.
    - Summary maps PKT-01 through PKT-04 to passing evidence.
  </acceptance_criteria>
  <done>Phase 10 focused verification is complete and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Documentation -> future implementation | Future agents rely on docs and decomposition for runtime routing decisions. |
| Verification suite -> release confidence | Focused tests define whether packet behavior is complete despite unrelated suite residuals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-07-01 | Repudiation | Phase 10 verification evidence | medium | mitigate | Summary records exact focused commands, full-suite result, and requirement coverage. |
| T-10-07-02 | Tampering | Runtime routing docs | medium | mitigate | Update routing policy and decomposition with concrete local route/core owners. |
| T-10-07-03 | Information Disclosure | Documentation examples | low | mitigate | Docs describe categories and route owners only; do not copy private candidate details or current compensation. |
| T-10-SC | Tampering | npm installs | low | accept | No package installs. |
</threat_model>

<verification>
Run the focused Phase 10 command in Task 2, `npm run lint:placeholders`, and `npm test` for final classification.
</verification>

<success_criteria>
- PKT-01 through PKT-04 are verified by focused tests.
- Runtime docs and decomposition point ordinary packet work at local APIs.
- Full skill runtime is documented as explicit retained behavior, not the default packet path.
</success_criteria>

<output>
Create `.planning/phases/10-local-packet-engine/10-07-SUMMARY.md` when done.
</output>
