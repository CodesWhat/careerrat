---
phase: 01-decomposition-map
plan: "04"
type: tdd
wave: 2
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - tests/decomposition-map.test.mjs
  - .planning/architecture/skill-decomposition.yml
  - .planning/architecture/discover-companies-target-contract.md
  - .planning/architecture/runtime-routing-policy.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
requirements_addressed:
  - ARCH-01
  - ARCH-02
  - ARCH-03
must_haves:
  truths:
    - "Validation test proves the decomposition inventory covers high-priority skills and required classification buckets."
    - "Validation test proves every owner path either exists now or uses an explicit `planned:` prefix."
    - "Validation test proves the target contract contains locked sourcing cascade, resolver cache fields, bakeoff lanes, confirmation rule, and no runtime implementation boundary."
    - "Validation test proves runtime routing policy distinguishes local APIs, DB verbs/CLI helpers, bounded AI, `/api/chat/*`, and `POST /api/skill/run`."
  artifacts:
    - path: "tests/decomposition-map.test.mjs"
      provides: "Machine-checkable drift guard for Phase 1 architecture artifacts."
      contains: "node:test"
    - path: ".planning/architecture/skill-decomposition.yml"
      provides: "Validated decomposition inventory."
      contains: "skills:"
    - path: ".planning/architecture/discover-companies-target-contract.md"
      provides: "Validated discovery target contract."
      contains: "companyBoardResolutionCache"
    - path: ".planning/architecture/runtime-routing-policy.md"
      provides: "Validated runtime routing policy."
      contains: "POST /api/skill/run"
  key_links:
    - from: "tests/decomposition-map.test.mjs"
      to: ".planning/architecture/skill-decomposition.yml"
      via: "parseYaml artifact validation"
      pattern: "parseYaml"
    - from: "tests/decomposition-map.test.mjs"
      to: ".planning/architecture/discover-companies-target-contract.md"
      via: "required content assertions"
      pattern: "companyBoardResolutionCache"
    - from: "tests/decomposition-map.test.mjs"
      to: ".planning/architecture/runtime-routing-policy.md"
      via: "required routing assertions"
      pattern: "POST /api/skill/run"
---

<objective>
Create the focused validation test for Phase 1 planning artifacts.

Purpose: The architecture map should remain machine-checkable so later phases cannot drift from the locked decomposition, cache, and routing decisions.
Output: `tests/decomposition-map.test.mjs`, plus artifact refinements only when the new test exposes a concrete gap.
</objective>

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-decomposition-map/01-CONTEXT.md
@.planning/phases/01-decomposition-map/01-RESEARCH.md
@.planning/architecture/skill-decomposition.yml
@.planning/architecture/discover-companies-target-contract.md
@.planning/architecture/runtime-routing-policy.md
@tests/yaml.test.mjs
@tests/config-yaml-parses.test.mjs
@src/core/profile/yaml.mjs
</context>

## Artifacts This Phase Produces

- `.planning/architecture/skill-decomposition.yml` - created by Plan 01-01 and validated here.
- `.planning/architecture/discover-companies-target-contract.md` - created by Plan 01-02 and validated here.
- `.planning/architecture/runtime-routing-policy.md` - created by Plan 01-03 and validated here.
- `tests/decomposition-map.test.mjs` - created by this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add decomposition artifact validation test</name>
  <files>tests/decomposition-map.test.mjs, .planning/architecture/skill-decomposition.yml, .planning/architecture/discover-companies-target-contract.md, .planning/architecture/runtime-routing-policy.md</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/01-decomposition-map/01-CONTEXT.md, .planning/phases/01-decomposition-map/01-RESEARCH.md, .planning/architecture/skill-decomposition.yml, .planning/architecture/discover-companies-target-contract.md, .planning/architecture/runtime-routing-policy.md, tests/yaml.test.mjs, tests/config-yaml-parses.test.mjs, src/core/profile/yaml.mjs</read_first>
  <behavior>
    - Test 1: `skill-decomposition.yml` parses with `parseYaml` and contains `skills` entries for `setup-searches`, `research-boards`, `discover-companies`, `search-jobs`, `evaluate-job`, `apply-job`, `email-comms`, `interview-prep`, and `track-outcomes`.
    - Test 2: every listed skill has array buckets named `deterministic`, `bounded_ai`, `full_skill_runtime`, `prompt_spec`, and `deferred`.
    - Test 3: every non-empty owner reference in the inventory either points to an existing repo path or starts with `planned:`.
    - Test 4: the `discover-companies` contract contains `companySeedSchema`, `companyBoardResolutionCache`, all required cache fields from D-05, all sourcing lanes from D-01/D-02/D-10, and the supported ATS versus unsupported public-page split from D-14.
    - Test 5: the routing policy contains local API, DB verb/CLI, bounded AI, `/api/chat/*`, and `POST /api/skill/run` rules for UI, CLI, and agents.
    - Test 6: all three artifacts contain the Phase 1 non-runtime boundary and D-01 through D-14 appear across the architecture artifacts.
  </behavior>
  <action>Create `tests/decomposition-map.test.mjs` using `node:test`, `node:assert/strict`, `node:fs`, `node:path`, and `parseYaml` from `../src/core/profile/yaml.mjs`. Write tests for the six behaviors listed above. The owner-path assertion must accept repo-relative files that exist and `planned:` owner strings; it must reject bare missing paths. Run the test after creating it. If the first run fails because an artifact is missing a required field, bucket, owner path, decision citation, or routing phrase, update only `.planning/architecture/skill-decomposition.yml`, `.planning/architecture/discover-companies-target-contract.md`, or `.planning/architecture/runtime-routing-policy.md` to satisfy the explicit assertion. Do not modify runtime files. Do not add package dependencies.</action>
  <acceptance_criteria>
    - `tests/decomposition-map.test.mjs` imports `test` from `node:test` and `parseYaml` from `../src/core/profile/yaml.mjs`.
    - `node --test tests/decomposition-map.test.mjs` exits 0.
    - The test fails if `skills.discover-companies.bounded_ai` is removed from the inventory.
    - The test fails if `companyBoardResolutionCache` is removed from the target contract.
    - The test fails if `POST /api/skill/run` is removed from the routing policy.
    - `git diff --name-only` after this task contains no `src/` runtime files from this plan.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/decomposition-map.test.mjs</automated>
  </verify>
  <done>Validation test exists, passes, and makes ARCH-01/ARCH-02/ARCH-03 artifact drift visible without changing runtime code.</done>
</task>

</tasks>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | phase-goal | Make the architecture explicit before changing runtime behavior. | 01-01, 01-02, 01-03, 01-04 | COVERED | Artifact plans create the map; validation plan enforces the non-runtime boundary. |
| REQ | ARCH-01 | Skill decomposition inventory classifies each step as deterministic code, bounded AI, full skill-agent run, prompt/spec, or deferred. | 01-01, 01-04 | COVERED | Inventory plus validation. |
| REQ | ARCH-02 | Inventory maps classified steps to existing or planned owners. | 01-01, 01-02, 01-04 | COVERED | Inventory and target contract name owner paths. |
| REQ | ARCH-03 | Routing policy defines when UI, CLI, and agents call local APIs instead of `POST /api/skill/run`. | 01-03, 01-04 | COVERED | Routing policy plus validation. |
| RESEARCH | rec-artifacts | Create `.planning/architecture/skill-decomposition.yml`, `.planning/architecture/discover-companies-target-contract.md`, `.planning/architecture/runtime-routing-policy.md`, and `tests/decomposition-map.test.mjs`. | 01-01, 01-02, 01-03, 01-04 | COVERED | Matches recommended artifact set. |
| RESEARCH | supported-ats-gap | Separate supported ATS promotion from unsupported/custom public-page cache. | 01-02, 01-04 | COVERED | Explicit contract and test assertion. |
| RESEARCH | routing-owner-paths | Reference full skill runtime, bounded AI, scanner, source config, discovery route, and provider owners by path. | 01-01, 01-02, 01-03, 01-04 | COVERED | Owner paths appear in artifacts and are testable. |
| CONTEXT | D-01 | Include multiple sourcing lanes. | 01-01, 01-02, 01-04 | COVERED | Inventory and contract list lanes. |
| CONTEXT | D-02 | Use cheapest-first runtime order. | 01-02, 01-03, 01-04 | COVERED | Contract and routing policy. |
| CONTEXT | D-03 | Use AI web search for gaps, judgment, ranking, and ambiguity. | 01-02, 01-03, 01-04 | COVERED | Contract and routing policy. |
| CONTEXT | D-04 | Resolve company board once and save durable result. | 01-02, 01-04 | COVERED | Resolver cache contract. |
| CONTEXT | D-05 | Cache required resolution fields. | 01-02, 01-04 | COVERED | Cache field assertions. |
| CONTEXT | D-06 | Re-resolution is event-driven or TTL-based. | 01-02, 01-04 | COVERED | Contract and validation. |
| CONTEXT | D-07 | Use Node fetch first, then Playwright for public JS-rendered pages. | 01-02, 01-04 | COVERED | Contract and validation. |
| CONTEXT | D-08 | Optimize for freshness, disappearing postings, cost, reliability, cacheability, and JD capture. | 01-02, 01-04 | COVERED | Metrics and capture contract. |
| CONTEXT | D-09 | Preserve first reachable full JD body locally. | 01-02, 01-04 | COVERED | Contract names sourced persistence owner. |
| CONTEXT | D-10 | Specify bakeoff without vendor commitment. | 01-02, 01-04 | COVERED | Bakeoff metrics and lane list. |
| CONTEXT | D-11 | Measure jobs per dollar, JD capture quality, apply-link quality, freshness, duplicate rate, unsupported-page rate, failures, and model/tool calls. | 01-02, 01-04 | COVERED | Bakeoff metrics. |
| CONTEXT | D-12 | Skills remain workflow contracts while product runtime decomposes work. | 01-01, 01-03, 01-04 | COVERED | Inventory and routing policy. |
| CONTEXT | D-13 | `discover-companies` becomes seed, resolver, scanner, gate, confirm-first write pipeline. | 01-02, 01-04 | COVERED | Target contract. |
| CONTEXT | D-14 | Separate supported ATS scanner promotion from unsupported/custom public-page cache. | 01-02, 01-04 | COVERED | Contract and validation. |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Architecture artifacts to automated test | Human-authored planning artifacts become machine-checked inputs. |
| Test assertions to runtime source tree | The test must guard architecture drift without requiring runtime changes in Phase 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Tampering | `tests/decomposition-map.test.mjs` | medium | mitigate | Tests assert required decisions, owner paths, and route strings so later artifact edits cannot silently remove them. |
| T-01-11 | Information Disclosure | architecture artifact tests | medium | mitigate | Tests must check repo-local architecture paths and must not read candidate workspace data or private tracker state. |
| T-01-12 | Repudiation | Phase 1 source coverage | low | mitigate | Source coverage audit in this plan maps GOAL, ARCH requirements, research constraints, and D-01 through D-14 to plan IDs. |
| T-01-SC | Tampering | package installs | low | accept | No package-manager install task exists in this plan; executor must not add npm, pip, or cargo installs while creating the validation test. |
</threat_model>

<verification>
Run `node --test tests/decomposition-map.test.mjs`, then run `npm test` if the focused validation passes and context budget allows.
</verification>

<success_criteria>
- Focused validation test passes.
- The test covers all Phase 1 architecture artifacts.
- Runtime files under `src/` are not modified by this phase.
- Source audit has no missing GOAL, REQ, RESEARCH, or CONTEXT items.
</success_criteria>

<output>
Create `.planning/phases/01-decomposition-map/01-04-SUMMARY.md` when done.
</output>
