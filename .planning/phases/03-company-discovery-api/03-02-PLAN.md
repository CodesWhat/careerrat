---
phase: 03-company-discovery-api
plan: "02"
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/core/db/migrations/006-company-discovery-cache.mjs
  - src/core/db/migrations.mjs
  - src/core/db/verbs/company-discovery.mjs
  - src/core/db/verbs/index.mjs
  - tests/company-discovery-cache-db.test.mjs
autonomous: true
requirements:
  - DISC-02
  - DISC-04
  - DISC-05
requirements_addressed:
  - DISC-02
  - DISC-04
  - DISC-05
must_haves:
  truths:
    - "Company board resolution and proposal state is DB-owned, not written into generated tracker/dashboard files, per D-14, D-29, and D-30."
    - "Resolution cache rows preserve company, domain, careers URL, board URL, provider, API URL, confidence, provenance, timestamps, scan result, failure count, zero-job count, and refresh reason per D-15 and resolved research."
    - "Pending proposal batches carry versioned state so decision routes can reject stale or conflicting updates with 409 per D-07 and D-23."
  artifacts:
    - path: "src/core/db/migrations/006-company-discovery-cache.mjs"
      provides: "SQLite tables for company board resolutions and proposal batches"
    - path: "src/core/db/verbs/company-discovery.mjs"
      provides: "DB verbs for resolution cache and proposal batch state"
    - path: "tests/company-discovery-cache-db.test.mjs"
      provides: "Migration, cache, latest-pending, due-list, and version-conflict coverage"
  key_links:
    - from: "src/core/db/migrations.mjs"
      to: "src/core/db/migrations/006-company-discovery-cache.mjs"
      via: "sequential migration registration"
      pattern: "ALL_MIGRATIONS"
    - from: "src/core/db/verbs/index.mjs"
      to: "src/core/db/verbs/company-discovery.mjs"
      via: "single DB verb import surface"
      pattern: "verbs barrel"
---

## Phase Goal

**As a** CareerRat discovery caller, **I want to** create and decide company proposals through local API/core behavior, **so that** discovery proves the skill-to-API pattern without launching full skills for deterministic work.

<objective>
Harden the DB-owned resolver cache and proposal state behind the callable API slice.

Purpose: Plan 03-01 created a working route slice. This plan completes the durable cache/proposal state required by D-14 through D-17 and the conflict boundary required by D-23.
Output: Migration 006 hardening, company discovery DB verbs, barrel exports, and focused DB tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-company-discovery-api/03-CONTEXT.md
@.planning/phases/03-company-discovery-api/03-RESEARCH.md
@.planning/phases/03-company-discovery-api/03-PATTERNS.md
@.planning/phases/03-company-discovery-api/03-VALIDATION.md
@.planning/phases/03-company-discovery-api/03-01-SUMMARY.md
@.planning/architecture/discover-companies-target-contract.md
@src/core/db/migrations.mjs
@src/core/db/migrations/005-source-config.mjs
@src/core/db/verbs/source-config.mjs
@src/core/db/verbs/index.mjs
@tests/db-source-config.test.mjs
</context>

## Artifacts this phase produces

- Hardened `src/core/db/migrations/006-company-discovery-cache.mjs`.
- Hardened `src/core/db/verbs/company-discovery.mjs`.
- Updated migration list in `src/core/db/migrations.mjs` if Plan 03-01 did not fully register migration 006.
- Updated DB verb barrel in `src/core/db/verbs/index.mjs`.
- New tests in `tests/company-discovery-cache-db.test.mjs`.
- Exported DB functions: `companyBoardResolutionGet`, `companyBoardResolutionUpsert`, `companyBoardResolutionListDue`, `companyProposalBatchPut`, `companyProposalBatchGet`, `companyProposalBatchLatest`, and `companyProposalBatchPatchState`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD DB-owned resolver cache and proposal state hardening</name>
  <files>src/core/db/migrations/006-company-discovery-cache.mjs, src/core/db/migrations.mjs, src/core/db/verbs/company-discovery.mjs, src/core/db/verbs/index.mjs, tests/company-discovery-cache-db.test.mjs</files>
  <read_first>
    <file>.planning/phases/03-company-discovery-api/03-CONTEXT.md</file>
    <file>.planning/phases/03-company-discovery-api/03-RESEARCH.md</file>
    <file>.planning/phases/03-company-discovery-api/03-PATTERNS.md</file>
    <file>.planning/phases/03-company-discovery-api/03-01-SUMMARY.md</file>
    <file>src/core/db/migrations.mjs</file>
    <file>src/core/db/migrations/005-source-config.mjs</file>
    <file>src/core/db/verbs/source-config.mjs</file>
    <file>src/core/db/verbs/index.mjs</file>
    <file>tests/db-source-config.test.mjs</file>
  </read_first>
  <behavior>
    - RED: Create `tests/company-discovery-cache-db.test.mjs` with temp-repo tests that initialize the DB, assert migration id 6 applies, and assert both `company_board_resolutions` and `company_discovery_proposals` exist with `json_valid(data)` constraints.
    - RED: Add tests for `companyBoardResolutionUpsert()` and `companyBoardResolutionGet()` preserving `company_key`, `company_name`, `company_domain`, `careers_url`, `job_board_url`, `ats_provider`, `api_url`, `confidence`, `provenance`, `first_resolved_at`, `last_verified_at`, `last_scan_result`, `failure_count`, `zero_job_count`, `next_refresh_reason`, and `status`.
    - RED: Add tests for `companyBoardResolutionListDue({ now })` using pinned refresh reasons and thresholds from resolved research: 14-day TTL, failure threshold 2, zero-job threshold 2, 403/404 status, failed extraction, provider change, and stored refresh reason.
    - RED: Add tests for `companyProposalBatchPut()`, `companyProposalBatchGet()`, `companyProposalBatchLatest({ status:"pending" })`, and `companyProposalBatchPatchState({ expectedVersion })`, including a stale-version conflict that throws an error with `code:"CONFLICT"`.
    - GREEN: Harden migration 006 in ascending order, then implement DB-only verbs that call `requireDb()` and `withTransaction()` and never call model, network, scanner, sourced persistence, tracker export, or source-config write code.
    - REFACTOR: Keep JSON clone/parse helpers local to `company-discovery.mjs`; keep generated columns and indexes aligned with lookup paths used by the tests.
  </behavior>
  <action>Complete the DB-owned resolver/proposal state per D-14, D-15, D-16, D-17, and resolved research decisions. `company_board_resolutions` must store JSON data with generated columns for `company_key`, `company_domain`, `ats_provider`, `status`, `last_verified_at`, `next_refresh_reason`, `failure_count`, and `zero_job_count`, plus indexes for company key, provider, status, verification time, and due-refresh scans. `company_discovery_proposals` must store JSON batch data with generated columns for `status`, `version`, and `created_at`, plus an index for latest pending reads. Register `migration006` in `ALL_MIGRATIONS` immediately after `migration005`. Implement DB verbs that preserve versioned pending state for D-23 decisions and return `CONFLICT` for stale `expectedVersion`. Do not write `workspace/tracker.json`, `workspace/activity.jsonl`, legacy source config files, sourced rows, or AI usage state from these verbs per D-29 and D-30.</action>
  <verify>
    <automated>node --test tests/company-discovery-cache-db.test.mjs tests/company-proposals-route.test.mjs tests/db-source-config.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/company-discovery-cache-db.test.mjs tests/company-proposals-route.test.mjs tests/db-source-config.test.mjs` exits 0.
    - Migration registration remains sequential and a new temp DB reaches `PRAGMA user_version = 6`.
    - Resolution cache upsert/read round-trips every D-15 and resolved-research field named in the action.
    - Due-refresh listing uses the pinned thresholds: 14 days stale TTL, failure count 2, zero-job count 2, 403/404, provider change, failed extraction, and stored refresh reason.
    - Latest-pending proposal lookup returns the newest pending batch and excludes decided batches.
    - Stale proposal patch attempts throw `code === "CONFLICT"` and leave stored JSON unchanged.
    - DB verbs do not create `workspace/tracker.json`, `workspace/activity.jsonl`, or `config/sourced-scan.json` in the temp repo.
  </acceptance_criteria>
  <done>Migration 006 and company-discovery DB verbs provide durable cache/proposal state with refresh-due queries, conflict checks, and no generated-file writes.</done>
</task>

</tasks>

<source_audit>

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | Phase 03 | Prove the skill-to-API pattern on the highest-leverage discovery flow | 03-01..03-07 | COVERED | The callable slice exists from 03-01; this plan hardens its DB state. |
| REQ | DISC-01 | AI/manual company seeds use candidate context and dedupe inputs | 03-01, 03-04, 03-07 | COVERED | DB state supports seed-derived proposal batches. |
| REQ | DISC-02 | Deterministic resolver owns supported ATS URL resolution | 03-01, 03-02, 03-03, 03-07 | COVERED | This plan creates durable resolver cache state. |
| REQ | DISC-03 | Existing ATS scanners verify current roles before proposal | 03-01, 03-05, 03-07 | COVERED | Proposal state stores scan summaries for gate/decision use. |
| REQ | DISC-04 | Dedupe, exclusions, relevance, comp plausibility, supported-ATS gates | 03-01, 03-02, 03-05, 03-07 | COVERED | This plan stores versioned proposal state; Plan 03-05 enforces gates. |
| REQ | DISC-05 | Confirmed additions write only through existing source-config/companies path | 03-06, 03-07 | COVERED | This plan supplies decision state and does not confirm writes. |
| CONTEXT | D-01, D-02, D-03 | Local-first, deterministic authority, retained skill fallback only | 03-01..03-07 | COVERED | DB state remains local deterministic state. |
| CONTEXT | D-04, D-05, D-06, D-07 | `/api/discovery` routes, thin handlers, stable status envelopes | 03-01, 03-06, 03-07 | COVERED | DB conflicts give route-mappable error codes. |
| CONTEXT | D-08, D-09, D-10, D-11, D-12, D-13 | Bounded AI seed generation and no-AI/manual fallback | 03-04, 03-07 | COVERED | AI state is not stored as trusted authority. |
| CONTEXT | D-14, D-15, D-16, D-17 | DB-owned resolver cache and supported/unsupported separation | 03-02, 03-03, 03-07 | COVERED | This plan directly implements storage and due-refresh state. |
| CONTEXT | D-18, D-19, D-20, D-21, D-22 | Cheapest-first resolver/scanner/JD capture pipeline | 03-03, 03-05, 03-07 | COVERED | Cache state supports resolver/scanner decisions. |
| CONTEXT | D-23, D-24, D-25, D-26, D-27 | Confirm-first proposal states and metadata | 03-02, 03-05, 03-06 | COVERED | Proposal batches are pending/versioned before decisions. |
| CONTEXT | D-28, D-29, D-30, D-31 | Confirmation writes through existing DB/source and sourced paths | 03-06, 03-07 | COVERED | Decision plan owns writes. |
| RESEARCH | R-CONSTANTS | TTL 14 days, zero-job threshold 2, failure threshold 2, batch max 12 | 03-02 | COVERED | DB due-list tests use every pinned constant. |
| PATTERN | P-DB | Follow SQLite JSON-table and source-config verb patterns | 03-02 | COVERED | This plan copies the migration/verb ownership pattern. |

</source_audit>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| core discovery -> SQLite | Resolver/proposal JSON enters durable local state. |
| proposal reader -> decision writer | Decision flows later rely on current proposal version and status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-03-02-01 | Tampering | `company_discovery_proposals` | high | mitigate | Store proposal `version` and require `expectedVersion` for state patches; stale attempts return `CONFLICT`. |
| T-03-02-02 | Information Disclosure | DB verb outputs | medium | mitigate | Store local proposal data only and return parsed JSON; do not export prompts, raw model text, or JD bodies from DB errors. |
| T-03-02-03 | Denial of Service | proposal/cache tables | medium | mitigate | Use generated columns and indexes for company key, provider, status, latest pending reads, and due-refresh scans. |
| T-03-02-04 | Tampering | generated dashboard files | high | mitigate | DB cache/proposal verbs never write generated tracker/dashboard compatibility files. |

</threat_model>

<verification>
Run the DB cache hardening slice:

<automated>node --test tests/company-discovery-cache-db.test.mjs tests/company-proposals-route.test.mjs tests/db-source-config.test.mjs</automated>
</verification>

<success_criteria>
- Company discovery cache/proposal tables exist in SQLite migration 006.
- Resolver cache rows preserve all D-15 and resolved-research fields.
- Proposal batches can be stored, read by id, read as latest pending, and patched with version conflict protection.
- Due-refresh queries use exact pinned constants.
- No generated tracker/dashboard files are direct write targets.
</success_criteria>

<output>
Create `.planning/phases/03-company-discovery-api/03-02-SUMMARY.md` when done.
</output>
