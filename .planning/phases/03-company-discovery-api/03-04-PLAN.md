---
phase: 03-company-discovery-api
plan: "04"
type: tdd
wave: 4
depends_on:
  - 03-03
files_modified:
  - src/core/discovery/company-context.mjs
  - src/core/discovery/company-seeds.mjs
  - src/core/discovery/company-proposals.mjs
  - tests/company-discovery-seeds.test.mjs
  - tests/company-proposals-route.test.mjs
autonomous: true
requirements:
  - DISC-01
requirements_addressed:
  - DISC-01
must_haves:
  truths:
    - "AI seed generation uses `runBoundedAI()` with labels `skill:\"discover-companies\"`, `action:\"seed-generate\"`, and `operation:\"company-seeds\"` per D-08 and D-09."
    - "`companySeedSchema` returns top-level `companies[]` and rejects trusted final URL, API URL, provider, and approval fields per D-10 and D-11."
    - "Seed prompt context includes candidate profile domain, role families, location posture, keep/cut signals, exclusions, dedupe inputs, and allowed comp floor fields for DISC-01."
    - "Privacy tests prove `current_base` and private current compensation never appear in prompts, route data, telemetry, or proposal output."
    - "Manual seeds work without AI; no AI and no manual seeds returns the shared no-AI/manual envelope with status 501 per D-13."
  artifacts:
    - path: "src/core/discovery/company-context.mjs"
      provides: "Candidate profile/source/dedupe context assembly with privacy filtering"
    - path: "src/core/discovery/company-seeds.mjs"
      provides: "Seed schema, manual seed normalization, bounded prompt assembly, and bounded AI seed call"
    - path: "tests/company-discovery-seeds.test.mjs"
      provides: "Seed schema, profile context, privacy, manual fallback, no-AI/manual envelope, labels, and native-preferred coverage"
  key_links:
    - from: "src/core/discovery/company-seeds.mjs"
      to: "src/core/ai/bounded-ai.mjs"
      via: "`runBoundedAI()` with native-preferred structured output"
      pattern: "runBoundedAI"
    - from: "src/core/discovery/company-proposals.mjs"
      to: "src/core/discovery/company-seeds.mjs"
      via: "proposal batches can request AI seeds when manual seeds are absent or additional seeds are requested"
      pattern: "generateCompanySeeds"
---

## Phase Goal

**As a** Rolester discovery caller, **I want to** create and decide company proposals through local API/core behavior, **so that** discovery proves the skill-to-API pattern without launching full skills for deterministic work.

<objective>
Add bounded AI company seed generation with explicit candidate-profile context and privacy guards.

Purpose: DISC-01 requires candidate-aware structured company seeds, but model output remains untrusted and advisory. This plan adds the AI seed owner while preserving manual seed operation, stable no-AI behavior, and compensation privacy.
Output: Candidate context module, seed schema/module, tests, and proposal orchestration integration.
</objective>

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-company-discovery-api/03-CONTEXT.md
@.planning/phases/03-company-discovery-api/03-RESEARCH.md
@.planning/phases/03-company-discovery-api/03-PATTERNS.md
@.planning/phases/03-company-discovery-api/03-VALIDATION.md
@.planning/phases/02-bounded-ai-foundation/02-01-SUMMARY.md
@.planning/phases/02-bounded-ai-foundation/02-03-SUMMARY.md
@.planning/phases/03-company-discovery-api/03-01-SUMMARY.md
@.planning/phases/03-company-discovery-api/03-03-SUMMARY.md
@AGENTS.md
@candidate/AGENTS.md
@src/core/ai/bounded-ai.mjs
@src/core/discovery/company-proposals.mjs
@tests/bounded-ai.test.mjs
@tests/company-proposals-route.test.mjs
</context>

## Artifacts this phase produces

- New `src/core/discovery/company-context.mjs`.
- New `src/core/discovery/company-seeds.mjs`.
- New `tests/company-discovery-seeds.test.mjs`.
- Exported `companySeedSchema`.
- Exported `buildCompanySeedContext()`, `normalizeManualCompanySeeds()`, and `generateCompanySeeds()`.
- Proposal route support for AI-generated seeds with no-AI/manual fallback.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD bounded AI company seed generation with profile privacy</name>
  <files>src/core/discovery/company-context.mjs, src/core/discovery/company-seeds.mjs, src/core/discovery/company-proposals.mjs, tests/company-discovery-seeds.test.mjs, tests/company-proposals-route.test.mjs</files>
  <read_first>
    <file>.planning/phases/03-company-discovery-api/03-CONTEXT.md</file>
    <file>.planning/phases/03-company-discovery-api/03-RESEARCH.md</file>
    <file>.planning/phases/03-company-discovery-api/03-PATTERNS.md</file>
    <file>.planning/phases/02-bounded-ai-foundation/02-01-SUMMARY.md</file>
    <file>.planning/phases/02-bounded-ai-foundation/02-03-SUMMARY.md</file>
    <file>.planning/phases/03-company-discovery-api/03-01-SUMMARY.md</file>
    <file>.planning/phases/03-company-discovery-api/03-03-SUMMARY.md</file>
    <file>AGENTS.md</file>
    <file>candidate/AGENTS.md</file>
    <file>src/core/ai/bounded-ai.mjs</file>
    <file>src/core/discovery/company-proposals.mjs</file>
    <file>tests/bounded-ai.test.mjs</file>
    <file>tests/company-proposals-route.test.mjs</file>
  </read_first>
  <behavior>
    - RED: Add schema tests asserting `companySeedSchema` requires top-level `companies[]`, accepts `name`, optional `domain_hint`, `why`, `role_family_hint`, `confidence`, and `source_hint`, and rejects `careers_url`, `job_board_url`, `api_url`, `provider`, `approved`, and other extra properties.
    - RED: Add `buildCompanySeedContext()` tests using a temp candidate setup that includes profile domain, role-family buckets, location posture, keep signals, cut signals, excluded companies, tracked companies, applications, sourced companies, and compensation fields; assert prompt context includes only allowed floor fields such as `minimum_base` and OE floor when configured.
    - RED: Add privacy tests proving `current_base` and private current compensation are omitted from prompt messages, route response data, `meta`, telemetry labels, proposal objects, rejection objects, and any serialized AI request options captured by the injected call seam.
    - RED: Add tests proving `generateCompanySeeds()` with manual seeds returns normalized companies and does not invoke an injected AI call.
    - RED: Add tests proving no manual seeds plus no AI route returns status 501 with `ok:false`, `code:"NO_AI_ROUTE"`, `manual.available:true`, and no chat/full-skill launch.
    - RED: Add tests proving AI seed generation calls `runBoundedAI()` or an injected call seam with `structuredMode:"native-preferred"`, `outputName:"company_seed_response"`, max seed count 12, and exact D-09 labels.
    - RED: Extend proposal route tests so a body requesting AI seeds uses the candidate/dedupe context and then feeds returned seeds into resolver/scanner/gate behavior from Plans 03-01 through 03-03.
    - GREEN: Implement `company-context.mjs`, `company-seeds.mjs`, and integrate seed generation into `createCompanyProposalBatch()` before resolution.
    - REFACTOR: Keep prompt construction deterministic and sanitized; keep route responses on the shared bounded AI envelope for AI failures.
  </behavior>
  <action>Create `src/core/discovery/company-context.mjs` and `src/core/discovery/company-seeds.mjs` per D-08, D-09, D-10, D-11, D-12, D-13, and DISC-01. `buildCompanySeedContext({ repoRoot, env })` must assemble candidate profile domain, role-family buckets, location posture, keep signals, cut signals, exclusions, already-tracked companies, applications, sourced companies, dedupe keys, and allowed compensation floor fields such as `minimum_base` or configured OE floor. It must never include `current_base` or private current compensation in prompt context, route data, telemetry, or proposal output. The exported `companySeedSchema` must be named exactly that and use top-level `companies[]` with a hard `maxItems` of 12. `generateCompanySeeds({ repoRoot, env, context, manualSeeds, requestedCount, call, now })` must normalize manual seeds first, call `runBoundedAI()` only when AI seeds are needed, and return the shared 501 manual envelope when there is no AI route and no manual input. Integrate it into `createCompanyProposalBatch()` without trusting AI URL/provider/write fields and without starting `POST /api/skill/run` or chat runtime.</action>
  <verify>
    <automated>node --test tests/company-discovery-seeds.test.mjs tests/company-proposals-route.test.mjs tests/bounded-ai.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/company-discovery-seeds.test.mjs tests/company-proposals-route.test.mjs tests/bounded-ai.test.mjs` exits 0.
    - Schema tests reject final URL, API URL, provider, approval, and unknown fields.
    - Prompt context tests explicitly cover profile domain, role-family buckets, location posture, keep/cut signals, exclusions, dedupe inputs, and allowed comp floor fields.
    - Privacy tests prove `current_base` and private current compensation are absent from prompts, route responses, telemetry metadata, and proposal/rejection output.
    - Manual seeds run with no AI invocation.
    - No AI and no manual seeds returns exactly status 501 and a manual fallback envelope.
    - AI seed calls carry exact labels `discover-companies`, `seed-generate`, and `company-seeds`.
    - Proposal route tests prove AI-generated seeds still require deterministic resolver/scanner/gate processing before presentation.
  </acceptance_criteria>
  <done>Company proposal creation can obtain schema-validated AI seed candidates using explicit candidate-profile context or continue manually with stable no-AI behavior and current-comp privacy protections.</done>
</task>

</tasks>

<source_audit>

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | Phase 03 | Prove skill-to-API on discovery | 03-01..03-07 | COVERED | This plan proves bounded AI is a seed source, not a runtime owner. |
| REQ | DISC-01 | Structured company seed generation with candidate context | 03-01, 03-04, 03-07 | COVERED | This plan implements the seed schema, bounded AI call, and profile context. |
| REQ | DISC-02 | Deterministic resolution | 03-01, 03-03 | COVERED | AI seeds feed resolver only as untrusted input. |
| REQ | DISC-03 | Scanner role proof | 03-01, 03-05 | COVERED | Generated seeds flow into scanner-backed proposal generation. |
| REQ | DISC-04 | Gates before presentation | 03-05 | COVERED | AI confidence never bypasses gate rules. |
| REQ | DISC-05 | Confirmed writes through existing path | 03-06 | COVERED | Seed generation has no write approval. |
| CONTEXT | D-01, D-02, D-03 | Local-first and deterministic authority | 03-04 | COVERED | AI owns seed judgment only. |
| CONTEXT | D-04, D-05, D-06, D-07 | API/status policy | 03-01, 03-06 | COVERED | No-AI maps through stable route envelopes. |
| CONTEXT | D-08, D-09, D-10, D-11, D-12, D-13 | Bounded AI seed contract | 03-04 | COVERED | This plan directly implements every listed decision. |
| CONTEXT | D-14, D-15, D-16, D-17 | DB resolver cache | 03-02, 03-03 | COVERED | Seeds do not replace cache ownership. |
| CONTEXT | D-18, D-19, D-20, D-21, D-22 | Resolver/scanner pipeline | 03-03, 03-05 | COVERED | AI output enters cheapest-first deterministic lanes. |
| CONTEXT | D-23, D-24, D-25, D-26, D-27 | Confirm-first proposal metadata | 03-05 | COVERED | Seed reasons become proposal rationale only after gate. |
| CONTEXT | D-28, D-29, D-30, D-31 | Confirmation-only write path | 03-06 | COVERED | Seed generation cannot approve writes. |
| RESEARCH | R-AI | Use `runBoundedAI()` native-preferred and no-AI/manual degradation | 03-04 | COVERED | This plan implements it. |
| PATTERN | P-AI | Reuse Phase 02 helper/envelope/label pattern | 03-04 | COVERED | Tests reference existing bounded-AI behavior. |

</source_audit>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| candidate context -> AI prompt | Local candidate/dedupe facts enter bounded model context. |
| AI provider -> seed parser | Untrusted model JSON enters local schema validation. |
| seed generator -> resolver | Validated seed fields enter deterministic resolution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-03-04-01 | Tampering | `companySeedSchema` | high | mitigate | `additionalProperties:false` rejects trusted final URL/provider/approval fields. |
| T-03-04-02 | Repudiation | AI usage labels | high | mitigate | Tests assert exact `skill`, `action`, and `operation` labels before invocation. |
| T-03-04-03 | Information Disclosure | candidate compensation privacy | high | mitigate | Tests assert `current_base` and private current compensation are absent from prompt, route, telemetry, and proposal output. |
| T-03-04-04 | Information Disclosure | bounded AI errors | high | mitigate | Reuse `runBoundedAI()` safe envelopes and avoid raw candidate/prompt/model text in route errors. |
| T-03-04-05 | Denial of Service | seed count | medium | mitigate | Enforce hard max 12 companies in schema and request handling. |

</threat_model>

<verification>
Run the seed-generation slice:

<automated>node --test tests/company-discovery-seeds.test.mjs tests/company-proposals-route.test.mjs tests/bounded-ai.test.mjs</automated>
</verification>

<success_criteria>
- `companySeedSchema` is exported and rejects write-ready fields.
- Candidate profile/domain/location/role/keep-cut/exclusion/dedupe/floor context is explicitly tested.
- Manual seeds remain AI-free.
- No-AI/no-manual returns a 501 manual envelope.
- AI seeds use Phase 02 bounded helper labels and local schema validation.
- Private current compensation does not appear outside local config reads.
</success_criteria>

<output>
Create `.planning/phases/03-company-discovery-api/03-04-SUMMARY.md` when done.
</output>
