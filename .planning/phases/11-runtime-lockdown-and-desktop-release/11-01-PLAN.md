---
phase: 11-runtime-lockdown-and-desktop-release
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/app-default-runtime-guard.test.mjs
autonomous: true
requirements: [SEC-01]
requirements_addressed: [SEC-01]
must_haves:
  truths:
    - "SEC-01: app-default product surfaces cannot add hidden retained full-runtime calls when local owners exist."
    - "D-06, D-07, D-08, D-09: product/default slices are guarded while explicit chat, debug/export, CLI, legacy, and retained-runtime surfaces remain classified."
  artifacts:
    - path: tests/app-default-runtime-guard.test.mjs
      provides: "Slice-aware static guard for app-default runtime references."
  key_links:
    - from: tests/app-default-runtime-guard.test.mjs
      to: "apps/web/src, src/cli product routes, apps/desktop/main.mjs"
      via: "Comment-stripped source scans and route-slice assertions."
---

<objective>
Create the SEC-01 product/default runtime guard so hidden retained full-runtime calls cannot re-enter app buttons or Electron product wiring.

Purpose: enforce D-06 through D-09 with a slice-aware guard instead of a repo-wide ban.
Output: a focused static test that scans exact app-default files and classifies retained runtime, chat, legacy/static, CLI, debug/export, and test references.
</objective>

## Phase Goal

**As a** CareerRat pilot user, **I want to** use the desktop app without hidden broad skill-tool execution, **so that** pilot use is local-first, safer, and release-ready.

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/APP-PRODUCT-PLAN.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-runtime-lockdown-and-desktop-release/11-CONTEXT.md
@.planning/phases/11-runtime-lockdown-and-desktop-release/11-RESEARCH.md
@.planning/phases/11-runtime-lockdown-and-desktop-release/11-VALIDATION.md
@.planning/phases/11-runtime-lockdown-and-desktop-release/11-PATTERNS.md
@AGENTS.md
@docs/ARCHITECTURE.md
@tests/db-app-shell-regression.test.mjs
@tests/quick-onboarding-auto-sourcing-regression.test.mjs
@tests/company-discovery-regression.test.mjs
@src/cli/discovery-route.mjs
@src/cli/tracker-dev.mjs
@src/cli/skill-run-route.mjs
@src/cli/chat-route.mjs
@apps/web/src/lib/api.js
@apps/desktop/main.mjs
</context>

## Artifacts This Phase Produces

- `tests/app-default-runtime-guard.test.mjs`: new SEC-01 guard.
- Test-local constants such as `APP_DEFAULT_FILES`, `MIXED_ROUTE_SLICES`, and `CLASSIFIED_RETAINED_RUNTIME_FILES` if those names fit the local style.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the app-default runtime guard for SEC-01</name>
  <files>tests/app-default-runtime-guard.test.mjs</files>
  <read_first>
    @.planning/phases/11-runtime-lockdown-and-desktop-release/11-CONTEXT.md
    @.planning/phases/11-runtime-lockdown-and-desktop-release/11-PATTERNS.md
    @tests/db-app-shell-regression.test.mjs
    @tests/quick-onboarding-auto-sourcing-regression.test.mjs
    @tests/company-discovery-regression.test.mjs
    @src/cli/discovery-route.mjs
    @src/cli/tracker-dev.mjs
    @apps/web/src/lib/api.js
    @apps/desktop/main.mjs
  </read_first>
  <behavior>
    - Product/default files from D-06 fail if they contain an unclassified retained full-runtime seam.
    - Mixed route modules are scanned by named slices so explicit handoff routes remain allowed per D-08 and D-09.
    - The guard strips JavaScript comments before matching, following the Phase 6 pattern.
    - The guard does not become a repo-wide ban; retained runtime owner files and tests remain classified.
  </behavior>
  <action>Create `tests/app-default-runtime-guard.test.mjs` using `node:test` and the full comment-stripping state machine from `tests/db-app-shell-regression.test.mjs`. Define exact product/default file lists for React `/app` surfaces, onboarding/search/deep-ingest/packet/evaluate/apply surfaces, product HTTP routes, and Electron product wiring per D-06. Define explicit retained-runtime classifications for `src/cli/skill-run-route.mjs`, `src/cli/chat-route.mjs`, `src/core/ai/skill-runtime.mjs`, `src/core/ai/chat-runtime.mjs`, `src/cli/tracker-dev.mjs` compatibility/debug/export mounts, legacy/static byte pages, CLI commands, and tests per D-08. For mixed files such as `src/cli/discovery-route.mjs`, assert local proposal slices are clean while explicit quick-start/next chat handoff slices are classified per D-07 and D-09.</action>
  <acceptance_criteria>
    - The new test names the product/default file set and retained-runtime classification set in source.
    - The route-slice assertions fail with path-specific messages when an app-default slice adds retained-runtime dispatch.
    - Explicit chat handoff and retained-runtime owner files are allowed only through named classification, not through a blanket exception.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/app-default-runtime-guard.test.mjs tests/db-app-shell-regression.test.mjs tests/quick-onboarding-auto-sourcing-regression.test.mjs</automated>
  </verify>
  <done>SEC-01 is guarded by a slice-aware static test that preserves D-06, D-07, D-08, and D-09 without blocking legitimate retained-runtime surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App UI -> local HTTP routes | User clicks and app requests must stay on local API, DB verb, bounded AI, or explicit chat handoff paths. |
| Product source -> retained runtime | Source-level references to retained full skill runtime can bypass app-local owners if unclassified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Elevation of Privilege | App-default product files | high | mitigate | Add the SEC-01 static guard in this plan so D-06 and D-07 product/default slices fail on hidden retained-runtime dispatch. |
| T-11-02 | Repudiation | Mixed route modules | medium | mitigate | Require route-slice labels and path-specific assertions so explicit handoffs are auditable per D-08 and D-09. |
| T-11-SC | Tampering | npm installs/upgrades | high | mitigate | No package install or upgrade is planned; if execution introduces one, stop for the Package Legitimacy Audit checkpoint from 11-RESEARCH.md. |
</threat_model>

<verification>
Run `node --test tests/app-default-runtime-guard.test.mjs tests/db-app-shell-regression.test.mjs tests/quick-onboarding-auto-sourcing-regression.test.mjs`.
</verification>

<success_criteria>
- SEC-01 guard exists and passes.
- D-06 through D-09 are enforced by exact product/default and retained-runtime classifications.
- No production source is changed by this plan.
</success_criteria>

<output>
Create `.planning/phases/11-runtime-lockdown-and-desktop-release/11-01-SUMMARY.md` when done.
</output>
