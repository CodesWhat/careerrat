---
phase: 11-runtime-lockdown-and-desktop-release
plan: "05"
type: execute
wave: 2
depends_on: ["11-04"]
files_modified:
  - apps/desktop/electron-builder.yml
  - apps/desktop/package.json
  - apps/desktop/build/entitlements.mac.plist
  - apps/desktop/build/entitlements.mac.inherit.plist
  - tests/desktop-package-resources.test.mjs
autonomous: false
requirements: [DESK-01]
requirements_addressed: [DESK-01]
user_setup:
  - service: apple-developer
    why: "Developer ID signing and notarization for the pilot macOS DMG per D-11 and D-12."
    env_vars:
      - name: APPLE_TEAM_ID
        source: "Apple Developer account membership details or CI secret"
      - name: APPLE_ID
        source: "Apple ID used for notarization if not using keychain profile"
      - name: APPLE_APP_SPECIFIC_PASSWORD
        source: "Apple ID app-specific password if not using keychain profile"
    dashboard_config:
      - task: "Create or verify a notarytool keychain profile such as rolester-notary"
        location: "Local macOS Keychain via xcrun notarytool store-credentials"
must_haves:
  truths:
    - "DESK-01: pilot macOS release config supports Developer ID signing, hardened runtime, notarization, stapling verification, and package privacy checks."
    - "D-11, D-12, D-13: notarized DMG is the pilot target, credentials stay outside source, and auto-update is documented as readiness unless actually implemented."
  artifacts:
    - path: apps/desktop/electron-builder.yml
      provides: "Signed/notarized macOS DMG release configuration."
    - path: apps/desktop/build/entitlements.mac.plist
      provides: "Hardened runtime entitlements for the app."
    - path: apps/desktop/build/entitlements.mac.inherit.plist
      provides: "Inherited hardened runtime entitlements for child processes."
  key_links:
    - from: apps/desktop/electron-builder.yml
      to: tests/desktop-package-resources.test.mjs
      via: "Static assertions for signing, notarization, entitlements, and staged resources."
---

<objective>
Wire the desktop pilot packaging path for a signed and notarized macOS DMG.

Purpose: satisfy D-11 and D-12 as real pilot requirements while keeping Apple credentials outside source and keeping D-13 update claims truthful.
Output: electron-builder signing/notarization config, entitlements files, package scripts, and package-resource tests.
</objective>

## Phase Goal

**As a** Rolester pilot user, **I want to** use the desktop app without hidden broad skill-tool execution, **so that** pilot use is local-first, safer, and release-ready.

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-runtime-lockdown-and-desktop-release/11-CONTEXT.md
@.planning/phases/11-runtime-lockdown-and-desktop-release/11-RESEARCH.md
@.planning/phases/11-runtime-lockdown-and-desktop-release/11-PATTERNS.md
@apps/desktop/electron-builder.yml
@apps/desktop/package.json
@apps/desktop/scripts/stage.mjs
@tests/desktop-package-resources.test.mjs
@tests/release-safety.test.mjs
</context>

## Artifacts This Phase Produces

- `apps/desktop/build/entitlements.mac.plist`
- `apps/desktop/build/entitlements.mac.inherit.plist`
- electron-builder macOS config for `forceCodeSigning`, entitlements, hardened runtime, and notarization.
- Desktop package script updates for pilot packaging or verification if needed.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin signed/notarized package configuration in tests</name>
  <files>tests/desktop-package-resources.test.mjs</files>
  <read_first>
    @apps/desktop/electron-builder.yml
    @apps/desktop/package.json
    @apps/desktop/scripts/stage.mjs
    @tests/desktop-package-resources.test.mjs
    @.planning/phases/11-runtime-lockdown-and-desktop-release/11-CONTEXT.md
    @.planning/phases/11-runtime-lockdown-and-desktop-release/11-RESEARCH.md
  </read_first>
  <behavior>
    - electron-builder config enables hardened runtime, force code signing, entitlements, and notarization for pilot builds.
    - Config does not contain Apple IDs, passwords, team secrets, or keychain profile secrets.
    - Staged resources still include app dist, hidden skill dirs, and SDK node_modules while excluding private candidate/workspace data.
    - Update readiness checks do not claim auto-update exists unless an implementation and metadata target exists.
  </behavior>
  <action>Extend `tests/desktop-package-resources.test.mjs` before config changes. Add static assertions for `forceCodeSigning`, `mac.entitlements`, `mac.entitlementsInherit`, real notarization enabled, no tracked Apple credential literals, existing staged runtime inclusions, and private-root exclusions. If adding a pilot script in `apps/desktop/package.json`, assert it builds the web app before staging and runs electron-builder after staging.</action>
  <acceptance_criteria>
    - RED fails on the current `mac.notarize: false` release config.
    - Tests remain static and do not contact Apple services.
    - No dependency upgrade or package install is introduced.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/desktop-package-resources.test.mjs tests/release-safety.test.mjs</automated>
  </verify>
  <done>Package tests pin D-11, D-12, and D-13 release configuration expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configure macOS pilot signing and notarization</name>
  <files>apps/desktop/electron-builder.yml, apps/desktop/package.json, apps/desktop/build/entitlements.mac.plist, apps/desktop/build/entitlements.mac.inherit.plist, tests/desktop-package-resources.test.mjs</files>
  <read_first>
    @apps/desktop/electron-builder.yml
    @apps/desktop/package.json
    @apps/desktop/scripts/stage.mjs
    @tests/desktop-package-resources.test.mjs
    @.planning/phases/11-runtime-lockdown-and-desktop-release/11-PATTERNS.md
  </read_first>
  <behavior>
    - macOS DMG packaging requires signing and notarization for pilot success.
    - Apple credentials are supplied through keychain or environment, never tracked files.
    - Entitlements are minimal and compatible with hardened runtime.
    - Existing local unsigned/dev flows remain development-only and are not described as pilot success.
  </behavior>
  <action>Update `apps/desktop/electron-builder.yml` to enable real macOS signing/notarization readiness with hardened runtime, `forceCodeSigning`, app and inherited entitlements paths, and notarization configured through electron-builder-supported external credentials or keychain profile. Add minimal `apps/desktop/build/entitlements.mac.plist` and `apps/desktop/build/entitlements.mac.inherit.plist`; do not store team IDs, Apple IDs, app-specific passwords, or keychain credentials in these files. Update `apps/desktop/package.json` only for credential-neutral pilot build or verification scripts if needed.</action>
  <acceptance_criteria>
    - `mac.notarize` is no longer disabled.
    - Entitlements files exist and contain no credentials.
    - `forceCodeSigning` is enabled for pilot packaging.
    - Desktop package-resource and release-safety tests pass.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/desktop-package-resources.test.mjs tests/release-safety.test.mjs</automated>
  </verify>
  <done>DESK-01 macOS packaging is configured for a signed and notarized pilot artifact without source-controlled Apple credentials.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Apple notarization credential readiness</name>
  <files>apps/desktop/electron-builder.yml, apps/desktop/package.json</files>
  <read_first>
    @.planning/phases/11-runtime-lockdown-and-desktop-release/11-RESEARCH.md
    @apps/desktop/README.md
    @docs/RELEASE.md
  </read_first>
  <action>After the automated config and static tests pass, verify that the local machine or CI has an Apple notarization credential path available. Prefer a keychain profile so credentials stay out of source. Do not commit generated DMGs, `.app` bundles, Apple credentials, or local build artifacts.</action>
  <acceptance_criteria>
    - A non-destructive notarytool credential check succeeds, or execution pauses with exact setup instructions.
    - No Apple credential value is written into tracked files.
    - The pilot success path remains signed and notarized DMG, not unsigned development packaging.
  </acceptance_criteria>
  <verify>
    <automated>xcrun notarytool history --keychain-profile rolester-notary --limit 1</automated>
    <human-check>Confirm that either `rolester-notary` exists or provide the keychain profile name/CI secret path to use for the final release verification.</human-check>
  </verify>
  <done>Apple notarization credentials are confirmed or the executor has a precise blocked setup item for the final rollup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build config -> Apple signing/notary service | Release credentials must be provided outside tracked source. |
| Staged package -> pilot user machine | Packaged resources must be signed, notarized, and free of private local workspace data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-12 | Tampering | macOS DMG artifact | high | mitigate | Configure Developer ID signing, hardened runtime, notarization, and stapling verification per D-11 and D-12. |
| T-11-13 | Information Disclosure | Apple notarization credentials | high | mitigate | Keep Apple credentials in keychain/CI env only and add static tests that reject credential literals in release config. |
| T-11-14 | Information Disclosure | Staged desktop resources | high | mitigate | Preserve package allowlist and release-safety checks excluding `candidate`, `workspace`, `.internal`, and `.rolester` data. |
| T-11-SC | Tampering | Electron/electron-builder upgrades | high | mitigate | No upgrades are planned; the 11-RESEARCH Package Legitimacy Audit requires a blocking checkpoint before changing SUS package versions. |
</threat_model>

<verification>
Run `node --test tests/desktop-package-resources.test.mjs tests/release-safety.test.mjs`. Then run the non-destructive notarytool keychain-profile check in the checkpoint.
</verification>

<success_criteria>
- electron-builder config is pilot-ready for signing and notarization without tracked credentials.
- Entitlements files exist and are covered by static tests.
- Release-safety checks still block private data in packaged resources.
</success_criteria>

<output>
Create `.planning/phases/11-runtime-lockdown-and-desktop-release/11-05-SUMMARY.md` when done.
</output>
