---
phase: 07-quick-onboarding-and-auto-sourcing
plan: 07
type: execute
wave: 3
depends_on:
  - 07-03
  - 07-06
files_modified:
  - config/targeting.schema.json
  - src/core/db/verbs/candidate.mjs
  - apps/web/src/lib/api.js
  - apps/web/src/onboarding/steps/FinishStep.jsx
  - apps/web/src/pages/SetupReadinessCard.jsx
autonomous: true
requirements:
  - ONB-01
  - RUN-02
user_setup: []
must_haves:
  truths:
    - "ONB-01: cadence/search posture is captured before or at first run, but missing comp/deep-ingest data does not block search_ready per D-01, D-02, and D-03."
    - "RUN-02: First search appears as a compact setup task, not chat or a nag, and keeps the user moving into deep onboarding per D-11 and D-13."
    - "RUN-02: UI exposes Not started, Running, Completed, and Failed with actionable failed copy per D-12."
  artifacts:
    - config/targeting.schema.json
    - apps/web/src/onboarding/steps/FinishStep.jsx
    - apps/web/src/pages/SetupReadinessCard.jsx
  key_links:
    - "FinishStep -> getSourcingRun/startFirstSearchRun API wrappers"
    - "Retry search button -> startFirstSearchRun() -> POST /api/sourcing/first-run/start"
    - "cadence controls -> saveCandidateFile('targeting', { search_preferences })"
    - "SetupReadinessCard -> first-search run status summary"
---

<objective>
Wire durable first-search state, cadence selection, and setup-task copy into onboarding UI.

Purpose: The user should be guided into the first deterministic search by default and then returned to deeper onboarding while durable status remains visible.
Output: Targeting cadence schema/default plus FinishStep and home setup-card UI.
</objective>

## Phase Goal

**As a** candidate, **I want to** start local job sourcing after the minimum onboarding facts are captured, **so that** I can keep adding deeper context while CareerRat starts finding roles.

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-CONTEXT.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-RESEARCH.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-UI-SPEC.md
@AGENTS.md
@config/targeting.schema.json
@src/core/db/verbs/candidate.mjs
@apps/web/src/lib/api.js
@apps/web/src/onboarding/steps/FinishStep.jsx
@apps/web/src/pages/SetupReadinessCard.jsx
@apps/web/src/onboarding/steps/FinishStep.test.jsx
@apps/web/src/pages/SetupReadinessCard.test.jsx
@tests/candidate-setup.test.mjs
</context>

## Artifacts This Phase Produces

- Dependency: `mammoth@1.12.0`.
- DOCX intake: `src/core/onboarding/resume-docx.mjs`, `extractDocxResumeText()`, `looksLikeUsableResumeText()`, `POST /api/onboard/resume-docx`, `extractResumeDocx(file)`.
- Sourcing run state: `src/core/db/migrations/007-sourcing-runs.mjs`, `sourcing_runs`, `src/core/db/verbs/sourcing-runs.mjs`.
- Sourcing orchestration: `src/core/onboarding/first-search-run.mjs`, `mountSourcingRoutes()`, `GET /api/sourcing/runs/latest`, `POST /api/sourcing/first-run/start`, `POST /api/sourcing/search/start`.
- UI: first-search setup task statuses `Not started`, `Running`, `Completed`, `Failed`; cadence choices `Daily`, `Every 3 days`, `Weekly`, `Manual only`; Jobs page `Search jobs` action.
- Targeting field: `targeting.search_preferences.cadence`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cadence preference schema/default and API wrappers</name>
  <files>config/targeting.schema.json, src/core/db/verbs/candidate.mjs, apps/web/src/lib/api.js</files>
  <read_first>
    - config/targeting.schema.json
    - src/core/db/verbs/candidate.mjs
    - apps/web/src/lib/api.js
    - tests/candidate-setup.test.mjs
  </read_first>
  <behavior>
    - `targeting.search_preferences.cadence.mode` accepts `daily`, `every-3-days`, `weekly`, or `manual`.
    - Cadence can be saved without making comp required for `search_ready`.
    - API wrappers read and start durable sourcing runs.
  </behavior>
  <action>Add `search_preferences.cadence` to `config/targeting.schema.json` with `mode` enum `daily`, `every-3-days`, `weekly`, `manual`, optional `recommended_from` enum `default` or `history`, and optional `saved_at`. Update `DEFAULTS.targeting.search_preferences` in `src/core/db/verbs/candidate.mjs` to include `{ cadence: { mode: "daily", recommended_from: "default" } }` while preserving existing `posting_age`. Do not add compensation to `search_ready`. Add `getSourcingRun({ purpose })`, `startFirstSearchRun(payload)`, and `startSearchRun(payload)` wrappers to `apps/web/src/lib/api.js`, using the existing `ApiError` contract and `/api/sourcing/*` routes per D-01, D-02, D-03, D-05, and D-07.</action>
  <acceptance_criteria>
    - Targeting schema still rejects unknown `search_preferences` keys.
    - Candidate setup tests prove comp remains outside `search_ready`.
    - API wrappers do not call discovery, chat, or skill-run endpoints.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/candidate-setup.test.mjs</automated>
  </verify>
  <done>Cadence preference is schema-valid, defaulted, and accessible to React without widening readiness gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace FinishStep discovery quick start with first-search task</name>
  <files>apps/web/src/onboarding/steps/FinishStep.jsx, apps/web/src/pages/SetupReadinessCard.jsx</files>
  <read_first>
    - apps/web/src/onboarding/steps/FinishStep.jsx
    - apps/web/src/pages/SetupReadinessCard.jsx
    - apps/web/src/onboarding/steps/FinishStep.test.jsx
    - apps/web/src/pages/SetupReadinessCard.test.jsx
    - .planning/phases/07-quick-onboarding-and-auto-sourcing/07-UI-SPEC.md
  </read_first>
  <behavior>
    - Search-ready onboarding asks "Search now?" with the yes/default path selected.
    - Continuing onboarding starts the first deterministic search unless the user explicitly chooses `Not now`.
    - The selected cadence is saved to `targeting.search_preferences.cadence` before the run starts and is rendered from saved state after reload.
    - Status labels and copy match the UI-SPEC.
    - Failed-state `Retry search` calls the first-run start API route and renders the returned retry run.
  </behavior>
  <action>In `FinishStep.jsx`, remove first-search use of `startDiscoveryQuickStart`, `startDiscoveryNext`, `DiscoveryChatPanel`, and discovery skill guidance from the Quick start sourcing surface. Keep explicit deeper interview `/chat` link separate. Add pure helpers such as `buildCadenceOptions(state, runState)` and `buildFirstSearchTask({ state, run })`. Render cadence choices `Daily`, `Every 3 days`, `Weekly`, `Manual only`; label default recommendation as `Default recommendation - no local history yet` unless run/source history supports `Recommended from recent search history`. Initialize selection from `state.data.targeting.search_preferences.cadence.mode`, save changes or the default continue choice through `saveCandidateFile("targeting", { search_preferences: { ...existingSearchPreferences, cadence: { mode, recommended_from, saved_at } } })`, and render saved state as compact text such as `Cadence: Daily` or `Cadence: Every 3 days`. Ensure the normal continue/search action awaits cadence persistence before calling `startFirstSearchRun`; if `Not now` is selected, save the chosen cadence, leave status `Not started`, and show `Search jobs now`. Render the first-search setup task with labels `Not started`, `Running`, `Completed`, and `Failed`; use copy from `07-UI-SPEC.md`, including zero-result completed copy and failed retry copy. Wire the `Retry search` button in the Failed state to call `startFirstSearchRun({ retry: true })`, which posts to `/api/sourcing/first-run/start`; after a 202 response, render the returned running retry state instead of mutating local copy only per D-12. Update `SetupReadinessCard.jsx` to show compact first-search status context without a modal or nag per D-04, D-05, D-07, D-11, D-12, and D-13.</action>
  <acceptance_criteria>
    - FinishStep tests pass for all status labels and no chat/discovery/full skill strings in first-search UI.
    - FinishStep tests pass for choosing a non-default cadence, persisted `saveCandidateFile("targeting", ...)` payload, and saved cadence display after rerender.
    - Running state disables start buttons but keeps deeper onboarding links/actions visible.
    - Completed state shows source attempted and roles found counts; zero-result completed copy is truthful.
    - Failed state shows `Retry search`, actionable error copy, and clicking it calls `POST /api/sourcing/first-run/start` through the API wrapper.
  </acceptance_criteria>
  <verify>
    <automated>npm --workspace apps/web run test -- src/onboarding/steps/FinishStep.test.jsx src/pages/SetupReadinessCard.test.jsx</automated>
  </verify>
  <done>Onboarding UI passes first-search task tests and no longer uses chat/discovery handoffs for the first search.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React onboarding -> candidate targeting DB | User cadence choice updates local candidate targeting state. |
| React onboarding -> sourcing API | Search-ready UI starts local deterministic sourcing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Spoofing | First-search status UI | medium | mitigate | UI derives status from durable sourcing run state and tests cover reload-rendered states. |
| T-07-10 | Elevation of Privilege | First-search controls | high | mitigate | First-search UI calls `/api/sourcing/*` wrappers only; tests forbid chat/discovery/skill strings. |
| T-07-11 | Tampering | Readiness/cadence schema | medium | mitigate | Schema/default update validates cadence without changing `search_ready` criteria. |
| T-07-SC | Tampering | npm installs | high | mitigate | This plan installs nothing; approved `mammoth` installation is isolated to Plan 07-04 with package audit evidence. |
</threat_model>

<verification>
- `node --test tests/candidate-setup.test.mjs`
- `npm --workspace apps/web run test -- src/onboarding/steps/FinishStep.test.jsx src/pages/SetupReadinessCard.test.jsx`
</verification>

<success_criteria>
- The onboarding path starts deterministic sourcing by default once search-ready unless the user chooses `Not now`.
- Failed first-search actionability is implemented as a route-backed retry action, not just UI copy.
- Cadence is visible and saved as preference, with no hidden scheduler implication.
- Search, Gate, Apply, and Deep ingest readiness remain separate.
</success_criteria>

<output>
Create `.planning/phases/07-quick-onboarding-and-auto-sourcing/07-07-SUMMARY.md` when done.
</output>
