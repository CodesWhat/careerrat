---
phase: 07-quick-onboarding-and-auto-sourcing
plan: 05
type: execute
wave: 1
depends_on:
  - 07-02
files_modified:
  - src/core/db/migrations/007-sourcing-runs.mjs
  - src/core/db/migrations.mjs
  - src/core/db/verbs/sourcing-runs.mjs
  - src/core/db/verbs/index.mjs
autonomous: true
requirements:
  - RUN-01
  - RUN-02
user_setup: []
must_haves:
  truths:
    - "RUN-01: sourcing_runs persists first-search idempotency and duplicate-run protection per D-04 and D-05."
    - "RUN-02: failed first-search rows are durable but retryable through sourcingRunStart after source setup is fixed per D-12."
    - "RUN-02: run rows retain status, summary, error, startedAt, completedAt, and updatedAt for React reloads per D-11 through D-13."
  artifacts:
    - src/core/db/migrations/007-sourcing-runs.mjs
    - src/core/db/verbs/sourcing-runs.mjs
  key_links:
    - "migration007 -> ALL_MIGRATIONS -> sourcing run verbs"
    - "sourcingRunStart -> sourcingRunComplete/sourcingRunFail"
    - "failed first-search row -> sourcingRunStart retryFailed creates a new running row"
---

<objective>
Implement durable SQLite sourcing run state and DB verbs.

Purpose: First-search and manual search runs need reload-safe state rather than module-local flags.
Output: Migration 007 plus run-state verbs exported from the DB verb barrel.
</objective>

## Phase Goal

**As a** candidate, **I want to** start local job sourcing after the minimum onboarding facts are captured, **so that** I can keep adding deeper context while CareerRat starts finding roles.

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-CONTEXT.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-RESEARCH.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-PATTERNS.md
@AGENTS.md
@src/core/db/migrations.mjs
@src/core/db/migrations/006-company-discovery-cache.mjs
@src/core/db/verbs/company-discovery.mjs
@src/core/db/verbs/source-config.mjs
@src/core/db/verbs/index.mjs
@tests/db-migrations.test.mjs
@tests/sourcing-runs.test.mjs
</context>

## Artifacts This Phase Produces

- Dependency: `mammoth@1.12.0`.
- DOCX intake: `src/core/onboarding/resume-docx.mjs`, `extractDocxResumeText()`, `looksLikeUsableResumeText()`, `POST /api/onboard/resume-docx`, `extractResumeDocx(file)`.
- Sourcing run state: `src/core/db/migrations/007-sourcing-runs.mjs`, `sourcing_runs`, `src/core/db/verbs/sourcing-runs.mjs`.
- Sourcing orchestration: `src/core/onboarding/first-search-run.mjs`, `mountSourcingRoutes()`, `GET /api/sourcing/runs/latest`, `POST /api/sourcing/first-run/start`, `POST /api/sourcing/search/start`.
- UI: first-search setup task statuses `Not started`, `Running`, `Completed`, `Failed`; cadence choices `Daily`, `Every 3 days`, `Weekly`, `Manual only`; Jobs page `Search jobs` action.
- Targeting field: `targeting.search_preferences.cadence`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sourcing_runs migration</name>
  <files>src/core/db/migrations/007-sourcing-runs.mjs, src/core/db/migrations.mjs</files>
  <read_first>
    - src/core/db/migrations.mjs
    - src/core/db/migrations/006-company-discovery-cache.mjs
    - tests/db-migrations.test.mjs
  </read_first>
  <behavior>
    - Migration 007 applies after migration 006 and is re-runnable through `runMigrations`.
    - `sourcing_runs` stores JSON data with generated lookup columns and indexes.
  </behavior>
  <action>Create `migration007` with `id: 7`, `name: "sourcing-runs"`, and an `up(db)` that creates `sourcing_runs`. Store canonical run data as JSON with `CHECK (json_valid(data))`; add generated columns for `purpose`, `status`, `started_at`, `completed_at`, and `updated_at`; add indexes for latest run by purpose and running-status lookup. Import and append `migration007` in `src/core/db/migrations.mjs` after `migration006`.</action>
  <acceptance_criteria>
    - `ALL_MIGRATIONS.at(-1).id === 7`.
    - Migration tests prove `_migrations` logs id 7 and re-run is a no-op.
    - Table and generated-column names match the Wave 0 tests exactly.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/db-migrations.test.mjs</automated>
  </verify>
  <done>Migration 007 is registered and passes migration tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add sourcing run verbs</name>
  <files>src/core/db/verbs/sourcing-runs.mjs, src/core/db/verbs/index.mjs</files>
  <read_first>
    - src/core/db/verbs/company-discovery.mjs
    - src/core/db/verbs/source-config.mjs
    - src/core/db/verbs/index.mjs
    - tests/sourcing-runs.test.mjs
  </read_first>
  <behavior>
    - `sourcingRunLatest` returns a status object for the latest purpose or `not_started` when absent.
    - `sourcingRunStart` creates or reuses runs idempotently by purpose.
    - `sourcingRunStart` can retry a failed first-search by creating new work instead of reusing the failed row.
    - `sourcingRunComplete` and `sourcingRunFail` persist summary/error JSON and terminal timestamps.
  </behavior>
  <action>Create `src/core/db/verbs/sourcing-runs.mjs` following the DB verb patterns from `company-discovery.mjs`: `requireDb`, `withTransaction`, cloning through JSON, stable `BAD_REQUEST`, `NOT_FOUND`, and `CONFLICT` codes. Export `SOURCING_RUN_STATUSES`, `sourcingRunLatest({ repoRoot, env, purpose })`, `sourcingRunStart({ repoRoot, env, purpose, id, metadata, retryFailed })`, `sourcingRunComplete({ repoRoot, env, id, summary })`, and `sourcingRunFail({ repoRoot, env, id, error })`. Use purposes `first-search` and `manual-search`; map stored snake_case fields to route-facing camelCase fields. Preserve idempotency for running and completed `first-search` rows, but when the latest `first-search` row is `failed` and `retryFailed:true` is provided, insert a new `running` row with `reused:false`, a fresh id, and metadata linking `retryOf` to the failed run id so `POST /api/sourcing/first-run/start` can return 202 for new retry work after source setup is fixed per D-12. Re-export these functions from `src/core/db/verbs/index.mjs`.</action>
  <acceptance_criteria>
    - Verbs use prepared statements and never interpolate caller-supplied values into SQL.
    - Starting a running first-search run returns the existing run with `reused:true`.
    - Starting a completed first-search run returns the completed run with `reused:true` unless caller uses `purpose:"manual-search"`.
    - Starting a failed first-search run with `retryFailed:true` creates a new running row with `reused:false`; without the retry flag it returns the failed row for display.
    - Summary and error objects are persisted as JSON and returned through `sourcingRunLatest`.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/sourcing-runs.test.mjs</automated>
  </verify>
  <done>Sourcing run verbs pass the Wave 0 DB state tests and are exported through the DB verb barrel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route/service -> SQLite | Local API actions write durable run state. |
| JSON run summary -> React | Scanner summaries and errors become user-visible status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | `sourcing_runs` writes | high | mitigate | Use prepared statements, JSON validity checks, controlled status enum, and transaction-wrapped updates. |
| T-07-06 | Repudiation | Duplicate/idempotent starts | medium | mitigate | DB verbs enforce purpose-based reuse for first-search and preserve timestamps plus summaries/errors. |
| T-07-12 | Information Disclosure | Run error payloads | medium | mitigate | Store concise error message/code only; do not persist raw JD bodies, resumes, prompts, or private compensation in run rows. |
| T-07-SC | Tampering | npm installs | high | mitigate | This plan installs nothing; approved `mammoth` installation is isolated to Plan 07-04 with package audit evidence. |
</threat_model>

<verification>
- `node --test tests/db-migrations.test.mjs tests/sourcing-runs.test.mjs`
</verification>

<success_criteria>
- Durable run records are available to later route and React plans.
- RUN-01/RUN-02 DB state requirements pass without adding external queue dependencies.
- D-12 failed first-search actionability is backed by a DB transition that creates new retry work instead of only changing UI copy.
</success_criteria>

<output>
Create `.planning/phases/07-quick-onboarding-and-auto-sourcing/07-05-SUMMARY.md` when done.
</output>
