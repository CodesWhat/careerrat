---
phase: 07-quick-onboarding-and-auto-sourcing
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/db-migrations.test.mjs
  - tests/sourcing-runs.test.mjs
  - tests/sourcing-route.test.mjs
  - tests/search-route.test.mjs
autonomous: true
requirements:
  - RUN-01
  - RUN-02
user_setup: []
must_haves:
  truths:
    - "RUN-01: A DB-backed first sourcing run starts idempotently when search_ready first becomes true per D-04 and D-05."
    - "RUN-02: Durable run state survives reloads and exposes Not started, Running, Completed, and Failed mappings per D-11, D-12, D-13."
    - "RUN-01: First search uses deterministic unauthenticated DB sources only, with no chat or skill runtime per D-08, D-09, and D-10."
  artifacts:
    - tests/db-migrations.test.mjs
    - tests/sourcing-runs.test.mjs
    - tests/sourcing-route.test.mjs
    - tests/search-route.test.mjs
  key_links:
    - "sourcing_runs migration -> sourcing run DB verbs"
    - "POST /api/sourcing/first-run/start -> runSourcedScan({ write:true })"
    - "route tests -> no chat/runtime/static generated-file first-search path"
---

<objective>
Create the RED contracts for durable DB-backed sourcing run state and deterministic first-search routing.

Purpose: React must be able to show first-search progress after reloads, and the first run must not launch hidden chat, skill sessions, browser sources, or compatibility-file readiness.
Output: Failing migration, DB verb, route, and deterministic-source tests.
</objective>

## Phase Goal

**As a** candidate, **I want to** start local job sourcing after the minimum onboarding facts are captured, **so that** I can keep adding deeper context while CareerRat starts finding roles.

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-CONTEXT.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-RESEARCH.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-VALIDATION.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-PATTERNS.md
@.planning/phases/06-canonical-db-app-shell/06-CONTEXT.md
@AGENTS.md
@src/core/db/migrations.mjs
@src/core/db/migrations/006-company-discovery-cache.mjs
@src/core/db/verbs/company-discovery.mjs
@src/core/db/verbs/source-config.mjs
@src/cli/search-route.mjs
@scripts/scan-sourced.mjs
@tests/db-migrations.test.mjs
@tests/search-route.test.mjs
</context>

## Artifacts This Phase Produces

- Dependency: `mammoth@1.12.0`.
- DOCX intake: `src/core/onboarding/resume-docx.mjs`, `extractDocxResumeText()`, `looksLikeUsableResumeText()`, `POST /api/onboard/resume-docx`, `extractResumeDocx(file)`.
- Sourcing run state: `src/core/db/migrations/007-sourcing-runs.mjs`, `sourcing_runs`, `src/core/db/verbs/sourcing-runs.mjs`.
- Sourcing orchestration: `src/core/onboarding/first-search-run.mjs`, `mountSourcingRoutes()`, `GET /api/sourcing/runs/latest`, `POST /api/sourcing/first-run/start`, `POST /api/sourcing/search/start`.
- UI: first-search setup task statuses `Not started`, `Running`, `Completed`, `Failed`; cadence choices `Daily`, `Every 3 days`, `Weekly`, `Manual only`; Jobs page `Search jobs` action.
- Targeting field: `targeting.search_preferences.cadence`.

<tasks>

<task type="auto">
  <name>Task 1: Add RED migration and run-state verb tests</name>
  <files>tests/db-migrations.test.mjs, tests/sourcing-runs.test.mjs</files>
  <read_first>
    - tests/db-migrations.test.mjs
    - tests/db-source-config.test.mjs
    - src/core/db/migrations.mjs
    - src/core/db/migrations/006-company-discovery-cache.mjs
    - src/core/db/verbs/company-discovery.mjs
  </read_first>
  <action>Update migration tests to require migration id 7 named `sourcing-runs` and a `sourcing_runs` table with JSON `data`, generated columns for `purpose`, `status`, `started_at`, `completed_at`, and `updated_at`, plus latest-purpose and running-status indexes. Create `tests/sourcing-runs.test.mjs` for `sourcingRunLatest`, `sourcingRunStart`, `sourcingRunComplete`, `sourcingRunFail`, and idempotent duplicate starts. Assert summaries and errors persist as JSON and status transitions reject invalid duplicate running starts per RUN-01, RUN-02, D-04, D-05, D-11, and D-12.</action>
  <acceptance_criteria>
    - `tests/sourcing-runs.test.mjs` uses temp DB workspace patterns from source-config tests.
    - Tests assert latest first-search lookup returns `not_started` when no run exists or returns the latest stored run if one exists.
    - Tests assert starting a completed first-search run reuses the completed run unless forced by manual-search purpose.
    - RED command fails because migration 7 and sourcing-run verbs do not exist yet.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'node --test tests/db-migrations.test.mjs tests/sourcing-runs.test.mjs; test $? -ne 0'</automated>
  </verify>
  <done>Run-state migration and verb tests are present and intentionally RED.</done>
</task>

<task type="auto">
  <name>Task 2: Add RED first-search route tests</name>
  <files>tests/sourcing-route.test.mjs, tests/search-route.test.mjs</files>
  <read_first>
    - tests/search-route.test.mjs
    - tests/onboard-route.test.mjs
    - src/cli/search-route.mjs
    - scripts/scan-sourced.mjs
    - src/core/db/verbs/source-config.mjs
  </read_first>
  <action>Create `tests/sourcing-route.test.mjs` with a minimal `addRoute` harness for `mountSourcingRoutes`. Cover `GET /api/sourcing/runs/latest?purpose=first-search`, `POST /api/sourcing/first-run/start`, and `POST /api/sourcing/search/start`. Use injected `fetchImpl` and seeded DB source config. Assert first-run start returns 202 for a new background run, 200 with `reused:true` for running/completed first runs, and 409 when candidate setup is not `search_ready`. Assert a run with zero deterministic sources becomes `failed` with actionable source setup error. Add `tests/search-route.test.mjs` coverage for deterministic source counts: enabled RSS and supported ATS companies count as attempted; browser/auth/url-query-only sources do not count per D-08, D-09, and D-10.</action>
  <acceptance_criteria>
    - Route tests assert response bodies never contain `chat`, `chatId`, `nextSkill`, `research-boards`, `discover-companies`, `search-jobs`, `/api/chat`, or `/api/skill/run`.
    - Tests assert source setup is read from SQLite `sourceConfigGet`, not `config/search-sources.yml`.
    - Tests assert first-run route uses stubbed public ATS/RSS fetches and never browser/session helpers.
    - RED command fails because `mountSourcingRoutes` and first-run endpoints do not exist yet.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'node --test tests/sourcing-route.test.mjs tests/search-route.test.mjs; test $? -ne 0'</automated>
  </verify>
  <done>First-search route tests are present and intentionally RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React/API -> sourcing run DB row | User actions start durable local run records. |
| Sourcing route -> public ATS/RSS scanner | First search fetches public deterministic sources. |
| Source config -> scanner | DB source config decides what can be scanned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | `sourcing_runs` DB writes | high | mitigate | RED tests require prepared-statement DB verbs, JSON validity, and controlled state transitions. |
| T-07-06 | Repudiation | Duplicate first-run starts | medium | mitigate | RED tests require idempotent `first-search` start and persisted summary/error data. |
| T-07-07 | Elevation of Privilege | Hidden browser/auth sourcing | high | mitigate | RED tests assert automatic first run uses only deterministic unauthenticated DB sources and no chat/runtime/browser path. |
| T-07-08 | Tampering | Compatibility source readiness | high | mitigate | RED tests assert SQLite source config is the product readiness path. |
| T-07-SC | Tampering | npm installs | high | mitigate | This plan installs nothing; approved `mammoth` installation is isolated to Plan 07-04 with package audit evidence. |
</threat_model>

<verification>
Wave 0 RED checks:
- `bash -lc 'node --test tests/db-migrations.test.mjs tests/sourcing-runs.test.mjs; test $? -ne 0'`
- `bash -lc 'node --test tests/sourcing-route.test.mjs tests/search-route.test.mjs; test $? -ne 0'`
</verification>

<success_criteria>
- RUN-01 and RUN-02 have failing DB and route coverage before implementation.
- First-search tests forbid hidden chat, full skill runtime, browser/auth sources, and compatibility-file readiness.
</success_criteria>

<output>
Create `.planning/phases/07-quick-onboarding-and-auto-sourcing/07-02-SUMMARY.md` when done.
</output>
