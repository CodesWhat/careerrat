---
phase: 07-quick-onboarding-and-auto-sourcing
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/src/onboarding/steps/FinishStep.test.jsx
  - apps/web/src/pages/SetupReadinessCard.test.jsx
  - apps/web/src/jobs/JobsPage.test.jsx
  - tests/candidate-setup.test.mjs
autonomous: true
requirements:
  - ONB-02
  - ONB-01
  - RUN-02
user_setup: []
must_haves:
  truths:
    - "ONB-01: search_ready stays earlier than gate_ready/apply_ready and does not require compensation per D-01, D-02, and D-03."
    - "ONB-01: selected search cadence is persisted to `targeting.search_preferences.cadence` and remains visible after save per D-07."
    - "ONB-02: candidate setup defaults keep PDF as the packet format while allowing DOCX as a board-required export need."
    - "RUN-02: Onboarding shows first search as a setup task with Not started, Running, Completed, and Failed states per D-11, D-12, and D-13."
    - "RUN-02: Jobs page shows manual Search jobs only after DB source setup exists per D-06."
  artifacts:
    - apps/web/src/onboarding/steps/FinishStep.test.jsx
    - apps/web/src/pages/SetupReadinessCard.test.jsx
    - apps/web/src/jobs/JobsPage.test.jsx
    - tests/candidate-setup.test.mjs
  key_links:
    - "candidate setup tests -> src/core/db/verbs/candidate.mjs readiness"
    - "candidate setup tests -> config/form-defaults.schema.json document_formats"
    - "FinishStep tests -> first-search task and cadence UI"
    - "FinishStep failed-state tests -> Retry search calls startFirstSearchRun() and POST /api/sourcing/first-run/start"
    - "JobsPage tests -> /api/search/sources DB readiness and /api/sourcing/search/start"
---

<objective>
Create RED UI, readiness, and candidate setup contracts for first-search task display, cadence selection, document format defaults, and Jobs-page reruns.

Purpose: The user must see sourcing as a compact setup task and keep moving into deeper onboarding while durable run state updates.
Output: Failing React tests plus candidate setup regressions for staged search/gate/apply readiness and document format defaults.
</objective>

## Phase Goal

**As a** candidate, **I want to** start local job sourcing after the minimum onboarding facts are captured, **so that** I can keep adding deeper context while Rolester starts finding roles.

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-CONTEXT.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-RESEARCH.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-VALIDATION.md
@.planning/phases/07-quick-onboarding-and-auto-sourcing/07-UI-SPEC.md
@config/form-defaults.schema.json
@src/core/db/verbs/candidate.mjs
@apps/web/src/onboarding/steps/FinishStep.jsx
@apps/web/src/pages/SetupReadinessCard.jsx
@apps/web/src/jobs/JobsPage.jsx
@apps/web/src/lib/api.js
</context>

## Artifacts This Phase Produces

- Dependency: `mammoth@1.12.0`.
- DOCX intake: `src/core/onboarding/resume-docx.mjs`, `extractDocxResumeText()`, `looksLikeUsableResumeText()`, `POST /api/onboard/resume-docx`, `extractResumeDocx(file)`.
- Sourcing run state: `src/core/db/migrations/007-sourcing-runs.mjs`, `sourcing_runs`, `src/core/db/verbs/sourcing-runs.mjs`.
- Sourcing orchestration: `src/core/onboarding/first-search-run.mjs`, `mountSourcingRoutes()`, `GET /api/sourcing/runs/latest`, `POST /api/sourcing/first-run/start`, `POST /api/sourcing/search/start`.
- UI: first-search setup task statuses `Not started`, `Running`, `Completed`, `Failed`; cadence choices `Daily`, `Every 3 days`, `Weekly`, `Manual only`; Jobs page `Search jobs` action.
- Targeting field: `targeting.search_preferences.cadence`.
- Form-defaults field: `form-defaults.document_formats` with PDF default and optional DOCX board-required export need.

<tasks>

<task type="auto">
  <name>Task 1: Add staged readiness and first-search onboarding UI tests</name>
  <files>tests/candidate-setup.test.mjs, apps/web/src/onboarding/steps/FinishStep.test.jsx, apps/web/src/pages/SetupReadinessCard.test.jsx</files>
  <read_first>
    - tests/candidate-setup.test.mjs
    - apps/web/src/onboarding/steps/FinishStep.test.jsx
    - apps/web/src/pages/SetupReadinessCard.test.jsx
    - src/core/db/verbs/candidate.mjs
    - apps/web/src/onboarding/steps/FinishStep.jsx
    - apps/web/src/pages/SetupReadinessCard.jsx
  </read_first>
  <action>Add or update tests so `search_ready` is true with source resume, target role/title, and remote/location posture even when `compensation.minimum_base` is absent, while `gate_ready` and `apply_ready` remain false per D-01, D-02, and D-03. In the same `tests/candidate-setup.test.mjs` edit, add document-format default tests that assert `form-defaults.document_formats.default_packet_format` defaults to `pdf`, schema validation allows `required_export_formats: ["docx"]`, and unknown formats are rejected per ONB-02. Add `FinishStep` tests for cadence choices `Daily`, `Every 3 days`, `Weekly`, `Manual only`; default recommendation copy; "Search now?" with yes/default behavior; first-search task states `Not started`, `Running`, `Completed`, and `Failed`; and continued deep onboarding links while running per D-04, D-05, D-07, D-11, D-12, and D-13. The cadence tests must simulate choosing a non-default mode, assert `saveCandidateFile("targeting", { search_preferences: { cadence: { mode: ... } } })` is called before starting the first search, and assert saved state re-renders as compact text such as `Cadence: Every 3 days`. The failed-state test must click `Retry search`, assert the UI calls `startFirstSearchRun()` rather than only changing local text, and assert the wrapper targets `POST /api/sourcing/first-run/start` so a previously failed first-search can start new retry work after source setup is fixed per D-12. Update `SetupReadinessCard` tests to show first-search status as checklist context, not a nag.</action>
  <acceptance_criteria>
    - Readiness test proves compensation is absent and `search_ready` remains true while stricter readiness rows stay locked.
    - Candidate setup tests fail until `config/form-defaults.schema.json` and `DEFAULTS["form-defaults"]` include `document_formats`.
    - FinishStep tests assert no first-search UI output contains `chat`, `skill`, `research-boards`, `discover-companies`, or `search-jobs`.
    - FinishStep tests assert cadence persistence and saved-state display, not only option rendering.
    - Running state test asserts `Searching deterministic public sources...` and deep onboarding links/actions remain visible.
    - Failed state test asserts `Retry search` and actionable error copy.
    - Failed state test asserts clicking `Retry search` calls the first-run start wrapper and updates to the returned 202 retry run state.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'node --test tests/candidate-setup.test.mjs; node_status=$?; npm --workspace apps/web run test -- src/onboarding/steps/FinishStep.test.jsx src/pages/SetupReadinessCard.test.jsx; web_status=$?; test $node_status -ne 0 -o $web_status -ne 0'</automated>
  </verify>
  <done>Staged readiness and onboarding first-search UI tests are present; new UI expectations are intentionally RED.</done>
</task>

<task type="auto">
  <name>Task 2: Add RED JobsPage search action tests</name>
  <files>apps/web/src/jobs/JobsPage.test.jsx</files>
  <read_first>
    - apps/web/src/jobs/JobsPage.jsx
    - apps/web/src/pages/SetupReadinessCard.test.jsx
    - apps/web/src/app-shell/DashboardContext.jsx
    - apps/web/src/lib/api.js
  </read_first>
  <action>Create `JobsPage.test.jsx` with Vitest and `renderToStaticMarkup`. Mock dashboard data and API wrappers. Assert the `Search jobs` header action is hidden or disabled when `/api/search/sources` reports no DB source setup, enabled when DB source setup exists, changes to `Searching...` while a run is active, and calls `startSearchRun()` rather than chat/discovery/full skill wrappers per D-06, D-08, D-09, and D-10.</action>
  <acceptance_criteria>
    - The test renders `PageScaffold` actions for source-ready state.
    - The no-source state shows `Finish Search setup before running a job search.`
    - The test fails against current `JobsPage.jsx` because no header search action exists.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'npm --workspace apps/web run test -- src/jobs/JobsPage.test.jsx; test $? -ne 0'</automated>
  </verify>
  <done>JobsPage manual search action tests are present and intentionally RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB readiness -> React setup UI | Canonical setup state controls visible search/gate/apply actions. |
| React Jobs page -> sourcing API | Manual rerun action starts local deterministic search. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Spoofing | First-search UI state | medium | mitigate | RED tests require UI states to reflect durable run status rather than local-only flags. |
| T-07-10 | Elevation of Privilege | Jobs-page search action | high | mitigate | RED tests require manual reruns to call local sourcing routes only after DB source setup exists. |
| T-07-11 | Tampering | Readiness gate widening | high | mitigate | Readiness tests preserve `search_ready` as distinct from `gate_ready` and `apply_ready`. |
| T-07-SC | Tampering | npm installs | high | mitigate | This plan installs nothing; approved `mammoth` installation is isolated to Plan 07-04 with package audit evidence. |
</threat_model>

<verification>
Wave 0 RED checks:
- `bash -lc 'node --test tests/candidate-setup.test.mjs; node_status=$?; npm --workspace apps/web run test -- src/onboarding/steps/FinishStep.test.jsx src/pages/SetupReadinessCard.test.jsx; web_status=$?; test $node_status -ne 0 -o $web_status -ne 0'`
- `bash -lc 'npm --workspace apps/web run test -- src/jobs/JobsPage.test.jsx; test $? -ne 0'`
</verification>

<success_criteria>
- ONB-01 and RUN-02 UI/readiness expectations are captured before implementation.
- ONB-02 document-format default/schema expectations are captured in the single Wave 0 owner for `tests/candidate-setup.test.mjs`.
- No test asks for hidden chat, skill runtime, or authenticated/browser sources in first search.
</success_criteria>

<output>
Create `.planning/phases/07-quick-onboarding-and-auto-sourcing/07-03-SUMMARY.md` when done.
</output>
