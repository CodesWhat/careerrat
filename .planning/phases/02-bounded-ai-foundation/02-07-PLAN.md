---
phase: 02-bounded-ai-foundation
plan: "07"
type: test
wave: 5
depends_on:
  - 02-04
  - 02-05
  - 02-06
files_modified:
  - tests/bounded-ai.test.mjs
  - tests/call-ai.test.mjs
  - tests/ai-proxy.test.mjs
  - tests/assist-route.test.mjs
  - tests/onboard-route.test.mjs
autonomous: true
requirements:
  - AIR-01
  - AIR-04
requirements_addressed:
  - AIR-01
  - AIR-04
must_haves:
  truths:
    - "BYOK and proxy paths preserve comparable skill/action telemetry labels per D-09 and D-12."
    - "Bounded route and telemetry tests catch missing labels as regressions per D-10."
    - "Final route regressions prove resume-AI SDK/provider failure paths return status 502 with `code:\"AI_PROVIDER_FAILED\"` per D-08."
    - "Usage rows and route envelopes remain metadata-only and do not contain prompts, raw model output, resumes, JDs, candidate facts, or page bodies per D-11."
  artifacts:
    - path: "tests/bounded-ai.test.mjs"
      provides: "Helper-level label and no-leak regressions"
    - path: "tests/call-ai.test.mjs"
      provides: "BYOK/proxy label propagation regressions for native and non-native calls"
    - path: "tests/ai-proxy.test.mjs"
      provides: "Proxy metering metadata-only regressions"
    - path: "tests/assist-route.test.mjs"
      provides: "Assist route envelope no-leak regressions"
    - path: "tests/onboard-route.test.mjs"
      provides: "Resume-AI envelope no-leak regressions"
  key_links:
    - from: "tests/call-ai.test.mjs"
      to: "src/core/ai/usage-log.mjs"
      via: "BYOK usage rows include labels and token/cost metadata only"
      pattern: "readUsageEvents"
    - from: "tests/ai-proxy.test.mjs"
      to: "src/cli/ai-proxy.mjs"
      via: "Proxy usage rows include labels and token/cost metadata only"
      pattern: "x-careerrat-skill"
---

## Phase Goal

**As a** CareerRat app surface, **I want to** call cheap bounded AI primitives, **so that** users receive consistent success, manual, and error envelopes without launching full skills.

<objective>
Add final telemetry and privacy regression coverage after all bounded helper consumers migrate.

Purpose: AIR-04 and D-09 through D-12 are cost-control and privacy boundaries. This plan adds regression tests across helper, BYOK, proxy, assist, and resume-AI paths after the code-producing plans establish the shared contract.
Output: Tests prove labels and metadata-only telemetry across BYOK/proxy and route envelopes.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-bounded-ai-foundation/02-CONTEXT.md
@.planning/phases/02-bounded-ai-foundation/02-RESEARCH.md
@.planning/phases/02-bounded-ai-foundation/02-04-SUMMARY.md
@.planning/phases/02-bounded-ai-foundation/02-05-SUMMARY.md
@.planning/phases/02-bounded-ai-foundation/02-06-SUMMARY.md
@src/core/ai/bounded-ai.mjs
@src/core/ai/call-ai.mjs
@src/core/ai/usage-log.mjs
@src/cli/ai-proxy.mjs
@tests/bounded-ai.test.mjs
@tests/call-ai.test.mjs
@tests/ai-proxy.test.mjs
@tests/assist-route.test.mjs
@tests/onboard-route.test.mjs
</context>

## Artifacts this phase produces

- Modified `tests/bounded-ai.test.mjs`.
- Modified `tests/call-ai.test.mjs`.
- Modified `tests/ai-proxy.test.mjs`.
- Modified `tests/assist-route.test.mjs`.
- Modified `tests/onboard-route.test.mjs`.
- New helper regression asserting label failures occur before invocation.
- New BYOK usage-row regression asserting allowed telemetry keys only.
- New proxy usage-row regression asserting allowed telemetry keys only.
- New route-envelope no-leak regression for assist schema/provider failures.
- New route-envelope no-leak regression for resume-AI schema, missing-configuration, and provider-failure paths.
- New allowed telemetry key set in tests: `id`, `at`, `source`, `skill`, `action`, `model`, `upstream`, `tokens_in`, `tokens_out`, `cache_read_tokens`, `cache_creation_tokens`, `web_searches`, `shared_cache_hit`, `cost_usd`, and `priced`.

<tasks>

<task type="auto">
  <name>Task 1: TDD telemetry label and no-leak regressions</name>
  <files>tests/bounded-ai.test.mjs, tests/call-ai.test.mjs, tests/ai-proxy.test.mjs, tests/assist-route.test.mjs, tests/onboard-route.test.mjs</files>
  <read_first>
    <file>.planning/phases/02-bounded-ai-foundation/02-CONTEXT.md</file>
    <file>.planning/phases/02-bounded-ai-foundation/02-RESEARCH.md</file>
    <file>.planning/phases/02-bounded-ai-foundation/02-04-SUMMARY.md</file>
    <file>.planning/phases/02-bounded-ai-foundation/02-05-SUMMARY.md</file>
    <file>.planning/phases/02-bounded-ai-foundation/02-06-SUMMARY.md</file>
    <file>src/core/ai/bounded-ai.mjs</file>
    <file>src/core/ai/call-ai.mjs</file>
    <file>src/core/ai/usage-log.mjs</file>
    <file>src/cli/ai-proxy.mjs</file>
    <file>tests/bounded-ai.test.mjs</file>
    <file>tests/call-ai.test.mjs</file>
    <file>tests/ai-proxy.test.mjs</file>
    <file>tests/assist-route.test.mjs</file>
    <file>tests/onboard-route.test.mjs</file>
  </read_first>
  <behavior>
    - REGRESSION: Add tests that serialize helper, assist, and resume-AI failure bodies and assert they do not expose prompt text, raw model replies, resume bytes/text, JD text, candidate facts, or page body content.
    - REGRESSION: Add or strengthen a resume-AI SDK/provider failure regression asserting status 502 with `body.code === "AI_PROVIDER_FAILED"` and shared manual fallback metadata, distinct from true missing-configuration status 501 with `body.code === "NO_AI_ROUTE"`.
    - REGRESSION: Add BYOK usage-row tests asserting skill/action labels are present for bounded native calls and usage event keys are limited to metadata/cost fields.
    - REGRESSION: Add proxy metering tests asserting proxy usage rows preserve skill/action labels for bounded calls and do not contain request body, response body, prompt, schema payload, or raw output fields.
    - REGRESSION: Add or strengthen missing-label tests so missing `skill`, `action`, or `operation` fails before any helper invocation or network request.
    - OWNER ROUTING: If a regression exposes production leakage or dropped labels, keep the failing regression test in this plan and fail fast with the owning prior plan identified in the summary: 02-01 for `bounded-ai.mjs`, 02-02 for `call-ai.mjs`, 02-03 for native helper integration, 02-04 for `assist-route`, 02-05 for `intake/classify`, or 02-06 for `resume-ai`. Do not patch production files from this plan.
    - TEST REFACTOR: Consolidate test-only helpers for allowed usage keys or forbidden-content scans inside the relevant test files; do not create a new production abstraction for tests.
  </behavior>
  <action>Add final AIR-04 and D-09 through D-12 regressions across the bounded helper, BYOK `callAI()` path, managed proxy path, assist route, and resume-AI route. Tests must prove labels are stable and metadata-only telemetry remains free of prompts, raw model output, resumes, JDs, candidate facts, and page bodies. If a regression fails because production code leaks content or drops labels, do not patch production from Plan 02-07; preserve the failing regression, stop the plan, and document the owning earlier plan that must receive the production fix. Do not broaden this plan into Phase 3 discovery behavior.</action>
  <verify>
    <automated>node --test tests/bounded-ai.test.mjs tests/call-ai.test.mjs tests/ai-proxy.test.mjs tests/assist-route.test.mjs tests/onboard-route.test.mjs tests/intake-classify.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The full bounded-AI subset command exits 0 without live AI credentials.
    - BYOK usage-row tests prove `skill` and `action` are non-null for bounded calls and no prompt/body/raw content keys exist.
    - Proxy usage-row tests prove `skill` and `action` are non-null for bounded calls and no prompt/body/raw content keys exist.
    - Route envelope tests prove no serialized assist or resume-AI failure body includes raw model reply content.
    - Resume-AI provider-failure tests prove SDK/provider/proxy/timeout/transport failures use status 502 with `code:"AI_PROVIDER_FAILED"` rather than status 501 `NO_AI_ROUTE`.
    - Missing label tests prove invocation/network spies remain uncalled.
  </acceptance_criteria>
  <done>Telemetry and no-leak tests lock the bounded AI cost/privacy contract across helper, BYOK, proxy, and migrated route paths.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| bounded routes -> usage ledger | AI usage metadata is written locally. |
| bounded routes -> managed proxy | Proxy receives request bodies and labels, then meters server-side. |
| migrated routes -> browser | Failure envelopes return to app clients. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-02-07-01 | Information Disclosure | usage rows | high | mitigate | Tests assert usage rows contain only allowed metadata/cost keys. |
| T-02-07-02 | Repudiation | BYOK/proxy labels | high | mitigate | Tests assert skill/action labels are preserved for bounded calls on both paths. |
| T-02-07-03 | Information Disclosure | route failure envelopes | high | mitigate | Tests assert serialized failure bodies do not contain raw model replies or sensitive input text. |
| T-02-07-04 | Tampering | missing-label invocation | medium | mitigate | Tests assert missing labels fail before model/proxy calls. |

</threat_model>

<verification>
Run the final bounded-AI subset:

<automated>node --test tests/bounded-ai.test.mjs tests/call-ai.test.mjs tests/ai-proxy.test.mjs tests/assist-route.test.mjs tests/onboard-route.test.mjs tests/intake-classify.test.mjs</automated>
</verification>

<success_criteria>
- AIR-04 telemetry is proven for BYOK and proxy paths.
- Missing labels are regression-tested.
- Route and telemetry outputs remain metadata-only.
</success_criteria>

<output>
Create `.planning/phases/02-bounded-ai-foundation/02-07-SUMMARY.md` when done.
</output>
