---
phase: 08-deep-ingest-lane
plan: 09
type: execute
wave: 7
depends_on: ["08-07", "08-08"]
files_modified:
  - tests/deep-ingest-regression.test.mjs
  - tests/fixtures/deep-ingest/evals/source-cases.json
  - tests/fixtures/deep-ingest/evals/expected-outcomes.json
  - .planning/phases/08-deep-ingest-lane/08-VERIFICATION-ROLLUP.md
autonomous: true
requirements: [ING-01, ING-02, ING-03, ING-04]
must_haves:
  truths:
    - "The implemented phase proves end-to-end source ingest, proposal review, manual fallback, and lane terminality across DB, API, and UI."
    - "The test suite covers no-AI/manual fallback and bounded schema-validated proposal paths without live provider dependence."
    - "Security-relevant regressions for untrusted input, unsafe URLs, private/local data leakage, oversized sources, ungrounded claims, current-comp leakage, and review bypass are blocked by tests."
  artifacts:
    - "tests/deep-ingest-regression.test.mjs"
    - "tests/fixtures/deep-ingest/evals/source-cases.json"
    - "tests/fixtures/deep-ingest/evals/expected-outcomes.json"
    - ".planning/phases/08-deep-ingest-lane/08-VERIFICATION-ROLLUP.md"
  key_links:
    - "Regression fixtures -> DB/API/UI tests -> verification rollup"
    - "Threat mitigations -> automated checks -> phase completion evidence"
---

## Phase Goal

**As a** CareerRat user, **I want to** drop profile, project, story, and job-search context into a local Deep ingest lane, **so that** reviewed evidence, stories, honesty boundaries, voice, role signals, and gaps become durable SQLite state without blocking sourcing.

<objective>
Close Phase 8 with end-to-end regression coverage and a verification rollup.

Purpose: The phase should finish with executable evidence that all user decisions, requirements, and threat mitigations are implemented across the vertical slices.
Output: Regression fixtures/tests and a concise verification rollup artifact.
</objective>

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-deep-ingest-lane/08-CONTEXT.md
@.planning/phases/08-deep-ingest-lane/08-RESEARCH.md
@.planning/phases/08-deep-ingest-lane/08-AI-SPEC.md
@.planning/phases/08-deep-ingest-lane/08-UI-SPEC.md
@.planning/phases/08-deep-ingest-lane/08-VALIDATION.md
@.planning/phases/08-deep-ingest-lane/08-01-SUMMARY.md
@.planning/phases/08-deep-ingest-lane/08-02-SUMMARY.md
@.planning/phases/08-deep-ingest-lane/08-03-SUMMARY.md
@.planning/phases/08-deep-ingest-lane/08-04-SUMMARY.md
@.planning/phases/08-deep-ingest-lane/08-05-SUMMARY.md
@.planning/phases/08-deep-ingest-lane/08-06-SUMMARY.md
@.planning/phases/08-deep-ingest-lane/08-07-SUMMARY.md
@.planning/phases/08-deep-ingest-lane/08-08-SUMMARY.md
@AGENTS.md
@candidate/AGENTS.md
</context>

## Artifacts This Phase Produces

- Test file: `tests/deep-ingest-regression.test.mjs`
- Fixture file: `tests/fixtures/deep-ingest/evals/source-cases.json`
- Fixture file: `tests/fixtures/deep-ingest/evals/expected-outcomes.json`
- Verification artifact: `.planning/phases/08-deep-ingest-lane/08-VERIFICATION-ROLLUP.md`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add end-to-end Deep ingest regression fixtures</name>
  <files>tests/deep-ingest-regression.test.mjs, tests/fixtures/deep-ingest/evals/source-cases.json, tests/fixtures/deep-ingest/evals/expected-outcomes.json</files>
  <read_first>
    @tests/deep-ingest-db.test.mjs
    @tests/deep-ingest-route.test.mjs
    @tests/deep-ingest-ai.test.mjs
    @tests/deep-ingest-source-scanner.test.mjs
    @tests/bounded-ai.test.mjs
    @.planning/phases/08-deep-ingest-lane/08-VALIDATION.md
    @.planning/phases/08-deep-ingest-lane/08-AI-SPEC.md
    @.planning/phases/08-deep-ingest-lane/08-CONTEXT.md
  </read_first>
  <behavior>
    - A happy-path fixture ingests a source, creates bounded proposals, confirms edited proposals, projects Library cards, and makes required lanes terminal.
    - A no-AI fixture uses manual input and still reaches explicit completed/deferred/not-available lane states.
    - A hostile-source fixture captures prompt injection, unsafe URL, local/private path, oversized body, ungrounded claim, current-comp/private data, and review-bypass attempts as gaps/rejections/manual fallback.
    - Regression tests use temp SQLite DBs and stubbed bounded-AI responses only.
  </behavior>
  <action>Create the two JSON fixture files with source cases and expected outcomes for the behaviors above. Add `tests/deep-ingest-regression.test.mjs` that composes the scanner, proposal validators, DB verbs, readiness evaluator, and route handlers from previous plans against a real temp DB. The test must cover ING-01 through ING-04 and cite the locked decisions D-01 through D-15 in case names or comments so failures map back to the user decisions. Do not call a live AI provider, browse the network, or rely on candidate-file compatibility exports.</action>
  <verify>
    <automated>node --test tests/deep-ingest-regression.test.mjs tests/deep-ingest-db.test.mjs tests/deep-ingest-route.test.mjs tests/deep-ingest-ai.test.mjs tests/deep-ingest-source-scanner.test.mjs tests/bounded-ai.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Regression tests fail if unreviewed proposals become trusted facts.
    - Regression tests fail if manual/no-AI fallback cannot complete or terminally defer a required lane.
    - Regression tests fail if unsafe/private/oversized/ungrounded inputs are accepted as trusted claims.
    - Regression tests run against temp SQLite DB files, not in-memory mocks.
  </acceptance_criteria>
  <done>Deep ingest has regression coverage for happy path, manual fallback, and hostile/unreadable source paths.</done>
</task>

<task type="auto">
  <name>Task 2: Run phase verification and write the rollup</name>
  <files>.planning/phases/08-deep-ingest-lane/08-VERIFICATION-ROLLUP.md</files>
  <read_first>
    @.planning/phases/08-deep-ingest-lane/08-VALIDATION.md
    @.planning/phases/08-deep-ingest-lane/08-CONTEXT.md
    @.planning/phases/08-deep-ingest-lane/08-RESEARCH.md
    @.planning/phases/08-deep-ingest-lane/08-AI-SPEC.md
    @.planning/phases/08-deep-ingest-lane/08-UI-SPEC.md
    @AGENTS.md
    @candidate/AGENTS.md
  </read_first>
  <action>Run the focused Deep ingest backend tests, the focused web tests, `npm run app:build`, and the full project test commands listed below. Write `.planning/phases/08-deep-ingest-lane/08-VERIFICATION-ROLLUP.md` with command, exit code, timestamp, and a requirement/decision coverage table for ING-01 through ING-04 and D-01 through D-15. If a command fails, record the failing command and exact failure summary in the rollup before stopping so the next gap-closure plan has durable evidence.</action>
  <verify>
    <automated>node --test tests/deep-ingest-*.test.mjs tests/bounded-ai.test.mjs tests/story-bank.test.mjs tests/writing-style.test.mjs && npm --workspace apps/web run test -- src/deep-ingest/DeepIngestPage.test.jsx src/library/LibraryPage.test.jsx src/onboarding/steps/FinishStep.test.jsx src/pages/SetupReadinessCard.test.jsx && npm run app:build && npm test && npm --workspace apps/web run test</automated>
  </verify>
  <acceptance_criteria>
    - The rollup contains rows for ING-01, ING-02, ING-03, ING-04, and D-01 through D-15.
    - The rollup lists each command executed and its exit code.
    - The rollup states whether `deep_ingest_complete`, no-AI fallback, proposal-first confirmation, source scanning limits, UI-SPEC labels, and full AI interview/chat exclusion passed.
    - When all commands pass, the rollup final status is `PASS`; when any command fails, final status is `FAIL` with the failing command named.
  </acceptance_criteria>
  <done>Phase 8 has durable verification evidence and a machine-runnable regression suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Regression fixtures to implementation | Synthetic hostile inputs exercise scanner, AI validators, DB verbs, routes, and UI assumptions. |
| Verification rollup to phase completion | Test results become the evidence used by orchestration to decide completion or gap closure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | Regression fixtures | medium | mitigate | Fixtures encode expected outcomes and run against implementation paths, not manually inspected snapshots. |
| T-08-02 | Information Disclosure | Test data | medium | mitigate | Fixtures use synthetic candidate/job data and prohibit current-comp/private real user values. |
| T-08-03 | Repudiation | Verification rollup | medium | mitigate | Rollup records command, exit code, timestamp, and coverage rows for each requirement and decision. |
| T-08-04 | Denial of Service | Full verification | low | mitigate | Focused tests run before full suite; failures stop after durable rollup entry. |
| T-08-05 | Tampering | Threat mitigation coverage | high | mitigate | Regression tests fail on review bypass, ungrounded trusted claims, unsafe URL acceptance, private/local leakage, and oversized source acceptance. |
| T-08-06 | Spoofing | AI responses | medium | mitigate | Tests use stubbed bounded-AI responses with schema validation; no live provider response is trusted. |
| T-08-SC | Tampering | npm installs | low | accept | No package install is needed. |
</threat_model>

<verification>
Run `node --test tests/deep-ingest-*.test.mjs tests/bounded-ai.test.mjs tests/story-bank.test.mjs tests/writing-style.test.mjs`, `npm --workspace apps/web run test -- src/deep-ingest/DeepIngestPage.test.jsx src/library/LibraryPage.test.jsx src/onboarding/steps/FinishStep.test.jsx src/pages/SetupReadinessCard.test.jsx`, `npm run app:build`, `npm test`, and `npm --workspace apps/web run test`.
</verification>

<success_criteria>
- Regression fixtures cover all source, proposal, review, fallback, terminality, and threat paths.
- Verification rollup is written with PASS/FAIL evidence.
- All Phase 8 requirement IDs and user decisions are traced to passing checks or named failures.
</success_criteria>

<output>
Create `.planning/phases/08-deep-ingest-lane/08-09-SUMMARY.md` when done.
</output>
