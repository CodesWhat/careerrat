---
phase: 05-verification-and-docs
plan: 04
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - AGENTS.md
  - docs/ARCHITECTURE.md
  - .planning/architecture/runtime-routing-policy.md
  - tests/decomposition-map.test.mjs
autonomous: true
requirements:
  - VER-05
must_haves:
  truths:
    - "VER-05: AGENTS.md, docs/ARCHITECTURE.md, and app route behavior describe the same runtime split."
    - "Docs name local company proposal APIs as the default app path, bounded AI only for company seed judgment, explicit chat handoffs, and retained allowlisted POST /api/skill/run."
    - "A docs drift guard fails if the required route classes or no-hidden-fallback language disappears."
  artifacts:
    - path: "AGENTS.md"
      provides: "Agent-facing routing contract aligned with app local proposal behavior"
    - path: "docs/ARCHITECTURE.md"
      provides: "Public architecture layer wording for local API, bounded AI, chat handoff, and retained runtime"
    - path: ".planning/architecture/runtime-routing-policy.md"
      provides: "Detailed route policy authority"
    - path: "tests/decomposition-map.test.mjs"
      provides: "Static docs drift guard for VER-05"
  key_links:
    - from: "tests/decomposition-map.test.mjs"
      to: "AGENTS.md"
      via: "required route-class phrases"
      pattern: "company-proposals|company-proposal-decisions"
    - from: "tests/decomposition-map.test.mjs"
      to: "docs/ARCHITECTURE.md"
      via: "layer split assertions"
      pattern: "Local API and DB Layer"
    - from: "tests/decomposition-map.test.mjs"
      to: ".planning/architecture/runtime-routing-policy.md"
      via: "policy route class assertions"
      pattern: "POST /api/skill/run"
---

<objective>
Align the agent contract, architecture docs, routing policy, and docs drift tests for VER-05.

Purpose: Runtime guarantees degrade when docs drift from app behavior; Phase 05 locks the local/default, bounded-AI, explicit-chat, and retained-runtime split in both docs and tests.
Output: Updated docs plus a static drift guard in `tests/decomposition-map.test.mjs`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-verification-and-docs/05-RESEARCH.md
@.planning/phases/05-verification-and-docs/05-PATTERNS.md
@.planning/phases/05-verification-and-docs/05-VALIDATION.md
@.planning/phases/04-runtime-routing/04-05-SUMMARY.md
@.planning/phases/04-runtime-routing/04-VERIFICATION.md
@AGENTS.md
@docs/ARCHITECTURE.md
@.planning/architecture/runtime-routing-policy.md
@tests/decomposition-map.test.mjs
@apps/web/src/lib/api.js
@apps/web/src/onboarding/steps/CompaniesStep.jsx
</context>

## Phase Goal

Lock in the cost, safety, and routing guarantees before broader migration.

## Artifacts this phase produces

- `AGENTS.md`: Updates or confirms the agent-facing company discovery routing language for VER-05.
- `docs/ARCHITECTURE.md`: Updates or confirms public architecture layer wording for VER-05.
- `.planning/architecture/runtime-routing-policy.md`: Updates or confirms detailed policy language for VER-05.
- `tests/decomposition-map.test.mjs`: Adds or strengthens static docs drift guard assertions.
- Command: `node --test tests/decomposition-map.test.mjs`.

<tasks>

<task type="auto">
  <name>Task 1: Align AGENTS and architecture docs with route behavior</name>
  <files>AGENTS.md, docs/ARCHITECTURE.md, .planning/architecture/runtime-routing-policy.md</files>
  <read_first>
    @AGENTS.md
    @docs/ARCHITECTURE.md
    @.planning/architecture/runtime-routing-policy.md
    @.agents/skills/discover-companies/SKILL.md
    @.agents/skills/search-jobs/SKILL.md
    @.planning/phases/04-runtime-routing/04-05-SUMMARY.md
    @.planning/phases/04-runtime-routing/04-VERIFICATION.md
  </read_first>
  <action>
    Update only the routing-policy portions of the docs. Ensure all three docs say that app company discovery defaults to local proposal create/read/decision APIs; bounded AI is limited to seed judgment and its output remains advisory until deterministic validation; `/api/discovery/quick-start`, `/api/discovery/next`, and `/api/chat/*` are explicit user-selected chat handoffs; and `POST /api/skill/run` remains retained allowlisted full skill runtime for tool-heavy workflows. Preserve the skill-led discovery order and confirm-first company/source writes from AGENTS.md. Keep examples domain-neutral and do not include private compensation values.
  </action>
  <verify>
    <automated>rg -n "company-proposals|company-proposal-decisions|/api/discovery/quick-start|/api/discovery/next|/api/chat|/api/skill/run" AGENTS.md docs/ARCHITECTURE.md .planning/architecture/runtime-routing-policy.md</automated>
  </verify>
  <acceptance_criteria>
    The three docs describe the same local/default, bounded-AI, explicit-chat, and retained-runtime split without implying hidden fallback from local proposal errors to chat or full skill runtime.
  </acceptance_criteria>
  <done>Docs are aligned with Phase 04 route behavior for VER-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add docs drift guard</name>
  <files>tests/decomposition-map.test.mjs</files>
  <read_first>
    @tests/decomposition-map.test.mjs
    @AGENTS.md
    @docs/ARCHITECTURE.md
    @.planning/architecture/runtime-routing-policy.md
    @apps/web/src/lib/api.js
    @apps/web/src/onboarding/steps/CompaniesStep.jsx
  </read_first>
  <behavior>
    - Test: all three docs include local proposal route names and retained runtime route names.
    - Test: docs distinguish local proposal default, bounded AI seed judgment, explicit chat handoff, and retained full skill runtime.
    - Test: docs or app-route guard rejects wording that suggests local proposal failures silently start chat or `POST /api/skill/run`.
    - Test: app API wrappers still name `/api/discovery/company-proposals` and `/api/discovery/company-proposal-decisions`.
  </behavior>
  <action>
    Extend `tests/decomposition-map.test.mjs` with a focused VER-05 drift test. Prefer phrase/section assertions over whole-file snapshots. Keep negative checks region- or phrase-scoped so the retained full skill runtime can still be documented where appropriate. Avoid brittle line-number assertions.
  </action>
  <verify>
    <automated>node --test tests/decomposition-map.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    The docs drift guard fails if required route-class language or app route names disappear, while still allowing legitimate retained runtime documentation.
  </acceptance_criteria>
  <done>VER-05 has an automated docs/app-route drift guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| documentation to future implementation | Docs are used as routing authority by agents and maintainers. |
| public docs to candidate-private data | Public architecture docs must not disclose candidate-specific compensation or search posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-10 | Repudiation | routing docs | medium | mitigate | Drift guard checks all three docs for the same route-class split. |
| T-05-11 | Information Disclosure | documentation examples | medium | mitigate | Keep examples domain-neutral and avoid private compensation or candidate-specific details. |
| T-05-12 | Elevation of Privilege | retained runtime wording | medium | mitigate | Docs and tests preserve allowlisted retained runtime as explicit, not hidden fallback. |
</threat_model>

<verification>
<automated>node --test tests/decomposition-map.test.mjs</automated>
</verification>

<success_criteria>
- VER-05 appears in this plan frontmatter and is covered by docs plus automated drift tests.
- AGENTS.md, docs/ARCHITECTURE.md, and runtime-routing policy align with current app routes.
- No source schema files, package dependencies, production tracker/candidate data, or unrelated release-safety tests are touched.
</success_criteria>

<output>
Create `.planning/phases/05-verification-and-docs/05-04-SUMMARY.md` when done.
</output>
