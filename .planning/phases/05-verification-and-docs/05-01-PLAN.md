---
phase: 05-verification-and-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/company-discovery-regression.test.mjs
autonomous: true
requirements:
  - VER-01
must_haves:
  truths:
    - "VER-01: Deterministic discovery resolver, scanner, gate, proposal-read, refresh, and confirmed-write paths do not invoke AI, chat, or retained full skill runtime."
    - "AI remains allowed only in the company seed module for schema-validated company seed generation."
    - "Regression coverage uses hermetic static/injected seams and never makes live AI, network, tracker, or candidate data writes."
  artifacts:
    - path: "tests/company-discovery-regression.test.mjs"
      provides: "Cost-boundary regression locks for deterministic company discovery paths"
  key_links:
    - from: "tests/company-discovery-regression.test.mjs"
      to: "src/core/discovery/*"
      via: "static ownership scan and mounted route seam assertions"
      pattern: "callAI|runBoundedAI|runSkillStream|startSession|/api/skill/run"
    - from: "tests/company-discovery-regression.test.mjs"
      to: "src/cli/discovery-route.mjs"
      via: "route-slice scan for local proposal create/read/decision paths"
      pattern: "company-proposals"
---

<objective>
Lock the Phase 05 cost boundary for VER-01 by making deterministic company discovery paths visibly AI-free.

Purpose: Phase 05 must prevent later migration work from accidentally routing cheap deterministic resolver, scanner, proposal state, refresh, or confirmed-write behavior through model calls, chat sessions, or retained full skill runtime.
Output: Focused regression assertions in `tests/company-discovery-regression.test.mjs`.
</objective>

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-verification-and-docs/05-RESEARCH.md
@.planning/phases/05-verification-and-docs/05-PATTERNS.md
@.planning/phases/05-verification-and-docs/05-VALIDATION.md
@.planning/phases/03-company-discovery-api/03-07-SUMMARY.md
@.planning/phases/04-runtime-routing/04-VERIFICATION.md
@tests/company-discovery-regression.test.mjs
@src/cli/discovery-route.mjs
@src/core/discovery/company-seeds.mjs
@src/core/discovery/company-proposals.mjs
@src/core/discovery/company-proposal-decisions.mjs
@src/core/discovery/company-board-resolver.mjs
@src/core/discovery/company-proposal-gate.mjs
</context>

## Phase Goal

Lock in the cost, safety, and routing guarantees before broader migration.

## Artifacts this phase produces

- `tests/company-discovery-regression.test.mjs`: Adds or strengthens VER-01 cost-boundary assertions.
- Command: `node --test tests/company-discovery-regression.test.mjs`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic discovery cost-boundary regressions</name>
  <files>tests/company-discovery-regression.test.mjs</files>
  <read_first>
    @.planning/phases/05-verification-and-docs/05-RESEARCH.md
    @.planning/phases/05-verification-and-docs/05-PATTERNS.md
    @.planning/phases/05-verification-and-docs/05-VALIDATION.md
    @tests/company-discovery-regression.test.mjs
    @src/cli/discovery-route.mjs
    @src/core/discovery/company-seeds.mjs
    @src/core/discovery/company-proposals.mjs
    @src/core/discovery/company-proposal-decisions.mjs
    @src/core/discovery/company-board-resolver.mjs
    @src/core/discovery/company-proposal-gate.mjs
  </read_first>
  <behavior>
    - Test: deterministic discovery files outside `src/core/discovery/company-seeds.mjs` have no `callAI(` or `runBoundedAI` usage.
    - Test: local proposal create/read/decision route slices in `src/cli/discovery-route.mjs` do not reference `runSkillStream`, `startSession`, or `/api/skill/run`.
    - Test: manual proposal create/read/approval and refresh paths leave `chatRuntime.starts.length` at zero and use injected deterministic seams.
    - Test: source-config and sourced-row write verbs remain limited to supported approval behavior already covered by the Phase 03 path.
  </behavior>
  <action>
    Extend the existing static ownership check in `tests/company-discovery-regression.test.mjs` so VER-01 is explicit. Allow bounded AI only in `src/core/discovery/company-seeds.mjs`; require resolver, scanner/gate, proposal orchestration, proposal reads, refresh, and confirmed-write code to stay free of direct AI, chat runtime, and retained full skill runtime references. Keep the test hermetic: use file reads and existing mounted-route fakes, not live AI/network calls. Do not edit production code unless the new regression exposes a real behavior defect; if that happens, patch only the owning discovery module and record the deviation in the SUMMARY.
  </action>
  <verify>
    <automated>node --test tests/company-discovery-regression.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    `node --test tests/company-discovery-regression.test.mjs` passes, and the file contains purpose-named assertions proving deterministic discovery paths do not call AI, chat, or retained full skill runtime for VER-01.
  </acceptance_criteria>
  <done>VER-01 has an explicit automated cost-boundary regression lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| deterministic discovery code to AI/runtime seams | Local resolver, scanner, gate, refresh, read, and write code must not cross into model or retained runtime execution. |
| tests to local filesystem | Static scans read source files and must not mutate tracker/candidate/workspace state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-01 | Information Disclosure | deterministic discovery paths | high | mitigate | Static and mounted-route tests prove deterministic paths avoid AI/chat/full runtime seams that could expose local context. |
| T-05-02 | Denial of Service | accidental full-runtime escalation | medium | mitigate | Regression forbids hidden `runSkillStream`, `startSession`, and `/api/skill/run` usage in local proposal paths. |
| T-05-03 | Tampering | test fixtures | low | mitigate | Use temp repos and source reads only; do not touch production tracker/candidate data. |
</threat_model>

<verification>
<automated>node --test tests/company-discovery-regression.test.mjs</automated>
</verification>

<success_criteria>
- VER-01 appears in this plan frontmatter and is covered by an automated regression.
- Deterministic discovery paths stay local and deterministic.
- No schema/ORM files, package dependencies, tracker exports, or `tests/release-safety.test.mjs` are touched.
</success_criteria>

<output>
Create `.planning/phases/05-verification-and-docs/05-01-SUMMARY.md` when done.
</output>
