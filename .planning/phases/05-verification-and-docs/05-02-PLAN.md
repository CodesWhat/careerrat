---
phase: 05-verification-and-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/company-discovery-seeds.test.mjs
  - tests/company-proposals-route.test.mjs
autonomous: true
requirements:
  - VER-02
  - VER-03
must_haves:
  truths:
    - "VER-02: Company seed structured-output failures cover malformed JSON, corrective retry, and schema rejection at helper and route level."
    - "VER-03: No-AI company proposal route behavior returns manual fallback metadata without starting chat or retained full skill runtime."
    - "Schema-invalid or unavailable AI cannot create proposal batches, confirmed source-config writes, sourced rows, or generated tracker files."
  artifacts:
    - path: "tests/company-discovery-seeds.test.mjs"
      provides: "Helper-level structured-output retry and schema rejection coverage for company seeds"
    - path: "tests/company-proposals-route.test.mjs"
      provides: "Route-level no-AI/manual and AI_SCHEMA_INVALID proposal behavior coverage"
  key_links:
    - from: "src/core/discovery/company-seeds.mjs"
      to: "src/core/ai/bounded-ai.mjs"
      via: "generateCompanySeeds and runBoundedAI"
      pattern: "AI_SCHEMA_INVALID|NO_AI_ROUTE"
    - from: "src/cli/discovery-route.mjs"
      to: "src/core/discovery/company-proposals.mjs"
      via: "POST /api/discovery/company-proposals"
      pattern: "createCompanyProposalBatch"
---

<objective>
Lock structured-output and no-AI negative behavior for company proposal seed generation.

Purpose: VER-02 and VER-03 need route-visible guarantees, not only lower-level helper coverage, so malformed model output and missing AI configuration remain safe manual states.
Output: Focused tests in `tests/company-discovery-seeds.test.mjs` and `tests/company-proposals-route.test.mjs`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-verification-and-docs/05-RESEARCH.md
@.planning/phases/05-verification-and-docs/05-PATTERNS.md
@.planning/phases/05-verification-and-docs/05-VALIDATION.md
@.planning/phases/02-bounded-ai-foundation/02-01-SUMMARY.md
@.planning/phases/02-bounded-ai-foundation/02-03-SUMMARY.md
@.planning/phases/03-company-discovery-api/03-04-SUMMARY.md
@tests/company-discovery-seeds.test.mjs
@tests/company-proposals-route.test.mjs
@tests/bounded-ai.test.mjs
@tests/structured-oneshot.test.mjs
@src/core/discovery/company-seeds.mjs
@src/core/discovery/company-proposals.mjs
@src/cli/discovery-route.mjs
</context>

## Phase Goal

Lock in the cost, safety, and routing guarantees before broader migration.

## Artifacts this phase produces

- `tests/company-discovery-seeds.test.mjs`: Adds or strengthens VER-02 helper-level retry/schema rejection assertions.
- `tests/company-proposals-route.test.mjs`: Adds or strengthens VER-02/VER-03 route-level manual fallback assertions.
- Command: `node --test tests/company-discovery-seeds.test.mjs tests/company-proposals-route.test.mjs tests/bounded-ai.test.mjs tests/structured-oneshot.test.mjs`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add company seed structured-output negative tests</name>
  <files>tests/company-discovery-seeds.test.mjs</files>
  <read_first>
    @tests/company-discovery-seeds.test.mjs
    @tests/bounded-ai.test.mjs
    @tests/structured-oneshot.test.mjs
    @src/core/discovery/company-seeds.mjs
    @src/core/ai/bounded-ai.mjs
    @src/core/ai/structured-oneshot.mjs
    @.planning/phases/05-verification-and-docs/05-PATTERNS.md
  </read_first>
  <behavior>
    - Test: malformed seed JSON gets exactly one corrective retry and succeeds when the retry returns schema-valid `companies`.
    - Test: schema-invalid company seed payload returns `AI_SCHEMA_INVALID` with `manual.available:true`, `ai.retried:true`, and no trusted URL/provider/write fields accepted.
    - Test: exhausted malformed output returns a safe manual envelope without raw model text, prompt text, candidate facts, or private compensation values.
  </behavior>
  <action>
    Extend `tests/company-discovery-seeds.test.mjs` around `generateCompanySeeds`, `companySeedSchema`, and the injected `call` seam. Write the expected behavior first; if an assertion already passes because Phase 02/03 code satisfies it, keep the test-only regression and note that in the SUMMARY. Keep all AI responses in fixtures and injected callbacks; do not use live providers.
  </action>
  <verify>
    <automated>node --test tests/company-discovery-seeds.test.mjs tests/bounded-ai.test.mjs tests/structured-oneshot.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    Helper-level tests explicitly prove malformed parse failure, corrective retry, schema rejection, and safe manual envelope behavior for VER-02.
  </acceptance_criteria>
  <done>VER-02 helper-level seed structured-output negatives are locked by automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add route-level no-AI and schema-invalid proposal tests</name>
  <files>tests/company-proposals-route.test.mjs</files>
  <read_first>
    @tests/company-proposals-route.test.mjs
    @tests/company-discovery-regression.test.mjs
    @src/cli/discovery-route.mjs
    @src/core/discovery/company-proposals.mjs
    @src/core/discovery/company-seeds.mjs
    @.planning/phases/05-verification-and-docs/05-VALIDATION.md
  </read_first>
  <behavior>
    - Test: POST `/api/discovery/company-proposals` with no manual seeds and no AI returns status 501/code `NO_AI_ROUTE`, `manual.available:true`, `ai.used:false`, and starts no chat session.
    - Test: route-level `AI_SCHEMA_INVALID` returns status 422 with manual metadata and creates no proposal batch.
    - Test: both failures call no confirmed write seams and leave generated `workspace/tracker.json` and `workspace/activity.jsonl` absent in the temp repo.
  </behavior>
  <action>
    Strengthen `tests/company-proposals-route.test.mjs` using the existing mounted route harness, fake chat runtime, forbidden write seams, and temp repo assertions. Route-level failures must pass through the bounded-AI envelope and stay local/manual; they must not call chat, retained full skill runtime, `companyAtsUpsert`, `sourcedUpsertBatch`, or generated tracker/activity writes.
  </action>
  <verify>
    <automated>node --test tests/company-proposals-route.test.mjs tests/company-discovery-seeds.test.mjs tests/bounded-ai.test.mjs tests/structured-oneshot.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    Route-level structured-output and no-AI failures are explicit, manual, write-free, and do not escalate to chat/full skill runtime for VER-02 and VER-03.
  </acceptance_criteria>
  <done>VER-02/VER-03 route-level negative behavior is locked by automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| model output to route data | Model text is untrusted until parsed, schema-validated, and normalized. |
| missing AI route to app controls | No-AI behavior must preserve manual/local user paths without hidden runtime escalation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-04 | Tampering | structured seed payloads | high | mitigate | Tests reject schema-invalid output and trusted write fields before proposal creation. |
| T-05-05 | Information Disclosure | AI error envelopes | high | mitigate | Tests assert safe manual metadata without raw prompt/model/candidate/private compensation leakage. |
| T-05-06 | Denial of Service | no-AI proposal route | medium | mitigate | Tests prove no-AI returns manual fallback instead of blocking local/manual controls or starting full runtime. |
</threat_model>

<verification>
<automated>node --test tests/company-discovery-seeds.test.mjs tests/company-proposals-route.test.mjs tests/bounded-ai.test.mjs tests/structured-oneshot.test.mjs</automated>
</verification>

<success_criteria>
- VER-02 and VER-03 appear in this plan frontmatter and have automated route/helper coverage.
- Malformed JSON, corrective retry, schema rejection, and no-AI/manual fallback behavior are tested.
- No real AI/network calls, package dependencies, tracker data writes, or unrelated release-safety edits are introduced.
</success_criteria>

<output>
Create `.planning/phases/05-verification-and-docs/05-02-SUMMARY.md` when done.
</output>
