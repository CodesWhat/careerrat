---
phase: ROL-API-06-canonical-db-app-shell
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/boards-route.test.mjs
  - tests/search-route.test.mjs
  - tests/scanner-seen-set-db.test.mjs
autonomous: true
requirements:
  - APP-02
  - APP-03
user_setup: []
must_haves:
  truths:
    - "APP-02: Source setup and scanner context product routes read/write DB-derived state per D-06, D-07, and D-08."
    - "APP-03: Scanner and source setup routes do not use generated tracker or legacy config files as product state per D-04 and D-05."
  artifacts:
    - tests/boards-route.test.mjs
    - tests/search-route.test.mjs
    - tests/scanner-seen-set-db.test.mjs
  key_links:
    - "boards-route tests -> DB sourceConfig verbs."
    - "search-route tests -> DB scanner context/results."
    - "scanner seen-set tests -> DB duplicate context for runSourcedScan."
---

<objective>
Create RED source setup and scanner context contracts for DB-first product routes.

Purpose: Phase 6 must move obvious source setup and scanner seams to DB before Phase 7 auto-sourcing builds on them.
Output: Updated source/search tests and a new scanner seen-set test that fail before implementation.
</objective>

## Phase Goal

**As a** Rolester app user, **I want to** set up sources and inspect scanner results through DB-backed APIs, **so that** later sourcing runs do not depend on legacy file shims.

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/ROL-API-06-canonical-db-app-shell/06-CONTEXT.md
@.planning/phases/ROL-API-06-canonical-db-app-shell/06-RESEARCH.md
@.planning/phases/ROL-API-06-canonical-db-app-shell/06-VALIDATION.md
@AGENTS.md
@src/cli/boards-route.mjs
@src/cli/search-route.mjs
@scripts/scan-sourced.mjs
@src/core/db/verbs/source-config.mjs
@src/core/db/verbs/sourced.mjs
@src/core/scoring/sourced-scanner.mjs
@tests/boards-route.test.mjs
@tests/search-route.test.mjs
</context>

## Artifacts This Phase Produces

- New tests: `tests/db-app-shell-regression.test.mjs`, `apps/web/src/app-shell/NavList.test.jsx`, `tests/scanner-seen-set-db.test.mjs`, `tests/db-scan-context.test.mjs`.
- Updated tests: `tests/packet-route.test.mjs`, `tests/boards-route.test.mjs`, `tests/search-route.test.mjs`.
- New DB scanner helper file: `src/core/db/scan-context.mjs`.
- New internal symbols: `DEBUG_EXPORT_ROUTES`, `isDebugExportRoute`, `readPacketApplicationsFromDb`, `readDbSearchSources`, `writeDbSearchSources`, `readDbSearchResults`, `hasConfiguredDbSourcesOnly`, `buildDbSeenSets`, `readDbScannerRows`, `buildSeenSetsForRun`.
- Changed route contracts: `GET /api/packet/list`, `GET /api/packet`, `POST /api/boards/add`, `POST /api/search/scan`, `GET /api/search/results`, `GET /api/search/sources`.
- Removed normal product affordance: the `Classic` external nav item from `/app`.
- New CLI flags: none.
- New DB fields or migrations: none.

<tasks>

<task type="auto">
  <name>Task 1: Add RED DB source setup route tests</name>
  <files>tests/boards-route.test.mjs</files>
  <read_first>
    - tests/boards-route.test.mjs
    - src/cli/boards-route.mjs
    - src/core/db/connection.mjs
    - src/core/db/verbs/source-config.mjs
    - src/core/providers/search-sources.mjs
  </read_first>
  <action>Update `POST /api/boards/add` tests so source setup requires an initialized DB, returns 409 when no DB exists, reads the current `search-sources` config through `sourceConfigGet`, writes the next config through `sourceConfigPut`, returns the existing `{ ok:true, searches }` list shape, and never creates or updates the legacy search-sources YAML file per D-04, D-06, D-07, and D-08. Keep `POST /api/boards/preview` deterministic and unchanged.</action>
  <acceptance_criteria>
    - Existing preview tests continue to expect 200 without DB because preview is pure URL construction.
    - Add-route tests initialize DB with `openDb({ repoRoot })` or candidate setup before successful writes.
    - Missing DB add-route test expects HTTP 409.
    - Successful add-route tests assert `sourceConfigGet({ repoRoot, name: "search-sources" })` contains the new search and the legacy YAML file is absent.
    - The RED tests fail against current `boards-route.mjs` because it writes the legacy YAML file.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'node --test tests/boards-route.test.mjs; test $? -ne 0'</automated>
  </verify>
  <done>Source setup route tests enforce DB source-config writes and fail against the current file-backed implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add RED DB scanner context and results tests</name>
  <files>tests/search-route.test.mjs</files>
  <read_first>
    - tests/search-route.test.mjs
    - src/cli/search-route.mjs
    - scripts/scan-sourced.mjs
    - src/core/db/verbs/source-config.mjs
    - src/core/db/verbs/sourced.mjs
  </read_first>
  <action>Update `tests/search-route.test.mjs` so `POST /api/search/scan`, `GET /api/search/results`, and `GET /api/search/sources` are DB-only product routes. Missing DB should return 409. DB with no configured sources should return the existing no-config 400 for scans. `GET /api/search/sources` should count only DB source config rows. `GET /api/search/results` should return DB sourced rows and ignore any existing scan-result JSON files per D-04, D-05, D-06, and D-07.</action>
  <acceptance_criteria>
    - Tests no longer treat legacy source config files as sufficient for product search routes.
    - Tests seed `sourceConfigGet`/`companyAtsUpsert` or sourced rows through DB helpers for successful cases.
    - A test writes a contradictory scan-result JSON file and proves `GET /api/search/results` returns DB rows instead.
    - The RED tests fail against current `search-route.mjs` because it still has legacy no-DB/file-result paths.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'node --test tests/search-route.test.mjs; test $? -ne 0'</automated>
  </verify>
  <done>Search route tests enforce DB-only scanner context/results and fail against the current mixed file-backed implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Add RED scanner seen-set DB tests</name>
  <files>tests/scanner-seen-set-db.test.mjs</files>
  <read_first>
    - scripts/scan-sourced.mjs
    - src/core/tracker/tracker-data.mjs
    - src/core/db/verbs/source-config.mjs
    - src/core/db/verbs/sourced.mjs
    - tests/search-route.test.mjs
  </read_first>
  <action>Create `tests/scanner-seen-set-db.test.mjs` with `node:test`. Seed an initialized DB with a tracked supported ATS company and an existing sourced/application row. Also write a contradictory generated tracker export containing a different seen URL. Run `runSourcedScan({ repoRoot, env:{}, fetchImpl, write:false, intake:false })` against a stub ATS response that includes one DB-duplicate offer, one tracker-export-only duplicate offer, and one new offer. Assert DB duplicates are filtered and tracker-export-only rows are not treated as seen when DB exists per D-04, D-05, D-06, and D-07.</action>
  <acceptance_criteria>
    - The test file seeds DB source config and rows without relying on generated tracker export state.
    - The test proves DB-mode seen sets come from DB rows by checking kept/duplicate behavior.
    - The RED test fails against current `scripts/scan-sourced.mjs` because it uses the legacy tracker seen-set helper even when DB exists.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'node --test tests/scanner-seen-set-db.test.mjs; test $? -ne 0'</automated>
  </verify>
  <done>Scanner dedupe tests enforce DB-derived seen sets and fail against the current tracker-export seen-set implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> source setup API | User-submitted board URLs cross into local source config writes. |
| Browser -> search API | Product scanner actions and result reads cross into local scanner orchestration. |
| Scanner -> DB/application rows | Dedupe and result context must use canonical DB state, not generated exports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering | `src/cli/boards-route.mjs` source setup writes | high | mitigate | Tests require `sourceConfigGet`/`sourceConfigPut` DB writes and absence of legacy YAML writes. |
| T-06-07 | Tampering | `src/cli/search-route.mjs` scanner results | high | mitigate | Tests require DB-only source counts, scan results, and missing-DB 409 responses. |
| T-06-08 | Repudiation | `scripts/scan-sourced.mjs` dedupe context | medium | mitigate | Seen-set tests prove DB rows drive duplicate filtering when DB exists. |
| T-06-SC | Tampering | npm installs | high | mitigate | No package-manager install tasks are planned; executors must not add or upgrade packages in Phase 6. |
</threat_model>

<verification>
Wave 0 RED verification:
- `bash -lc 'node --test tests/boards-route.test.mjs; test $? -ne 0'`
- `bash -lc 'node --test tests/search-route.test.mjs; test $? -ne 0'`
- `bash -lc 'node --test tests/scanner-seen-set-db.test.mjs; test $? -ne 0'`
</verification>

<success_criteria>
- Source setup, scanner route, and scanner seen-set DB contracts are present and intentionally RED before implementation.
- APP-02 and APP-03 have route-level coverage for source setup and scanner context.
</success_criteria>

<output>
Create `.planning/phases/ROL-API-06-canonical-db-app-shell/06-03-SUMMARY.md` when done.
</output>
