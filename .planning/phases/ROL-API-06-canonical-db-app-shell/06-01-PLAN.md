---
phase: ROL-API-06-canonical-db-app-shell
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/db-app-shell-regression.test.mjs
  - apps/web/src/app-shell/NavList.test.jsx
autonomous: true
requirements:
  - APP-01
  - APP-03
  - APP-04
user_setup: []
must_haves:
  truths:
    - "APP-01: React /app normal navigation has no Classic link or normal legacy route affordance per D-01, D-02, D-03."
    - "APP-03: Product app files do not use generated tracker or activity exports as source of truth per D-04 and D-05."
    - "APP-04: Static guards fail when product files regain generated-file, raw-feed, or legacy product-route dependencies per D-09 and D-10."
  artifacts:
    - tests/db-app-shell-regression.test.mjs
    - apps/web/src/app-shell/NavList.test.jsx
  key_links:
    - "NavList.jsx -> NavList.test.jsx enforces normal product nav retirement."
    - "Product file allowlist -> db-app-shell-regression.test.mjs enforces debug/export-only exceptions."
---

<objective>
Create the RED regression contracts for the canonical DB app shell navigation and static dependency boundary.

Purpose: Phase 6 must fail fast if /app still advertises legacy product paths or if product code reads generated tracker/activity exports instead of DB-derived routes.
Output: Two failing tests that lock APP-01, APP-03, APP-04 before implementation begins.
</objective>

## Phase Goal

**As a** Rolester app user, **I want to** use the Electron/React app as the DB-backed product surface, **so that** normal workflows do not depend on compatibility exports or legacy static pages.

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/APP-PRODUCT-PLAN.md
@.planning/phases/ROL-API-06-canonical-db-app-shell/06-CONTEXT.md
@.planning/phases/ROL-API-06-canonical-db-app-shell/06-RESEARCH.md
@.planning/phases/ROL-API-06-canonical-db-app-shell/06-VALIDATION.md
@AGENTS.md
@docs/ARCHITECTURE.md
@apps/web/src/app-shell/NavList.jsx
@apps/web/src/App.jsx
@apps/web/src/app-shell/DashboardContext.jsx
@apps/web/src/lib/api.js
@src/cli/tracker-dev.mjs
@src/cli/dashboard-route.mjs
@src/cli/data-route.mjs
@src/cli/packet-route.mjs
@src/cli/boards-route.mjs
@src/cli/search-route.mjs
@tests/company-discovery-regression.test.mjs
</context>

## Artifacts This Phase Produces

- New tests: `tests/db-app-shell-regression.test.mjs`, `apps/web/src/app-shell/NavList.test.jsx`, `tests/scanner-seen-set-db.test.mjs`.
- Updated tests: `tests/packet-route.test.mjs`, `tests/boards-route.test.mjs`, `tests/search-route.test.mjs`.
- New DB scanner helper file: `src/core/db/scan-context.mjs`.
- New internal symbols: `DEBUG_EXPORT_ROUTES`, `isDebugExportRoute`, `readPacketApplicationsFromDb`, `readDbSearchSources`, `writeDbSearchSources`, `readDbSearchResults`, `hasConfiguredDbSourcesOnly`, `buildDbSeenSets`, `readDbScannerRows`, `buildSeenSetsForRun`.
- Changed route contracts: `GET /api/packet/list`, `GET /api/packet`, `POST /api/boards/add`, `POST /api/search/scan`, `GET /api/search/results`, `GET /api/search/sources`.
- Removed normal product affordance: the `Classic` external nav item from `/app`.
- New CLI flags: none.
- New DB fields or migrations: none.

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Make Electron/React /app DB-source-of-truth and remove compatibility surfaces from product paths | 01-07 | COVERED | Tests and implementation plans cover nav, packet, source setup, scanner context, and static guards.
REQ | APP-01 | /app is canonical product surface; compatibility surfaces are not normal UX | 01,04 | COVERED | Nav test and app shell implementation remove Classic/legacy product affordances per D-01..D-03.
REQ | APP-02 | Dashboard, packet, tracker/activity, scanner context, and source setup views read DB-derived snapshots | 02,03,05,06,07 | COVERED | Packet/source/scanner route tests and implementations move product seams to DB-derived reads.
REQ | APP-03 | tracker.json/activity.jsonl are compatibility/export artifacts only | 01,02,03,05,07 | COVERED | Static guard plus DB-only packet/search tests prevent product reads from generated exports.
REQ | APP-04 | Static guards prevent product dependencies on generated tracker/activity files | 01,04 | COVERED | `tests/db-app-shell-regression.test.mjs` owns the guard and allowlist.
RESEARCH | - | No new package installs; use existing React/Vite/node:test/DB verbs | 01-07 | COVERED | Plans install no packages; T-06-SC blocks package-manager changes.
RESEARCH | - | Reuse `requireDb`, `sourceConfigGet`, `sourceConfigPut`, `assembleTrackerObject`, route mount patterns | 05,06,07 | COVERED | Implementation actions name these owners.
RESEARCH | - | `packet-route.mjs` must stop using storage adapter tracker reads | 02,05 | COVERED | Packet RED tests and implementation cover this exact seam.
RESEARCH | - | `boards-route.mjs` must stop product writes to search-sources YAML | 03,06 | COVERED | Source setup tests and implementation use DB source config verbs.
RESEARCH | - | `search-route.mjs` and scanner context must stop product reads from scan-result files and tracker exports | 03,07 | COVERED | Search route and seen-set tests plus DB helper cover this seam.
CONTEXT | D-01 | Remove legacy product affordances from app path | 01,04 | COVERED | Nav tests and implementation cite D-01.
CONTEXT | D-02 | Legacy/generated surfaces only debug/export utilities | 01,04 | COVERED | Static guard allowlist and debug route constants cite D-02.
CONTEXT | D-03 | New app structure, not compatibility project | 04 | COVERED | App shell implementation removes compatibility framing.
CONTEXT | D-04 | Product routes DB-derived and fail closed without DB | 02,03,05,06,07 | COVERED | Packet, board add, and search routes verify 409/no DB behavior.
CONTEXT | D-05 | Generated tracker/activity exports not source of truth | 01,02,03,05,07 | COVERED | Static guard and route tests cite D-05.
CONTEXT | D-06 | Dashboard, packet, tracker/activity, scanner context, source setup converge on DB | 02,03,05,06,07 | COVERED | DB-derived route contracts cover each surface.
CONTEXT | D-07 | Move source setup/scanner seams DB-first now | 03,06,07 | COVERED | Board and scanner plans cover this directly.
CONTEXT | D-08 | One app-local DB/API layer for later auto-sourcing | 06,07 | COVERED | Source config and scanner route actions preserve app-local DB APIs.
CONTEXT | D-09 | Aggressive static regression guards | 01,04 | COVERED | Static guard plan and implementation cite D-09.
CONTEXT | D-10 | Narrow debug/export allowlists | 01,04 | COVERED | Guard and tracker-dev debug constants cite D-10.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Add RED static guard for DB-only product dependencies</name>
  <files>tests/db-app-shell-regression.test.mjs</files>
  <read_first>
    - tests/company-discovery-regression.test.mjs
    - src/cli/tracker-dev.mjs
    - src/cli/packet-route.mjs
    - src/cli/boards-route.mjs
    - src/cli/search-route.mjs
    - apps/web/src/App.jsx
    - apps/web/src/app-shell/NavList.jsx
    - apps/web/src/lib/api.js
  </read_first>
  <action>Create `tests/db-app-shell-regression.test.mjs` using `node:test`, `node:assert/strict`, and `readFileSync`. Define explicit `PRODUCT_FILES` for React app code and app-facing route modules, plus a named debug/export allowlist for `tracker-dev.mjs` and CLI export owners. Strip JavaScript comments before token scans so comments cannot create false positives. Add assertions that product files do not contain generated tracker export reads, generated activity export reads, storage-adapter tracker calls, raw tracker/activity API feed calls, legacy source setup file reads, scan-result file reads, or legacy seen-set helper usage as source of truth. Add a separate assertion that any remaining compatibility route references in `tracker-dev.mjs` appear behind named debug/export route constants per D-02, D-09, and D-10.</action>
  <acceptance_criteria>
    - `tests/db-app-shell-regression.test.mjs` exists and imports only Node built-ins.
    - The test scans `apps/web/src/App.jsx`, `apps/web/src/app-shell/NavList.jsx`, `apps/web/src/app-shell/DashboardContext.jsx`, `apps/web/src/lib/api.js`, `src/cli/dashboard-route.mjs`, `src/cli/data-route.mjs`, `src/cli/packet-route.mjs`, `src/cli/boards-route.mjs`, `src/cli/search-route.mjs`, and `scripts/scan-sourced.mjs`.
    - The RED test fails against current source because current product code still has at least one legacy/export dependency or unclassified debug/export reference.
    - The test includes narrow allowlist assertions for debug/export owners and does not forbid legitimate CLI import/export modules globally.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'node --test tests/db-app-shell-regression.test.mjs; test $? -ne 0'</automated>
  </verify>
  <done>The static DB app shell guard is present, intentionally RED before implementation, and covers APP-03/APP-04 plus D-09/D-10.</done>
</task>

<task type="auto">
  <name>Task 2: Add RED NavList product-nav retirement test</name>
  <files>apps/web/src/app-shell/NavList.test.jsx</files>
  <read_first>
    - apps/web/src/app-shell/NavList.jsx
    - apps/web/src/app-shell/AppShell.jsx
    - apps/web/src/pages/SetupReadinessCard.test.jsx
    - apps/web/src/network/NetworkPage.test.jsx
    - apps/web/src/components/icons.jsx
  </read_first>
  <action>Create `apps/web/src/app-shell/NavList.test.jsx` with Vitest and `renderToStaticMarkup` under `MemoryRouter`. Mock `useNeedsYouCount()` to return a nonzero count for badge coverage. Assert the rendered nav includes Home, Settings, Onboarding, Inbox, Jobs, Calendar, Network, and Library; assert it does not render the `Classic` label and does not render a normal link to the legacy tracker route per D-01, D-02, and APP-01.</action>
  <acceptance_criteria>
    - The test file imports `describe`, `expect`, `it`, and `vi` from Vitest and renders `NavList` with `MemoryRouter`.
    - The test asserts expected normal SPA nav labels and the Inbox badge still render.
    - The RED test fails against current `NavList.jsx` because it still renders `Classic`.
    - The test command is non-watch mode through the existing web workspace `vitest run` script.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'npm --workspace apps/web run test -- src/app-shell/NavList.test.jsx; test $? -ne 0'</automated>
  </verify>
  <done>The nav retirement contract is present, intentionally RED before implementation, and covers APP-01 plus D-01/D-02/D-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> React /app | User-visible product navigation and route discovery happen in untrusted browser-rendered UI. |
| React/app route modules -> local files | Product code must not read generated compatibility exports as canonical state. |
| Debug/export allowlist -> generated files | Compatibility exports remain accessible only through named debug/export owners. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | Product route generated-file reads | high | mitigate | `tests/db-app-shell-regression.test.mjs` fails product-file dependencies on generated tracker/activity exports and storage-adapter tracker reads. |
| T-06-02 | Spoofing | React product navigation | medium | mitigate | `NavList.test.jsx` fails if legacy/static routes are advertised as normal product UX. |
| T-06-03 | Information Disclosure | Debug/export raw feeds | medium | mitigate | Static guard requires named debug/export allowlist and prevents raw feed paths from app product files. |
| T-06-SC | Tampering | npm installs | high | mitigate | No package-manager install tasks are planned; executors must not add or upgrade packages in Phase 6. |
</threat_model>

<verification>
Wave 0 RED verification:
- `bash -lc 'node --test tests/db-app-shell-regression.test.mjs; test $? -ne 0'`
- `bash -lc 'npm --workspace apps/web run test -- src/app-shell/NavList.test.jsx; test $? -ne 0'`
</verification>

<success_criteria>
- APP-01, APP-03, and APP-04 have failing regression coverage before implementation.
- D-01 through D-03 and D-09 through D-10 are represented in executable tests.
- No product source files are modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/ROL-API-06-canonical-db-app-shell/06-01-SUMMARY.md` when done.
</output>
