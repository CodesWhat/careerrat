---
phase: ROL-API-06-canonical-db-app-shell
plan: 06
type: execute
wave: 1
depends_on:
  - 06-03
files_modified:
  - src/cli/boards-route.mjs
autonomous: true
requirements:
  - APP-02
  - APP-03
user_setup: []
must_haves:
  truths:
    - "APP-02: Product source setup writes the DB `search-sources` source config row per D-06, D-07, and D-08."
    - "APP-03: Product source setup does not write legacy search source files as canonical state per D-05."
  artifacts:
    - src/cli/boards-route.mjs
  key_links:
    - "POST /api/boards/add -> sourceConfigGet/sourceConfigPut"
    - "boards-route tests -> legacy file absence assertion"
---

<objective>
Migrate board/source setup product writes from legacy files to DB source config.

Purpose: Phase 7 auto-sourcing needs product source setup to be DB-owned instead of YAML-owned.
Output: `POST /api/boards/add` writes DB source config and fails closed when DB is missing.
</objective>

## Phase Goal

**As a** Rolester app user, **I want to** add job-board sources through DB-backed product APIs, **so that** source setup feeds scanner runs without legacy config shims.

<execution_context>
@/Users/sbenson/.codex/gsd-core/workflows/execute-plan.md
@/Users/sbenson/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/ROL-API-06-canonical-db-app-shell/06-CONTEXT.md
@.planning/phases/ROL-API-06-canonical-db-app-shell/06-RESEARCH.md
@.planning/phases/ROL-API-06-canonical-db-app-shell/06-VALIDATION.md
@AGENTS.md
@src/cli/boards-route.mjs
@src/core/db/verbs/source-config.mjs
@src/core/providers/search-sources.mjs
@tests/boards-route.test.mjs
</context>

## Artifacts This Phase Produces

- New tests: `tests/db-app-shell-regression.test.mjs`, `apps/web/src/app-shell/NavList.test.jsx`, `tests/scanner-seen-set-db.test.mjs`.
- Updated tests: `tests/packet-route.test.mjs`, `tests/boards-route.test.mjs`, `tests/search-route.test.mjs`.
- New DB scanner helper file: `src/core/db/scan-context.mjs`.
- New internal symbols: `DEBUG_EXPORT_ROUTES`, `isDebugExportRoute`, `readPacketApplicationsFromDb`, `readDbSearchSources`, `writeDbSearchSources`, `readDbSearchResults`, `hasConfiguredDbSourcesOnly`, `buildDbSeenSets`, `readDbScannerRows`, `buildSeenSetsForRun`.
- Changed route contracts: `GET /api/packet/list`, `GET /api/packet`, `POST /api/boards/add`, `POST /api/search/scan`, `GET /api/search/results`, `GET /api/search/sources`.
- Removed normal product affordance: the `Classic` external nav item from `/app`.
- New CLI flags: none.
- New DB fields or migrations: none.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist board additions through DB source config verbs</name>
  <files>src/cli/boards-route.mjs</files>
  <read_first>
    - src/cli/boards-route.mjs
    - src/core/db/verbs/source-config.mjs
    - src/core/providers/search-sources.mjs
    - tests/boards-route.test.mjs
  </read_first>
  <behavior>
    - `POST /api/boards/preview` stays deterministic and does not require DB.
    - `POST /api/boards/add` returns 409 when no DB exists.
    - Successful board additions read and write the DB `search-sources` config and do not create the legacy config file.
  </behavior>
  <action>After confirming boards tests are RED, remove legacy file read/write imports and `atomicWriteFile` usage from `src/cli/boards-route.mjs`. Import `sourceConfigGet` and `sourceConfigPut`. Add internal `readDbSearchSources(pathCtx)` and `writeDbSearchSources(pathCtx, next)` helpers around `sourceConfigGet({ ...pathCtx, name: "search-sources" })` and `sourceConfigPut({ ...pathCtx, name: "search-sources", data: next })`. Keep `addSearchFromUrl`, schema validation through `config/search-sources.schema.json`, and `listSearches(next)` response behavior. Convert `NO_DATABASE` into HTTP 409 `{ ok:false, error }`; keep malformed URL and schema failures as 400 per D-04, D-06, D-07, and D-08.</action>
  <acceptance_criteria>
    - `src/cli/boards-route.mjs` no longer imports `atomicWriteFile`, `mkdirSync`, `dirname`, or `userPath` for product source setup writes.
    - `POST /api/boards/add` writes DB source config and returns the same `searches` list shape.
    - Successful tests prove the legacy search-sources file remains absent.
    - `POST /api/boards/preview` tests still pass unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/boards-route.test.mjs tests/db-app-shell-regression.test.mjs</automated>
  </verify>
  <done>Product source setup writes DB source config only and remains compatible with existing board preview behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> `/api/boards/add` | User-submitted source URLs become scanner source config. |
| Boards route -> DB source config | Source setup state is persisted to canonical DB config rows. |
| Boards route -> schema file | Static schema validates config shape but is not user state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering | `src/cli/boards-route.mjs` source setup writes | high | mitigate | Use `sourceConfigGet`/`sourceConfigPut`, validate schema, and assert no legacy config file writes. |
| T-06-10 | Denial of Service | `POST /api/boards/add` invalid input | low | mitigate | Preserve existing body cap and 400 validation for missing/unparseable URLs. |
| T-06-SC | Tampering | npm installs | high | mitigate | No package-manager install tasks are planned; executors must not add or upgrade packages in Phase 6. |
</threat_model>

<verification>
- `node --test tests/boards-route.test.mjs tests/db-app-shell-regression.test.mjs`
</verification>

<success_criteria>
- APP-02 source setup writes are DB-owned.
- APP-03 guard confirms product source setup no longer depends on legacy source files as state.
</success_criteria>

<output>
Create `.planning/phases/ROL-API-06-canonical-db-app-shell/06-06-SUMMARY.md` when done.
</output>
