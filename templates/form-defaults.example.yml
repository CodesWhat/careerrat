source: "Job Board"
option_aliases:
  source:
    - "Other"
  location: []
work_authorization: "Yes"
requires_sponsorship: "No"
current_employer:
current_title:
expected_base:
linkedin:
github:
portfolio:
# Deprecated compatibility only. CareerRat never treats this as consent.
eeo_default: "Prefer not to answer"
voluntary_self_identification:
  enabled: false
  default_action: "leave_blank"
  confirmed_at:
  answers: {}
screening_answers: {}
# Set true only if you want the agent to pause before filling configured
# current_employer/current_title values on every application.
confirm_current_role: false
